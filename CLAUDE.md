# Shakespeare Academy — contexte projet

Document de référence complet : `docs/Shakespeare_Academy_Cahier_de_Cadrage_v1.0.docx`.
**En cas de doute ou de divergence, ce cahier de cadrage fait foi — pas cette page, qui n'en est qu'un résumé opérationnel.**
Décisions métier encore non validées par l'école : `DECISIONS_PENDING.md` — ne jamais inventer une valeur pour un point qui y figure comme `OUVERT`.

## Doctrine

Critère central de réussite : sécuriser toute la chaîne de l'inscription à la clôture journalière, avec une traçabilité financière totale. Une opération financière validée n'est **jamais** modifiée ni supprimée : toute correction passe par une annulation contrôlée et une contre-écriture (RG09, RG18).

Avant d'ajouter une fonctionnalité, se demander : *est-elle nécessaire pour sécuriser l'inscription, la facturation, l'encaissement ou la clôture d'un élève, sans ressaisie ni incohérence ?* Si non, elle n'est pas prioritaire (cf. §1.2 du cadrage, hors périmètre initial : notes, emplois du temps, cantine, transport, paie, portail parent, mobile money, appli mobile).

## Règles non négociables (voir cadrage §5 pour le détail complet)

- **RG01** — Un élève a un matricule permanent et une seule inscription active par année scolaire.
- **RG02** — Une classe appartient à un niveau, une section et une année scolaire.
- **RG03** — Les frais sont versionnés par année scolaire ; modifier un tarif ne modifie jamais les factures déjà émises.
- **RG08** — Un trop-perçu est interdit (sauf compte d'avance explicitement activé — non activé au MVP).
- **RG09** — Une transaction validée est immuable. L'annulation crée une transaction inverse liée à l'originale.
- **RG10** — Les numéros de reçu sont uniques, séquentiels et générés côté serveur.
- **RG13** — La solvabilité est **calculée**, jamais saisie manuellement.
- **RG15** — Toutes les actions sensibles sont journalisées (utilisateur, horodatage, ancienne/nouvelle valeur) — voir `AuditService` (`apps/api/src/audit/`), append-only par conception.
- **RG16** — Les montants XAF sont des entiers, aucun calcul financier en flottant.
- **RG18** — Aucune suppression physique d'élève, inscription, facture ou transaction en production.
- **Permissions fines, jamais par rôle seul** (§3, §14) : chaque action sensible est gardée par une permission précise (`@RequirePermission('CODE')`), vérifiée côté serveur même pour un appel API direct — masquer un bouton côté client n'est jamais une sécurité.
- **Le Secrétaire-caissier ne valide jamais ses propres annulations, remises exceptionnelles ou écarts de caisse** — seule la Direction approuve (à construire au Lot 4).

## Ordre de développement (cadrage §17, détaillé dans le message de validation du plan)

0. Cadrage — décisions métier, nomenclatures, tarifs, formats de reçus (`DECISIONS_PENDING.md`).
1. **Socle** — auth, utilisateurs/rôles/permissions, années scolaires, structure académique. **FAIT, voir état actuel ci-dessous.**
2. **Élèves, responsables, inscriptions et réinscriptions (sans paiement). FAIT, voir état actuel ci-dessous.**
3. **Tarifs, factures, trois tranches, remises, solvabilité calculée. FAIT, voir état actuel ci-dessous.**
4. **Paiements et reçus (encaissement, réimpression, autres frais). FAIT, voir état actuel ci-dessous — la caisse au sens strict (ouverture/clôture de session, D20-D23) n'est pas construite.**
5. **Sorties financières, clôture de journée (rapport). FAIT partiellement, voir état actuel ci-dessous — rapports/exports plus larges (comptables, rapprochements) non construits.**
6. Import initial, formation, pilote, sauvegarde, mise en production.

**Extension de périmètre « vie scolaire 360° » (21 septembre 2026, décidée par le propriétaire du projet, décision D50 tranchée).** Lots 7 à 14 **proposés** : 7 référentiel pédagogique et personnel, 8 emploi du temps, 9 assiduité des élèves, 10 pointage des enseignants, 11 comptes parents et portail, 12 notifications, 13 messagerie sécurisée et annonces, 14 pilotage 360°. Détail, règles RV01 à RV12 et critères d'acceptation : `docs/Addendum_Cadrage_v1.1_Vie_Scolaire_360.md` ; décisions D50 à D78 : `DECISIONS_PENDING.md` §17. **Tant que ces décisions ne sont pas validées par la Direction, ne coder que ce qui repose sur une valeur paramétrable, jamais une valeur codée en dur** (horaires, seuils de retard, délais, rétention). Le critère de la doctrine ci-dessus (sécuriser l'inscription, la facturation, l'encaissement) reste celui des Lots 1 à 6 ; les Lots 7 et suivants suivent l'addendum, qui ne modifie aucune règle financière (RG08, RG09, RG10, RG16, RG18).

## Stack

Next.js (apps/web, scaffoldé — aucune page métier encore construite, le Lot 1 est backend uniquement, comme l'a été le socle multi-tenant d'Elyon) · NestJS 11 (apps/api) · PostgreSQL 16 / Prisma 6.19.3 (apps/api/prisma/schema.prisma) · Docker Compose (Postgres dev + test uniquement pour l'instant).

**Choix de version Prisma délibéré** : Prisma 7/8-rc introduisent une rupture majeure (plus d'URL dans `schema.prisma`, adapters obligatoires, `prisma.config.ts`) — jugée trop récente et complexe pour ce socle. Rester sur Prisma 6.x (LTS stable) tant qu'une raison concrète n'impose pas la migration.
**`@nestjs/config` et `@nestjs/jwt` fixés en version pré-ESM** (`4.0.4` et `11.0.2` respectivement, `--save-exact`) : leurs versions plus récentes (12.x) publient du pur ESM, incompatible avec la transformation CommonJS de `ts-jest` utilisée par la suite e2e (`Must use import to load ES Module`). Revoir seulement si toute la chaîne de test bascule vers un runner nativement ESM.

## État actuel du scaffold

- **Lot 1 (socle) fait et vérifié** : `apps/api` compile (`npm run build`), démarre (`npm run start:dev`) et sert du trafic réel — vérifié manuellement via `curl` (login réel, création de section réelle, token JWT valide) en plus des tests automatisés.
- `apps/api/prisma/schema.prisma` : uniquement les entités du Lot 1 — `School`, `AcademicYear`, `Section`/`Cycle`/`Level`/`Class`, `User`/`Role`/`Permission`/`RolePermission`, `RefreshToken`, `AuditLog`. Les entités de facturation/paiement/caisse (Lots 3-5) sont **volontairement absentes** : plusieurs règles dont elles dépendent restent `OUVERT` dans `DECISIONS_PENDING.md` (granularité des remises, gestion du trop-perçu, etc.) — les modéliser maintenant risquerait de figer une hypothèse fausse.
- **Mono-établissement, pas multi-tenant** : une seule ligne `School` existe. `schoolId` est présent sur les entités scopées (anticipation d'une extension multi-site future, cadrage §11) mais aucun mécanisme de sélection de tenant n'est construit — `SchoolService.getDefaultId()` (`apps/api/src/school/`) résout systématiquement l'unique établissement. Ne pas confondre avec le modèle multi-tenant d'Elyon (autre projet, header `x-tenant-id`) : ici, il n'y a qu'un seul restaurant... pardon, une seule école.
- **Auth** (`apps/api/src/auth/`) : JWT d'accès court (15 min par défaut, `JWT_ACCESS_TTL`) signé et vérifié côté serveur à chaque requête (`JwtAuthGuard` recharge l'utilisateur + permissions depuis la base à chaque appel, jamais depuis le payload du jeton — un compte désactivé ou un rôle modifié a un effet immédiat). Rafraîchissement via cookie `httpOnly` (`refresh_token`, scope `path=/auth`), rotation à chaque `POST /auth/refresh` (l'ancien jeton est révoqué). Mots de passe hachés Argon2id. Verrouillage après 5 échecs consécutifs (15 min, `MAX_TENTATIVES_CONNEXION`/`DUREE_VERROUILLAGE_MINUTES` dans `auth.service.ts` — décision technique de sécurité, pas une règle métier de l'école, donc non listée dans `DECISIONS_PENDING.md`). Récupération administrée (`PATCH /users/:id/reset-password`, `USER_MANAGE`) : jamais d'auto-inscription ni de lien de réinitialisation par e-mail.
- **Permissions** (`apps/api/src/roles/`) : catalogue de 6 permissions Lot 1 (`SETTINGS_MANAGE`, `USER_MANAGE`, `ROLE_MANAGE`, `ACADEMIC_YEAR_MANAGE`, `ACADEMIC_STRUCTURE_MANAGE`, `AUDIT_LOG_READ`) — **volontairement incomplet** : les codes cités en exemple par le cadrage §3 (`PAYMENT_CREATE`, `PAYMENT_CANCEL_APPROVE`, `CASH_CLOSE`, `EXPENSE_CREATE`) seront ajoutés à `prisma/seed-data.ts` au fur et à mesure que leurs modules (Lots 3-5) seront construits, jamais avant qu'une route ne les vérifie réellement. 5 rôles seedés conformes au tableau du cadrage §3 (`ADMINISTRATEUR`, `DIRECTION`, `SECRETAIRE_CAISSIER`, `COMPTABLE`, `AUDITEUR`) — `SECRETAIRE_CAISSIER`/`COMPTABLE` n'ont encore aucune permission assignée (leur domaine n'existe pas avant les Lots 2-5). `GET /roles`/`GET /permissions` sont lisibles par tout utilisateur authentifié (nécessaire pour peupler un sélecteur de rôle) ; seules les mutations exigent `ROLE_MANAGE`.
- **Structure académique** (`apps/api/src/{sections,cycles,levels,classes}/`) : hiérarchie `Section → Cycle → Level → Class` conforme au modèle recommandé du cadrage §2. Lecture ouverte à tout utilisateur authentifié, écriture derrière `ACADEMIC_STRUCTURE_MANAGE`. `Class.serie`/`Class.groupe` : colonnes prévues par anticipation de la décision D02 (séries de lycée, sous-groupes), non exploitées tant qu'elle reste ouverte.
- **Années scolaires** (`apps/api/src/academic-years/`) : statuts `BROUILLON`/`ACTIVE`/`CLOTUREE`. Une seule année `ACTIVE` à la fois — **activer une nouvelle année échoue explicitement s'il en existe déjà une active** (jamais de bascule automatique silencieuse ; la Direction doit d'abord clôturer l'ancienne). Une année `CLOTUREE` ne peut plus être modifiée.
- **Audit** (`apps/api/src/audit/`) : `AuditService.log()` est **append-only par conception** (aucune méthode de mise à jour/suppression exposée). Sérialise automatiquement les valeurs Prisma (dates comprises) en JSON — jamais besoin de sérialiser soi-même côté appelant. Ne journalise **jamais** un hash de mot de passe, ni avant ni après une réinitialisation. `GET /audit-logs` derrière `AUDIT_LOG_READ`, filtrable par utilisateur/entité/période.
- **Établissement** (`apps/api/src/school/`) : `GET /school` public à tout utilisateur connecté, `PATCH /school` derrière `SETTINGS_MANAGE`. `devise`/`fuseauHoraire` fixés par les exigences non fonctionnelles du cadrage (XAF, Africa/Brazzaville) — pas exposés en modification tant qu'aucun besoin réel de les changer n'apparaît.
- **Tests** : 39 tests e2e (Jest + Supertest, `apps/api/test/*.e2e-spec.ts`) contre une base Postgres dédiée (`shakespeare_academy_test`, jamais la base de dev) — auth (login/lockout/refresh/logout/changement de mot de passe), permissions fines (CA14 : refus serveur direct sans la permission requise), années scolaires (règle d'unicité de l'année active), structure académique (hiérarchie, doublons, filtres), utilisateurs (création/désactivation/réinitialisation), rôles/permissions (catalogue, assignation), audit (journalisation, jamais de hash), établissement. **Suite e2e exécutée avec `--runInBand`** (obligatoire : plusieurs fichiers de test partagent la même base dédiée, une exécution parallèle provoque des collisions de clés uniques/FK entre suites). Fixtures partagées : `prisma/seed-data.ts` (référentiel rôles/permissions/admin, réutilisé par le vrai script de seed **et** par les tests, pour ne jamais faire diverger les deux) + `test/utils/` (`test-app.ts`, `clean-database.ts`, `fixtures.ts`).
- **Seed** (`apps/api/prisma/seed.ts`, `npm run db:seed`) : idempotent, crée l'établissement (nom par défaut "Shakespeare Academy", à corriger via `PATCH /school` avec le vrai nom/adresse une fois connus), les 5 rôles, les 6 permissions Lot 1, et un compte `ADMINISTRATEUR` (`admin@shakespeareacademy.cg` / `ChangeMe123!` par défaut, **à changer immédiatement en production** — `doitChangerMotDePasse: true` dès la création, `ADMIN_SEED_EMAIL`/`ADMIN_SEED_PASSWORD` pour surcharger).
- **Lot 2 (élèves, responsables, inscriptions) fait et vérifié** : mêmes garanties que le Lot 1 (build/lint propres, e2e verts, vérification manuelle réelle via `curl`).
- `NumberSequence` (`apps/api/src/common/number-sequence.service.ts`) : séquence atomique générique (`schoolId`, `type`, `scope`), un seul `upsert` (INSERT ... ON CONFLICT côté Postgres) donc atomique sous accès concurrent sans verrou explicite. Utilisée pour le matricule (`type: 'MATRICULE'`, `scope` vide — permanent, D32) et le numéro d'inscription (`type: 'ENROLLMENT'`, `scope: academicYearId` — par année). Réutilisable telle quelle pour les numéros de reçu/pièce du Lot 4 (RG10).
- **Matricule** : généré automatiquement si non fourni, format `000001` (6 chiffres, sans année — D32 corrigé en cours de Lot 2 : un premier brouillon de `DECISIONS_PENDING.md` proposait un format incluant l'année, qui aurait contredit la permanence du matricule déjà actée dans la même section). **Numéro d'inscription** : `{libellé année}-{5 chiffres}` (ex. `2026-2027-00001`), par année — jamais confondu avec le matricule, qui lui ne change jamais.
- **Élèves/responsables** (`apps/api/src/students/`) : `POST /students` crée l'élève et son premier responsable dans une même transaction Prisma (cadrage §12), conforme au parcours §4.1. Un responsable est retrouvé-ou-créé par téléphone au sein de l'établissement (`Guardian.@@unique([schoolId, telephone])`, même patron R9 qu'Elyon) — un même parent n'est jamais dupliqué pour un deuxième enfant. `POST /students/:id/guardians`/`PATCH /students/guardians/:id`/`DELETE /students/:id/guardians/:guardianId` gèrent les responsables au-delà de la création initiale (fratrie, tuteur additionnel) — le lien `StudentGuardian` peut être détaché sans que RG18 s'applique (ce n'est ni un élève, ni une inscription, ni une facture, ni une transaction).
- **D33 (détection de doublon) implémentée** : `POST /students` compare nom+prénom+date de naissance normalisés (accents/casse ignorés) aux élèves déjà connus. Une correspondance exacte renvoie 409 avec le doublon potentiel dans le corps de la réponse, sauf si `forcerCreation: true` est explicitement passé — alerte non bloquante, jamais un blocage définitif ni une fusion automatique (l'écran de fusion reste différé, comme documenté dans D33).
- **`GET /students/search`** (cadrage §4.1/§12) : matricule, nom/prénom (normalisés), téléphone d'un responsable, ou date de naissance exacte (format `AAAA-MM-JJ`).
- **Inscriptions/réinscriptions** (`apps/api/src/enrollments/`) : `POST /enrollments` couvre les deux cas du cadrage §4.1/§4.2 avec un seul endpoint — `type` (`INSCRIPTION`/`REINSCRIPTION`) est **toujours dérivé côté serveur** (REINSCRIPTION si l'élève a au moins une inscription antérieure, quel que soit son statut), jamais choisi par le client. RG01 (une seule inscription **active** par élève et par année) appliqué en application, pas en contrainte DB : une inscription annulée ne bloque pas une nouvelle inscription pour la même année (correction possible sans contrainte artificielle). Rejette une classe qui n'appartient pas à l'année scolaire indiquée (400) et toute inscription dans une année `CLOTUREE` (409). `POST /enrollments/:id/cancel` exige un motif, journalisé (RG15) — pas d'approbation Direction à ce stade (aucune facture n'y est encore rattachée, contrairement à une annulation de paiement au Lot 4).
- **Alerte impayés antérieurs (§4.2) volontairement absente** : elle suppose des factures/paiements qui n'existent pas avant le Lot 3/4. L'implémenter maintenant aurait exigé d'inventer une donnée financière — reportée telle quelle, sans placeholder trompeur (ex. toujours "aucun impayé").
- **Permissions Lot 2** : `STUDENT_READ` (lecture élèves/responsables/inscriptions — Administrateur, Direction, Secrétaire-caissier, Auditeur) et `ENROLLMENT_MANAGE` (création/modification élève, responsable, inscription, annulation — Administrateur, Secrétaire-caissier uniquement), une seule permission de gestion plutôt qu'une par sous-domaine (le cadrage §3 regroupe ces activités pour le même rôle). Comptable n'a accès à aucune des deux (son périmètre, sorties/rapports, arrive au Lot 5).
- **Tests** : 57 tests e2e au total (+18 pour le Lot 2 : création élève+responsable en transaction, matricules séquentiels, réutilisation d'un responsable partagé, D33 avec/sans `forcerCreation`, recherche, dossier élève, rattachement/détachement de responsable, permissions STUDENT_READ/ENROLLMENT_MANAGE, inscription/réinscription, RG01, classe hors année, année clôturée, annulation avec motif + audit, filtres).
- **Frontend des Lots 1 à 5 fait et vérifié manuellement dans le navigateur** (`apps/web`) : App Router, Tailwind v4. Aucune page pour le Lot 6 (import initial, pilote, sauvegarde) — n'existe pas encore côté backend.
- **Refonte du design (16 septembre 2026)** — l'interface initiale (classes Tailwind par défaut, gris/bleu générique) a été jugée insuffisamment soignée ; refonte complète vers un système de design propre plutôt qu'un simple ravalement de couleurs. `app/globals.css` définit une palette indigo/or via le bloc `@theme` de Tailwind v4 (génère automatiquement les classes `bg-primary`, `text-ink-muted`, etc. — jamais de couleur Tailwind par défaut du type `slate-500` dans le code applicatif désormais), `color-scheme: light` explicite sur `:root` (empêche le navigateur d'appliquer ses styles sombres natifs aux champs/boutons selon le thème système). Police `Fraunces` (`next/font/google`, `app/layout.tsx`, variable `--font-display`) pour les titres, en plus des `Geist Sans`/`Geist Mono` déjà en place. `components/ui.tsx` réécrit en bibliothèque de composants partagés (`Card`, `Button` à 5 variantes, `Input`/`Select`/`Field`, `Badge`, `StatCard`, `EmptyState`, `Stepper`) — remplace le style inline dupliqué page par page. `components/app-shell.tsx` (remplace `app-header.tsx`, supprimé) : sidebar fixe à gauche en desktop, tiroir coulissant avec hamburger en mobile (`lucide-react`, nouvelle dépendance, pour les icônes). Migration mécanique des classes `slate-*` vers les tokens du thème sur toutes les pages existantes (script Node ponctuel, non conservé). Toutes les pages Lot 1/2/3 vérifiées dans le navigateur en desktop (1280px) et mobile (375px), y compris avec `prefers-color-scheme: dark` émulé (aucune page n'a de variante sombre dédiée — `color-scheme: light` garantit qu'aucune ne casse pour autant).
- **Bug frontend trouvé et corrigé pendant cette refonte** : `lib/api.ts` traitait tout 401 (y compris depuis `/auth/login`/`/auth/refresh` eux-mêmes) comme "session expirée, tenter un rafraîchissement" — un simple mauvais mot de passe déclenchait un rafraîchissement (qui échoue, aucune session valide) puis affichait "Session expirée, veuillez vous reconnecter." au lieu du vrai message serveur "Identifiant ou mot de passe incorrect.". Corrigé via une liste `AUTH_ENTRY_POINTS` (`/auth/login`, `/auth/refresh`) exclue de cette logique — ces deux routes affichent toujours leur message réel.
- **Polish visuel des pages restantes fait (16 septembre 2026)** — le premier passage de la refonte n'avait donné un traitement bespoke qu'à Login/Dashboard/AppShell ; les autres pages n'avaient reçu que la migration mécanique des classes `slate-*`. Complété sur Élèves (liste), dossier élève, Structure académique, Utilisateurs & rôles, Journal d'audit, Établissement et Années scolaires : avatars à initiales (élèves, utilisateurs), icônes `lucide-react` par section, badges colorés sémantiquement (ex. `actionColor()` sur le journal d'audit — vert pour *_CREATE/APPROVE/REQUEST, bleu pour *_UPDATE, orange pour *_CANCEL/REJECT, rouge pour *_DELETE), `EmptyState` illustré partout où une liste peut être vide, étapes numérotées en badges circulaires sur Structure académique (Section→Cycle→Niveau→Classe). **Vue en cartes sur mobile** (`sm:hidden`/`hidden sm:block`) en plus du tableau desktop existant, pour Élèves et Utilisateurs — les tableaux HTML restent illisibles sur un écran de téléphone même avec un simple `overflow-x-auto`, une vraie disposition alternative était nécessaire pour l'exigence "compatible mobile". Aucun changement de comportement, purement présentationnel — vérifié dans le navigateur (élève de test créé/consulté/nettoyé).
- **Auth frontend** (`apps/web/lib/api.ts`, `apps/web/contexts/auth-context.tsx`) : le jeton d'accès ne vit **qu'en mémoire** (jamais `localStorage` ni cookie non-httpOnly), cohérent avec le choix côté API — au chargement de la page, un rafraîchissement silencieux via `POST /auth/refresh` (cookie httpOnly) restaure la session sans redemander le mot de passe. `api.ts` retente automatiquement une requête ayant reçu 401 après un rafraîchissement réussi ; sinon, redirige vers `/login`. `doitChangerMotDePasse` (renvoyé par `/auth/login`, pas par `/auth/me`) redirige vers `/changer-mot-de-passe` avant tout accès au tableau de bord.
- **Permissions côté client** : `useAuth().hasPermission(code)` masque des boutons/liens pour l'ergonomie uniquement (ex. nav "Utilisateurs & rôles" visible seulement avec `USER_MANAGE`) — **jamais un contrôle de sécurité**, le serveur revérifie systématiquement (§14, déjà appliqué côté API). Une page consultée sans la permission de mutation reste lisible avec ses champs désactivés plutôt que masquée entièrement (ex. `/parametres/etablissement` sans `SETTINGS_MANAGE`).
- **Écrans Lot 1** : connexion, changement de mot de passe forcé, tableau de bord (année active + effectif + raccourcis), établissement, années scolaires (création/activation/clôture), structure académique (Section→Cycle→Niveau→Classe en 4 colonnes avec sélection en cascade), utilisateurs & rôles (deux onglets : création/désactivation/réinitialisation de mot de passe ; permissions par rôle sous forme de pastilles cliquables), journal d'audit (liste filtrable par objet).
- **Écrans Lot 2** : liste élèves avec recherche (debounce 300ms sur `GET /students/search`), dossier élève (identité, responsables avec ajout/détachement, parcours annuel avec annulation d'inscription), et un **assistant d'inscription unique** (`/eleves/inscription`) couvrant à la fois la première inscription et la réinscription — reflète exactement la conception backend (`POST /enrollments` dérive `type` automatiquement) : onglet "Élève déjà connu" (recherche) vs "Nouvel élève" (formulaire avec responsable), puis sélection année/section/cycle/niveau/classe en cascade, puis confirmation. La détection de doublon (D33) y est gérée : un 409 avec `doublonPotentiel` affiche une alerte avec un bouton explicite « Ce n'est pas un doublon, créer quand même » (jamais un contournement silencieux).
- **Bug réel trouvé et corrigé en vérifiant dans le navigateur** : sur `/parametres/structure`, créer une classe renvoyait bien 201 côté serveur mais n'apparaissait pas dans la liste — `loadAll()` (rechargement après création) ne rafraîchissait jamais `classes`, qui dépend d'un `useEffect` séparé keyé sur `[selectedLevelId, selectedYearId]` (des valeurs inchangées par un simple ajout). Corrigé en extrayant `loadClasses()` et en l'appelant explicitement après chaque création, en plus de `loadAll()`.
- **Contrainte d'environnement de vérification (pas un bug du projet)** : dans cette session, le port 3000 par défaut du serveur de développement Next.js renvoyait une réponse HTML corrompue/mise en cache pour toutes les requêtes de chunks JS (`Uncaught SyntaxError: Unexpected token '<'`) au sein du navigateur intégré de l'outil — reproductible avec Turbopack et webpack, mais absent en `curl` direct depuis la machine, et absent après avoir changé le port du serveur web (3010). Cause isolée à l'infrastructure de la session d'outillage (probablement un cache de proxy propre à ce port dans cet environnement), pas au code : aucun changement de configuration n'a été fait dans le projet pour cette raison (le port 3000 reste la valeur par défaut normale de `next dev`).
- Voir `README.md` (racine) pour les instructions d'installation/lancement/tests.

## Lot 3 — tarifs, factures, remises, solvabilité (16 septembre 2026)

**Fait et vérifié** : schéma migré, 18 tests e2e (75 au total, tous verts), build/lint propres, vérification manuelle réelle dans le navigateur (données créées via l'API, financial-status/discount/approve testés en direct, données de test nettoyées après coup).

- **Modèles** (`apps/api/prisma/schema.prisma`) : `FeeType` (code, nom, `obligatoire`, `avecTranches`), `FeeSchedule` (par `academicYearId`+`levelId`+`feeTypeId`, unique) avec soit `montant` (frais à montant unique) soit des `InstallmentSchedule` enfants (frais en tranches) — **jamais les deux** : `FeeSchedulesService` refuse la création si `montant`/`installments` ne correspondent pas exactement à `FeeType.avecTranches`. `Invoice` (une par `Enrollment`, `@unique`, statut `EMISE`/`ANNULEE`) et `InvoiceLine` (RG03 : `montant`/`libelle` sont un **snapshot** figé à la génération, jamais recalculés si la grille tarifaire change ensuite). `Discount` (par `InvoiceLine`, type `MONTANT_FIXE`/`POURCENTAGE`, statut `EN_ATTENTE`/`APPROUVEE`/`REJETEE`, auteur + approbateur). `School.seuilImpayeCritiqueFcfa` (`Int?`, D40) : seuil configurable au-delà duquel `EN_RETARD` devient `IMPAYE_CRITIQUE` — `null` par défaut, jamais inventé.
- **Génération automatique de facture à l'inscription** (`EnrollmentsService.create()`/`cancel()`, `InvoicesService`) : la création d'une `Enrollment` et la génération de sa facture sont dans **la même transaction Prisma** (CA02 : jamais une inscription sans facture, même vide) — pour chaque `FeeSchedule` dont `FeeType.obligatoire = true` correspondant au (niveau, année) de la classe, une ligne par tranche (si `avecTranches`) ou une ligne unique (sinon). **Un frais facultatif (`obligatoire = false`) n'est jamais facturé automatiquement** — testé explicitement. Annuler une inscription annule sa facture dans la même transaction (`InvoicesService.cancelForEnrollment`).
- **D39 résolue (17 septembre 2026)** : `FeeType.appliesTo` (`TOUS`/`INSCRIPTION`/`REINSCRIPTION`, `TOUS` par défaut) — cas réel confirmé par l'école : "Frais d'inscription" (45 000 XAF) réservé aux nouveaux élèves, "Frais de réinscription" (60 000 XAF) réservé aux élèves déjà connus, jamais les deux à la fois. `InvoicesService.generateForEnrollment()` ne retient que les `FeeSchedule` dont le `FeeType` correspond à `TOUS` ou au type exact de l'inscription. Testé explicitement (`fees-and-invoices.e2e-spec.ts`), 76 tests e2e au total.
- **Bug réel trouvé et corrigé (17 septembre 2026)** : sur `/parametres/structure`, après la création d'une section/d'un cycle/d'un niveau, rien ne sélectionnait automatiquement l'élément créé — la colonne suivante restait bloquée sur "Sélectionnez une section/un cycle/un niveau" jusqu'à ce que l'utilisateur reclique dessus, sans aucun indice que c'était nécessaire (signalé par l'utilisateur : "on ne peut pas ajouter un cycle, un niveau et une classe" — confirmé en observant zéro cycle/niveau/classe créés en production malgré deux sections déjà présentes). Corrigé par une sélection automatique de l'élément fraîchement créé à chaque étape. Deuxième blocage au même endroit : le formulaire de création de classe disparaissait silencieusement sans aucune année scolaire créée, sans explication — remplacé par un message explicite avec lien direct vers "Années scolaires".
- **Remises** (`apps/api/src/discounts/`) : `POST /discounts` (permission `ENROLLMENT_MANAGE`, comme la création d'inscription) crée toujours une remise `EN_ATTENTE` — **aucun seuil d'auto-approbation** (D17, provisoire, seuil = 0). `POST /discounts/:id/approve|reject` exigent `DISCOUNT_APPROVE`, **attribuée à Direction uniquement** (D19 — Administrateur a `FEE_MANAGE` mais pas `DISCOUNT_APPROVE`, testé explicitement en 403). `computeApprovedDiscountAmount()` (`discounts/discount-amount.util.ts`) : seules les remises `APPROUVEE` réduisent le solde d'une ligne, toujours plafonné au montant de la ligne (jamais négatif), réutilisé à la fois par `DiscountsService` et `FinancialStatusService`.
- **Statut de solvabilité** (`apps/api/src/financial-status/`, `GET /students/:id/financial-status`, cahier §6) : calculé, **jamais saisi**. `montantPaye` (mis à jour au Lot 4, 17 septembre 2026) somme désormais les vrais `Payment` `VALIDE` de chaque ligne — un paiement `ANNULE` (RG09) n'est jamais compté. Statuts implémentés : `SOLVABLE` (rien facturé, ou tout couvert par une remise sans être `EXONERE`), `A_ECHOIR` (solde futur, aucune échéance passée), `EN_RETARD` (au moins une échéance passée non couverte), `IMPAYE_CRITIQUE` (`EN_RETARD` **et** montant exigible au-dessus de `School.seuilImpayeCritiqueFcfa`, D40), `EXONERE` (remises approuvées couvrant 100 % du montant facturé). Une facture `ANNULEE` n'entre jamais dans le calcul. `prochaineEcheance` et `lignesEnRetard` exposés pour l'explicabilité (jamais une simple couleur sans détail, cahier §6).
- **Permissions Lot 3** : `FEE_MANAGE` (Administrateur uniquement — configuration tarifaire) et `DISCOUNT_APPROVE` (Direction uniquement — D19). Lecture des types de frais/grilles tarifaires/factures ouverte à tout utilisateur authentifié (comme la structure académique), écriture derrière les permissions ci-dessus.
- **Frontend** : `/tarifs` (nav "Tarifs & facturation", `FEE_MANAGE` pour les formulaires de création — lecture seule sinon) — panneau "Types de frais" (création + liste avec badges Obligatoire/Tranches) et panneau "Grilles tarifaires" (filtrable par année/niveau, formulaire dynamique : champ montant unique ou lignes de tranches selon le type de frais choisi). `components/financial-status-card.tsx` (intégré au dossier élève, `/eleves/[id]`) : badge de statut coloré, quatre indicateurs (facturé/remises/payé/restant), prochaine échéance, lignes de la facture de l'inscription active avec bouton "Demander une remise" (`ENROLLMENT_MANAGE`) et boutons "Approuver"/"Rejeter" sur une remise `EN_ATTENTE` (`DISCOUNT_APPROVE`) — vérifié en direct dans le navigateur (demande créée, approuvée, montants recalculés immédiatement sans rechargement de page).
- **Tests** : `test/fees-and-invoices.e2e-spec.ts`, 18 cas — types de frais (création, doublon de code, 403 sans `FEE_MANAGE`), grilles tarifaires (montant unique, tranches, incohérence montant/tranches rejetée), génération de facture (montant unique, tranches, frais facultatif jamais facturé, annulation d'inscription annule la facture), remises (création/approbation/rejet, double approbation refusée en 409, 403 pour Administrateur), solvabilité (SOLVABLE sans facture, EN_RETARD sur frais immédiatement exigible, A_ECHOIR sur échéance future, EXONERE à 100% de remise, facture annulée exclue du calcul). `test/utils/clean-database.ts` étendu (discount/invoiceLine/invoice/installmentSchedule/feeSchedule/feeType, enfants avant parents).

## Lot 4 — paiements et reçus (17 septembre 2026)

**Fait et vérifié** : schéma migré, 12 nouveaux tests e2e (88 au total, tous verts), build/lint propres, vérifié manuellement de bout en bout dans le navigateur (paiement partiel, solde, reçu imprimé, réimpression, données de test nettoyées après coup). Demande explicite de l'utilisateur : "payer les frais d'inscription et les frais de scolarité, générer un reçu pour chaque opération, pouvoir réimprimer un reçu plus tard" — la caisse (ouverture/clôture de session, D20-D23) n'a **pas** été construite, hors du périmètre de cette demande.

- **D41 (nouvelle décision, simplification délibérée)** : un `Payment` cible **toujours une seule** `InvoiceLine`, jamais réparti sur plusieurs — le montant est systématiquement plafonné au solde restant de cette ligne (`montant - remises approuvées - paiements VALIDE déjà encaissés`). Ce choix rend un trop-perçu (RG08) structurellement impossible sans compte d'avance (D11, toujours non activé) et évite tout le débat D12/D13 sur la ventilation multi-lignes — un parent qui paie l'inscription ET une tranche de pension fait simplement deux paiements distincts, chacun avec son propre reçu (exactement ce que l'utilisateur a demandé : "pour chaque opération, générer un reçu").
- **Modèle** (`apps/api/prisma/schema.prisma`) : `Payment` (`invoiceLineId`, `montant`, `modePaiement` `ESPECES`/`MOBILE_MONEY`, `referenceExterne` optionnelle — requise si `MOBILE_MONEY`, cadrage §11 déjà tranché : jamais une intégration API automatisée, un simple enregistrement manuel avec référence), `numeroRecu` (unique par école), `statut` `VALIDE`/`ANNULE`, `motifAnnulation`/`annuleParUserId` — **RG09/RG18 : un paiement `VALIDE` n'est jamais modifié ni supprimé**, une correction passe par `ANNULE` (motif obligatoire), le solde de la ligne se recalcule alors automatiquement en excluant ce paiement (aucune écriture inverse séparée à créer, le recalcul suffit).
- **`PaymentsService`** (`apps/api/src/payments/`) : `POST /payments` (`PAYMENT_CREATE`) refuse tout montant `<= 0`, tout montant dépassant le solde restant (400), et tout paiement sur une ligne déjà soldée (409) ou une facture `ANNULEE` (409). Numéro de reçu généré via `NumberSequenceService` (`type: 'RECEIPT'`, série continue par école, format `REC-000001` — D28). `POST /payments/:id/cancel` exige `PAYMENT_CANCEL_APPROVE`, **Direction uniquement** (même principe que `DISCOUNT_APPROVE`/D19 — Administrateur peut encaisser mais pas annuler, testé explicitement en 403). `POST /payments/:id/reprint` journalise `PAYMENT_REPRINT` (D30, RG15) et renvoie les mêmes données de reçu — l'émission initiale (`PAYMENT_CREATE`) n'est jamais comptée comme une réimpression.
- **Permissions Lot 4** : `PAYMENT_CREATE` (Administrateur + Secrétaire-caissier — encaissement ET réimpression, même geste opérationnel cahier §3) et `PAYMENT_CANCEL_APPROVE` (Direction uniquement). Codes repris tels quels du cahier §3 (déjà cités en exemple dans ce fichier avant même que le module existe).
- **`FinancialStatusService` mis à jour** : `montantPaye` n'est plus figé à 0 (voir plus haut) — chaque ligne inclut désormais ses paiements `VALIDE`, le solde par ligne (et donc `montantRestant`/le statut global) reflète les vrais encaissements.
- **Frontend** : `components/financial-status-card.tsx` étendu — chaque ligne de facture affiche son solde restant réel et un bouton "Payer" (`PAYMENT_CREATE`) ouvrant un formulaire inline (montant pré-rempli au solde restant, mode de paiement, référence externe si Mobile Money) ; section "Historique des paiements" listant chaque reçu avec boutons "Réimprimer" (`PAYMENT_CREATE`) et "Annuler" (`PAYMENT_CANCEL_APPROVE`, si `VALIDE`). Nouvelle page `/recus/[id]` (hors du groupe `(dashboard)`, sans le chrome `AppShell`) : reçu imprimable (école, numéro, élève, motif, montant, mode, caissier ; bandeau "REÇU ANNULÉ" si le paiement l'est), bouton "Imprimer" (`window.print()`, classes `print:` Tailwind pour masquer les boutons à l'impression) et lien "Retour au dossier".
  **Bug réel trouvé et corrigé en vérifiant dans le navigateur** : le premier jet ouvrait le reçu via `window.open(url, "_blank")` — un nouvel onglet n'a **jamais** le jeton d'accès (qui ne vit qu'en mémoire JS de l'onglet d'origine, choix de sécurité délibéré, voir "Auth frontend" plus haut), donc le nouvel onglet affichait systématiquement l'écran de connexion, et se retrouvait sur le tableau de bord après connexion — jamais sur le reçu attendu. Corrigé en remplaçant par une navigation client `router.push()` dans le **même onglet** (le jeton reste disponible), avec un lien "Retour au dossier" pour revenir ensuite — testé de bout en bout (paiement → reçu affiché → retour → second paiement soldant la ligne → réimpression du premier reçu, tout fonctionnel sans perte de session).
- **Tests** : `test/payments.e2e-spec.ts`, 12 cas — encaissement complet avec numéro de reçu séquentiel, paiement partiel (D10) puis complément soldant la ligne, refus au-delà du solde (400), Mobile Money sans référence refusé (400), 403 sans `PAYMENT_CREATE`, annulation par Direction avec libération du solde, 403 pour Administrateur sur l'annulation, refus de double annulation (409), réimpression journalisée (D30), refus d'encaisser sur une facture annulée (409), `financial-status` reflétant un vrai paiement (passage à `SOLVABLE`), listes par élève/par ligne.
- **Reçu enrichi (17 septembre 2026, demande explicite)** : logo de l'établissement (`School.logoUrl`, upload `POST /school/logo` — `multer`/`diskStorage`, JPEG/PNG/WebP 5 Mo max, servi statiquement via `app.useStaticAssets` dans `main.ts`, `NestExpressApplication` — fichiers dans `apps/api/uploads/`, volume Docker nommé `academy_uploads` pour survivre à une recréation de conteneur, jamais dans l'image), montant en toutes lettres (`apps/web/lib/number-to-words-fr.ts`, règles de pluriel françaises correctes sur "cent"/"vingt" — ne prennent un "s" que si rien de plus petit ne suit dans le nombre entier, ex. "trois cent mille" jamais "trois cents mille"), emplacements "Signature du caissier"/"Cachet de la Direction" (zones vides, aucune signature électronique). `process.cwd()` utilisé pour les chemins d'upload plutôt que `__dirname` — ce dernier diffère entre dev (`ts-node` depuis `src/`) et production (`dist/src/`), piège identifié avant de casser quoi que ce soit.
- **Correction de saisie sur une inscription (17 septembre 2026, demande explicite : "si on a fait une erreur sur le nom de l'élève ou la classe, on doit pouvoir modifier")** : `PATCH /students/:id` existait déjà côté API (Lot 2) mais n'avait pas de formulaire — ajouté dans `/eleves/[id]` (bouton "Modifier" sur la carte Identité). Nouveau `PATCH /enrollments/:id/change-class` (D43) : corrige la classe d'une inscription `ACTIVE`, refuse explicitement un changement vers un **niveau différent** (400 — les `InvoiceLine` déjà émises sont un snapshot lié au niveau, RG03) ; un vrai changement de niveau reste annulation + nouvelle inscription. `Enrollment.class` expose désormais `levelId` (`ENROLLMENT_INCLUDE`) pour que le frontend sache proposer les classes-sœurs du même niveau.
- **"Autres frais" — module de facturation ponctuelle (17 septembre 2026, demande explicite : tenues, livres, cantine, autres)** : `POST /invoices/:id/lines` (`ENROLLMENT_MANAGE`) ajoute une `InvoiceLine` manuelle à une facture déjà émise, pour un `FeeType` sans tranches (`avecTranches=true` refusé en 400 — les tranches passent toujours par `/fee-schedules`). Aucun nouveau modèle : les types de frais facultatifs (tenue, cantine…) se créent normalement via `/tarifs` (`FEE_MANAGE`, `obligatoire=false`), cette route ne fait qu'y rattacher un montant ponctuel à UN élève précis. Une fois la ligne créée, le paiement/reçu réutilise exactement le flux Lot 4 existant. Bouton "Autre frais" dans `financial-status-card.tsx`.

## Lot 5 — sorties financières, clôture de journée, élèves insolvables (17 septembre 2026)

**Fait et vérifié** : schéma migré, 11 nouveaux tests e2e (106 au total, tous verts), build/lint propres, vérifié manuellement de bout en bout dans le navigateur (sortie enregistrée → approuvée par Direction → apparaît dans la clôture du jour ; élève avec échéance non couverte listé comme insolvable). **La caisse au sens strict (session d'ouverture/clôture avec fonds initial, D20-D23) n'a pas été construite** — voir D44 : la "clôture de journée" est un rapport recalculé à la demande, pas une session figée.

- **Sorties financières** (`apps/api/src/expenses/`) : `Expense` (catégorie, montant, description, `dateDepense`, statut `EN_ATTENTE`/`APPROUVEE`/`REJETEE`). Catégories confirmées par l'école (D24) : Versement banque, Paiement salaire, Paiement facture, Achat matériel, Autre — `EXPENSE_CATEGORIES` (`expense-categories.ts`), une simple liste validée côté DTO plutôt qu'un enum Postgres, pour rester extensible sans migration. `POST /expenses` (`EXPENSE_CREATE` — Administrateur + Comptable, cadrage §3 : "Comptable : sorties, rapports...", jusqu'ici sans aucune permission réelle) crée toujours `EN_ATTENTE` ; `POST /expenses/:id/approve|reject` exigent `EXPENSE_APPROVE` (Direction uniquement, D25 — même principe que `DISCOUNT_APPROVE`/`PAYMENT_CANCEL_APPROVE`, seuil de validation = 0, provisoire). Écran `/depenses`.
- **Clôture de journée** (`apps/api/src/reports/`, `GET /reports/cash-closing?date=`, `CASH_CLOSE`) : recalculée à chaque appel (jamais une valeur stockée) — entrées = `Payment` `VALIDE` du jour, sorties = `Expense` `APPROUVEE` du jour (une dépense encore `EN_ATTENTE` n'est jamais comptée), solde du jour, et solde cumulé (toutes entrées - toutes sorties depuis le début, jusqu'à la fin du jour sélectionné). Écran `/cloture` avec sélecteur de date (par défaut aujourd'hui), quatre indicateurs + détail des mouvements du jour.
- **Élèves insolvables** (`GET /reports/insolvent-students`, `STUDENT_READ`) : parcourt tous les élèves `ACTIF`, réutilise `FinancialStatusService.getForStudent()` (même logique que le dossier individuel, jamais un statut recalculé différemment), filtre `EN_RETARD`/`IMPAYE_CRITIQUE`, trié par montant exigible décroissant. Complexité N+1 assumée (effectif d'une école pilote). Écran `/insolvables` avec contact du responsable prioritaire (lien `tel:`).
- **Permissions Lot 5** : `EXPENSE_CREATE` (Administrateur, Comptable), `EXPENSE_APPROVE` (Direction), `CASH_CLOSE` (Administrateur, Direction, Secrétaire-caissier, Comptable — accès en lecture aux rapports financiers). Comptable a enfin de vraies permissions (`STUDENT_READ`, `EXPENSE_CREATE`, `CASH_CLOSE`) après être resté vide depuis le Lot 1.
- **Tests** : `test/expenses.e2e-spec.ts` (6 cas) et `test/reports.e2e-spec.ts` (5 cas) — création/approbation/rejet d'une sortie, catégorie hors liste refusée (400), 403 pour Administrateur sur l'approbation, refus de double approbation (409), filtre par statut ; clôture additionnant entrées/sorties/solde correctement, dépense `EN_ATTENTE` exclue, 403 sans `CASH_CLOSE` ; élève avec frais exigible non couvert listé comme insolvable, élève entièrement payé absent de la liste.

## Déploiement production — académy.lobima.online (12 septembre 2026)

> **Domaine changé le 24 septembre 2026** : l'application est désormais servie sur `https://ecole-shakespeare.com` (web) et `https://api.ecole-shakespeare.com` (API) ; `academy.lobima.online` et `api-academy.lobima.online` ne sont plus que des redirections. Les entrées ci-dessous, datées, gardent les anciens noms telles qu'elles ont été écrites ; voir la dernière entrée de ce fichier pour le détail du changement.

**Déployé et vérifié en conditions réelles** : `https://academy.lobima.online` (web) et `https://api-academy.lobima.online` (API), sur le **même VPS qu'Elyon** (autre projet du même opérateur, `elyon-restaurant-mvp`), à la demande explicite de l'utilisateur — pas un choix d'architecture pris seul.

- **Cohabitation avec Elyon sur un VPS partagé** : Shakespeare Academy vit dans son propre répertoire (`/docker/shakespeare-academy` sur le VPS) avec son propre `docker-compose.yml` (nom de projet `shakespeare-academy`, réseau `shakespeare-academy_default`, base Postgres dédiée `academy-db`) — **jamais géré par le `docker-compose.yml` d'Elyon**. Seuls `academy-api`/`academy-web` rejoignent *en plus* le réseau Docker d'Elyon (`elyon-restaurant-mvp_default`, déclaré `external: true`) pour être joignables par son Caddy déjà en place sur les ports 80/443 — aucun deuxième Caddy possible sur les mêmes ports.
- **Piège réel rencontré en déployant, pas anticipé** : `academy-db` s'appelait initialement `db` — mais Elyon a **aussi** un service nommé `db`. Un conteneur connecté à deux réseaux Docker où le même alias existe sur chacun résout ce nom de façon ambiguë : `academy-api` s'est retrouvé à résoudre "db" vers la base **d'Elyon** (échec d'authentification, mauvais réseau) plutôt que la sienne. Corrigé en renommant le service `db` → `academy-db` (jamais un nom générique pour un service qui vit sur un réseau partagé avec un autre projet).
- **Deux autres bugs réels trouvés et corrigés pendant ce déploiement** :
  1. Le point d'entrée du Dockerfile (`node dist/main.js`, copié du patron Elyon) était faux pour ce projet : `tsconfig.json` ne fixe pas `rootDir`, et la compilation couvre à la fois `src/` et `prisma/` (à cause de `seed.ts`) — tsc calcule alors une racine commune et préserve `src/` sous `dist/`. Le vrai point d'entrée est `dist/src/main.js`. Puisque `dist/prisma/seed.js` est lui aussi déjà compilé (mêmes dépendances `@prisma/client`/`argon2`, déjà en `dependencies` de production), la production peut lancer le seed via `node dist/prisma/seed.js` sans jamais avoir besoin de `ts-node` (resté en devDependency, absent de l'image de production).
  2. `docker-compose.yml` positionne les variables optionnelles à une chaîne vide (`${ADMIN_SEED_EMAIL:-}`) quand elles sont absentes de `.env.production` — jamais réellement *absentes* du `process.env` du conteneur. `seed-data.ts` utilisait `options.adminEmail ?? 'valeur par défaut'` : `??` ne se déclenche que sur `null`/`undefined`, jamais sur `''`. Un premier compte admin a donc été créé avec un email **et un mot de passe vides**, invisible tant que personne ne testait un vrai login. Corrigé dans `prisma/seed.ts` (`process.env.ADMIN_SEED_EMAIL || undefined`, jamais la valeur brute) — potentiellement le même piège pour toute variable optionnelle future lue via `??` plutôt que `||`.
- **Structure des fichiers Compose** : `docker-compose.yml` est sûr par défaut (aucun port publié sur `academy-db`, pas de montage lié aux tests e2e) — utilisable tel quel aussi bien en dev qu'en production. `docker-compose.override.yml.example` (copié en `docker-compose.override.yml`, gitignored) republie le port Postgres (`5544`) et monte `init-test-db.sql` **uniquement** pour le développement local — jamais présent sur le VPS. Même patron que `docker-compose.override.yml.example` côté Elyon.
- **Secrets** : `.env.production` (jamais commité, sur le VPS uniquement) contient `POSTGRES_PASSWORD`/`JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` générés aléatoirement (`openssl rand -hex 32`), jamais des valeurs par défaut. Voir `.env.production.example` à la racine pour la liste complète.
- **Caddy** : deux blocs ajoutés à la suite du `Caddyfile` **d'Elyon** (`/docker/elyon-restaurant-mvp/Caddyfile` sur le VPS — fichier appartenant à l'autre projet, sauvegardé avant modification) pour `academy.lobima.online`/`api-academy.lobima.online`, appliqués par un **rechargement gracieux** (`caddy reload`, jamais un restart du conteneur) pour ne jamais interrompre le trafic Elyon en cours. Certificats Let's Encrypt de production obtenus et vérifiés (émetteur réel, pas auto-signé) dès que le DNS (deux enregistrements A vers `77.37.87.119`, ajoutés par l'utilisateur) a propagé.
- **Compte administrateur de production** : créé via `docker exec ... node dist/prisma/seed.js` puis son mot de passe changé immédiatement après la première connexion réelle (le mot de passe par défaut `ChangeMe123!` documenté dans ce fichier ne doit **jamais** rester actif sur un déploiement public) — nouveau mot de passe connu seulement de l'utilisateur.
- **Vérifié de bout en bout en conditions réelles** : DNS, HTTPS (certificat réel), CORS (`WEB_ORIGIN`), login réel, changement de mot de passe forcé, tableau de bord chargé — sans jamais interrompre Elyon (vérifié après coup : `app.lobima.online`/`api.lobima.online` toujours 200, tous les conteneurs Elyon toujours `healthy`/`Up`, aucun redémarrage de leurs conteneurs).
- **Structure académique et tarifs réels configurés en production (17 septembre 2026)** : à la demande de l'utilisateur, structure complète créée pour l'année active 2026/2027 — section Francophone (Maternelle : TPS/PS/MS/GS, Primaire : CP/CE1/CE2/CM1/CM2, Collège : 6e/5e/4e/3e, Lycée : 2nde C) et section Anglophone (Maternelle : Pre-nursery/Nursery 1/Nursery 2, Primaire : Class 1-6) — 23 niveaux, une classe par niveau (même nom, à scinder en plusieurs classes par niveau plus tard si l'effectif le justifie). Tarifs réels : "Frais d'inscription" (45 000 XAF, `appliesTo: INSCRIPTION`), "Frais de réinscription" (60 000 XAF, `appliesTo: REINSCRIPTION`), "Pension" en 3 tranches égales (`appliesTo: TOUS`) — 110 000 XAF/tranche en Maternelle, 100 000 en Primaire/Collège, 120 000 en Lycée (330 000/300 000/300 000/360 000 XAF par an selon le cycle). 69 grilles tarifaires créées (23 niveaux × 3 types de frais) via un script ponctuel (non conservé dans le dépôt, propre à cette seule école) appelant directement l'API de production. **Dates limites des 3 tranches de pension fixées par défaut à titre provisoire** (1er octobre / 15 janvier / 1er avril, 5 jours de grâce chacune) faute d'un calendrier de paiement exact communiqué par l'école — modifiables à tout moment via `PATCH /fee-schedules/:id` ou l'écran `/tarifs`, à ajuster dès que l'école confirme ses vraies échéances. Vérifié avec un vrai élève de test (inscription en TPS → facture exacte 45 000 + 3×110 000 = 375 000 XAF, aucune ligne de réinscription — confirme le bon fonctionnement de D39 sur données réelles) puis nettoyé.
- **Vérification publique des reçus par QR code (D29, 17 septembre 2026, demande explicite : "le reçu doit être difficilement falsifiable")** : `Payment.verificationToken` (jeton opaque distinct du numéro de reçu séquentiel — jamais ce dernier, devinable par simple incrémentation) — `GET /payments/verify/:token` public (`@Public()`), renvoie uniquement numéro/montant/statut/date/motif/nom de l'élève/établissement, jamais de téléphone ni autre donnée sensible. `PermissionsGuard` a dû être corrigé pour respecter `@Public()` (jusqu'ici seul `JwtAuthGuard` en tenait compte — une route publique au sein d'un contrôleur par ailleurs protégé par `@RequirePermission` restait bloquée en 403, `request.user` n'étant jamais peuplé). QR code (`qrcode`, généré côté client) affiché sur `/recus/[id]`, pointant vers une nouvelle page publique `/verifier-recu/[token]` (aucun compte requis, consultable en scannant depuis un téléphone). Vérifié en production avec un vrai reçu (`REC-000002`) : `GET /payments/verify/:token` répond sans en-tête d'authentification, page de vérification affichant "Reçu authentique" avec les bonnes informations, reçu imprimable affichant le QR code correctement positionné.
- **Import initial de 25 élèves déjà inscrits, depuis le fichier Excel de suivi de l'école (17 septembre 2026)** — voir `DECISIONS_PENDING.md` §15 (D45-D48) pour le détail des décisions prises avec l'utilisateur avant d'importer (traitement des tarifs de fratrie via remises, absence de compte Direction pour les approuver, absence de contacts responsables, mode de paiement par défaut). Un changement de code réel en a découlé : **`CreateStudentDto.responsable` devient optionnel** (D31 révisé) — le schéma (`StudentGuardian`, table de jonction) l'a toujours permis, seul le DTO l'imposait ; un responsable reste ajoutable à tout moment via le bouton "Ajouter" déjà existant sur la fiche élève. Import réalisé via un script ponctuel (non conservé dans le dépôt) appelant la vraie API de production (jamais d'écriture SQL directe pour élève/facture/paiement) — seule l'approbation des 51 remises l'a été, en base, à la demande explicite de l'utilisateur et strictement scopée à cet import (le contrôle applicatif "Direction approuve une remise" reste intact pour l'avenir). Vérifié manuellement dans le navigateur sur un cas avec remise et un cas sans — montants exacts des deux côtés (voir DECISIONS_PENDING.md pour le détail).
- **Tableau de bord en couleurs, liste par classe, exports PDF et Excel de toutes les listes (21 septembre 2026, demande explicite)** : `StatCard` (`components/ui.tsx`) devient une carte teintée avec liseré gauche, icône pleine et valeur colorées selon le sens (`success` vert = positif comme encaissé ou solde, `danger` rouge = négatif comme restant dû, impayé ou sorties, `warning` orange = à surveiller comme remises ou éléments en attente, `info` bleu = neutre, `primary`/`accent` = identité), avec une jauge optionnelle (`progress`) ; le taux de recouvrement change de couleur par seuil (75 et plus vert, 40 à 74 orange, en dessous rouge), le solde de caisse passe au rouge s'il est négatif. Le tableau de bord ajoute une barre segmentée de la facturation (encaissé vert, remises orange, restant dû rouge), des barres par section et par classe (bleu Anglophone, violet Francophone), les pourcentages garçons/filles. Les listes en cartes reçoivent la même logique (sorties : orange en attente, vert approuvée, rouge rejetée ; clôture : entrées vertes avec +, sorties rouges avec - ; insolvables : impayé critique en rouge). **Nouvelle page `/eleves-par-classe`** (`GET /reports/students-by-class`, JSON, mêmes lignes que l'export CSV existant qui en est désormais dérivé) : un bloc par classe (section, cycle, effectif, garçons/filles), filtre par classe et recherche, élèves cliquables. **Exports PDF et Excel (.xlsx) sur toutes les listes** : `lib/export.ts` (100 % navigateur, `jspdf` + `jspdf-autotable` + `write-excel-file`, chargés à la demande) et `components/export-buttons.tsx` (`ExportButtons`, nom de l'établissement lu une fois via `GET /school`) exportent exactement les données affichées (recherche et filtres compris) ; pages couvertes : tableau de bord (indicateurs + effectif par classe), élèves, élèves par classe (un onglet Excel et une section PDF par classe, plus l'export de chaque classe seule), insolvables, sorties financières, clôture de journée (récapitulatif, entrées, sorties), tarifs, utilisateurs, journal d'audit. Les montants restent numériques dans Excel (format `#,##0`, sommables) avec une ligne de total quand elle a un sens ; le PDF remplace l'espace fine insécable de `toLocaleString("fr-FR")`, absent des polices standard de jsPDF. Vérifié dans le navigateur : fichiers réellement produits (xlsx = archive valide avec un onglet par classe, PDF relu visuellement, accents corrects) ; **le lien du menu "Élèves" restait actif sur "Élèves par classe"** (`startsWith("/eleves")`), corrigé en comparant avec `href + "/"`. Les anciens boutons CSV des pages Élèves et Insolvables sont remplacés (les routes `/reports/export/*` restent disponibles).
- **Correctif suite au redéploiement (17 septembre 2026, pas une régression du code)** : redéployer ce lot a révélé que les commandes `docker compose build`/`up` avaient été lancées sans `--env-file .env.production` — sur ce projet (contrairement à un fichier `.env` par défaut que Docker Compose charge automatiquement), les vraies valeurs de production ne sont jamais reprises sans ce flag explicite, `academy-api` retombant silencieusement sur les valeurs par défaut du `docker-compose.yml` (`POSTGRES_PASSWORD: shakespeare`) au lieu du vrai mot de passe généré aléatoirement — le site est resté indisponible (401/500 en cascade) jusqu'à ce que le mot de passe réel de la base soit resynchronisé et que les conteneurs soient relancés avec `--env-file .env.production`. Toute commande `docker compose` future sur ce déploiement doit inclure ce flag.

- **Application installable (PWA) et mode hors ligne avec synchronisation (21 septembre 2026, demande explicite : "Internet étant un souci en Afrique", périmètre choisi : tout, paiements compris)** — voir `DECISIONS_PENDING.md` §16 (D49) pour la décision et ses limites. **Installation** : `app/manifest.ts`, icônes générées (`public/icons/`, « S » doré sur fond indigo, **générique** : le vrai logo n'existe pas en fichier), bouton « Installer l'application » (`components/install-app-button.tsx`, `lib/pwa-install.ts` : vraie invite sur Chrome/Edge/Android, explication « Partager → Sur l'écran d'accueil » sur iOS) dans le menu latéral et la page de connexion. **Service worker** `public/sw.js` (enregistré uniquement en production, `components/pwa-register.tsx`) : fichiers statiques d'abord depuis le cache, pages et données de navigation réseau d'abord avec repli sur la dernière copie (réseau lent = 3,5 s), `public/offline.html` en dernier recours ; jamais les appels à l'API (autre adresse). **Lectures hors ligne** : `lib/api.ts` copie chaque GET réussi dans IndexedDB (`lib/offline-cache.ts`, propre à un utilisateur, effacée à la déconnexion) et la relit si le réseau échoue (délais 15 s lecture / 30 s écriture) ; la recherche d'élèves retombe sur la liste copiée ; « Préparer le mode hors ligne » (`lib/offline-warmup.ts`, auto toutes les 6 h) copie élèves, factures, paiements et demande au service worker de précharger les pages. **Connectivité** (`lib/connectivity.ts`) : `navigator.onLine` n'est pas fiable, l'état bascule hors ligne à la première requête en échec et revient en ligne sur une vraie réponse ou un `/health` toutes les 15 s. **Session** : au démarrage sans réseau, `/auth/me` copié restaure l'utilisateur (aucun mot de passe redemandé). **Saisies hors ligne** (`lib/outbox.ts`, `lib/offline-actions.ts`) : file d'attente rejouée dans l'ordre, chaînage `$ref:<id>` (une inscription attend la création de l'élève), envoi par le seul compte auteur, survit à la déconnexion (confirmation demandée) ; encaissement → **reçu provisoire** `PROV-{appareil}-{jjmmaa}-{n}` (`app/recus/provisoire/[id]`), le reçu officiel REC-xxxxxx n'existe qu'après l'envoi (le reçu officiel affiche « Remplace le reçu provisoire … ») ; solde de l'élève recalculé localement avec les encaissements en attente. Page **« Synchronisation »** (`/hors-ligne`) : état, Réessayer, « Créer malgré le doublon », Abandonner. **Serveur** : `IdempotencyInterceptor` global (table `idempotency_records`, clé `Idempotency-Key` par utilisateur ; 409 si la clé sert sur une autre route, 400 si mal formée), `Payment.numeroProvisoire` (unique par école) et `saisieHorsLigneAt`, `dateSaisie` bornée à 45 jours ; migration `20260921120000_offline_sync`. Exclusivement en ligne : approbations, annulations, réimpressions auditées, autre frais, changement de classe, clôture. 10 tests e2e ajoutés (`offline-sync.e2e-spec.ts`), 125 au total. **Vérifié dans le navigateur (build de production, réseau coupé par surcharge de `fetch` puis arrêt réel de l'API)** : encaissement hors ligne → reçu provisoire, solde recalculé, synchronisation automatique → REC-000007 ; nouvel élève + inscription hors ligne → envoyés dans l'ordre ; rechargement complet API arrêtée → session, dossier et solde relus depuis la copie. **Non vérifié ici** : l'enregistrement du service worker n'a pas pu être testé (le navigateur intégré de cette session refuse de l'enregistrer ; Chrome n'était pas connecté) — à contrôler sur un vrai téléphone après déploiement (installation, ouverture sans Internet).

- **Lot 7 : espace de saisie « Vie scolaire » (référentiel pédagogique et personnel) (21 septembre 2026)** : la Direction a validé D50 à D78 sans modification (`DECISIONS_PENDING.md` §17 passée en TRANCHÉ) et a demandé de préparer l'espace où elle saisira elle-même ce qu'elle devait fournir. **Rien n'est prérempli** (aucun créneau, matière, enseignant ni date). **Backend** `apps/api/src/pedagogy/` : jours de classe (`School.joursClasse Int[]`, lundi à vendredi par défaut, D52), créneaux horaires `TimeSlot` (grille commune ou propre à une section, D53 ; format `HH:mm`, chevauchement refusé dans une même grille, jamais entre grille commune et grille de section), salles `Room`, matières `Subject` + `SubjectLevel` (niveaux auxquels une matière est enseignée, volume hebdomadaire facultatif ; retirer un niveau déjà affecté est refusé), enseignants `Teacher` + `TeacherSubject` (fiche sans compte, sans paie ni contrat, D56 ; homonyme exact refusé), affectations `TeachingAssignment` (un seul enseignant par classe et matière, RV01 ; la matière doit être enseignée au niveau de la classe, enseignant actif obligatoire), trimestres `Term` et jours sans classe `CalendarEvent` (D55 ; dates dans l'année scolaire, pas de chevauchement de trimestres, année clôturée non modifiable), classe pilote `Class.pilote` et enseignant volontaire `Teacher.volontairePilote` (un seul de chaque, exclusivité garantie par le service), récapitulatif `GET /pedagogy/summary` (ce qui est saisi et ce qui manque, dont `affectations.requises`/`manquantes`). Nouvelle permission **`PEDAGOGY_MANAGE`** (Administrateur et Direction) : toute écriture et toute lecture des enseignants (données personnelles) l'exigent ; la lecture des créneaux, salles, matières, trimestres et calendrier est ouverte à tout utilisateur connecté. Chaque action est journalisée (RG15). Migration `20260921130000_lot7_referentiel_pedagogique` (nommée après `offline_sync` : `prisma migrate dev` avait proposé un horodatage antérieur, renommé pour garder l'ordre d'application). `test/utils/clean-database.ts` mis à jour (enfants avant parents). **Frontend** : page `/vie-scolaire` (lien de menu réservé à `PEDAGOGY_MANAGE`), sept onglets (`components/vie-scolaire/`) : suivi de la saisie (huit rubriques avec statut et raccourcis, choix de la classe pilote et de l'enseignant volontaire, rappel des deux documents à remettre hors application), horaires, matières, enseignants, affectations (grille par classe), calendrier, salles ; boutons +/− partout. Saisie en ligne uniquement (une erreur hors ligne est affichée clairement). **Ne fait pas encore** : l'emploi du temps lui-même (séances, conflits) est le Lot 8 ; un créneau ou une salle supprimé aujourd'hui pourra devenir non supprimable quand des séances les référenceront (à traiter au Lot 8 : désactiver plutôt que supprimer). 35 tests e2e ajoutés (`pedagogy.e2e-spec.ts`), 160 au total ; vérifié dans le navigateur (saisie de créneaux avec refus de chevauchement, matière avec niveau, enseignant, salle, affectation, trimestre, jour férié, suivi passant de 0/8 à 5/8, aucun débordement à 390 px sur les sept onglets) ; données de test supprimées de la base locale après vérification.
- **Lot 8 : emploi du temps (21 septembre 2026)** : séances hebdomadaires versionnées, conflits refusés, changements ponctuels. **Backend** `apps/api/src/timetable/` : `Timetable` (une version par année : `BROUILLON` modifiable, `PUBLIE` immuable avec date d'effet, `ARCHIVE` quand une version plus récente la remplace, RV03), `TimetableEntry` (classe, matière, enseignant, salle, jour, créneau ; les heures sont copiées du créneau à la saisie), `TimetableException` (annulation, remplacement d'enseignant, changement de salle à une date précise : n'écrit jamais dans la version, l'historique publié reste intact). **Saisie** : l'enseignant se déduit de l'affectation de la classe (jamais saisi à la main) ; le créneau doit être un cours (pas une pause) de la grille de la section de la classe ; le jour doit être un jour de classe ; salle active et enseignant actif obligatoires. **RV02** : classe, enseignant et salle ne peuvent pas être à deux endroits au même moment (deux séances qui s'enchaînent sont permises) ; le refus nomme la séance en cause ; la publication revérifie tout (paire par paire, affectations changées, enseignants ou salles devenus inactifs) et est bloquée avec la liste des problèmes. Un seul brouillon par année ; une nouvelle version repart par défaut d'une copie de la version en vigueur (`vide: true` pour repartir de zéro) ; date d'effet dans l'année et strictement postérieure à celle de la version en vigueur ; année clôturée non modifiable. `POST /timetables/:id/resync` remet à jour les enseignants d'un brouillon après un changement d'affectation (un conflit créé est signalé, rien n'est écrasé). **Semaine réelle** : `GET /timetable/day|week` applique à la version en vigueur ce jour-là les changements ponctuels et les jours sans classe (calendrier, jours hors `joursClasse`), filtrables par classe, enseignant ou salle ; un changement ponctuel exige une version publiée en vigueur à cette date, un jour de classe, un remplaçant ou une salle libres à ce moment (une séance annulée libère ses ressources), un motif, et un seul changement par séance et par date. **Permissions** : nouvelle **`TIMETABLE_READ`** (Administrateur, Direction, Secrétaire-caissier, Auditeur) pour la consultation ; toute écriture (brouillon, publication, changements ponctuels) reste sous `PEDAGOGY_MANAGE`, et les brouillons sont invisibles sans elle (404). Les changements ponctuels sont sous `PEDAGOGY_MANAGE` en attendant le rôle Surveillant des Lots 9 et 10. **Protections du Lot 7** : un créneau utilisé ne change plus d'heures ni de type et ne se supprime pas, une salle utilisée (séance ou changement ponctuel) ne se supprime pas (la désactiver), une affectation qui sert dans un emploi du temps ne se supprime pas. Audit RG15 sur chaque action. Migration `20260921140000_lot8_emploi_du_temps` (nommée après `offline_sync`, même raison qu'au Lot 7). **Frontend** : page `/emploi-du-temps` (lien de menu sous `TIMETABLE_READ`, `components/emploi-du-temps/`) : onglet Versions (sélecteur de version, grille jours × créneaux par classe, enseignant ou salle, ajout par + / changement de salle / retrait en brouillon, boutons Vérifier, Mettre à jour les enseignants, Publier avec date d'effet, Supprimer le brouillon) et onglet Semaine réelle (navigation par semaine, changement ponctuel par séance, Rétablir) ; export PDF et Excel de ce qui est affiché. La vue par enseignant tire ses noms des séances (la liste des enseignants reste réservée à `PEDAGOGY_MANAGE`). **Ne fait pas encore** : impression mise en page d'une grille (l'export est une liste triée jour et heure), notification aux parents d'un changement (Lot 12), rôle Surveillant (Lots 9 et 10). 21 tests e2e ajoutés (`timetable.e2e-spec.ts`), 181 au total ; vérifié dans le navigateur (ajout de séances, refus d'un conflit d'enseignant avec le message nommant la séance, publication, annulation ponctuelle en semaine réelle, export PDF produit, vue par enseignant, aucun débordement à 390 px après correction de la barre de navigation de semaine) ; données de test supprimées de la base locale. **Déployé en production le 21 septembre 2026** : migration appliquée au démarrage de l'API (tables d'emploi du temps vides), `TIMETABLE_READ` accordée aux quatre rôles par un script ponctuel (sans réinitialiser les autres permissions), routes vérifiées (401 sans jeton), conteneurs Elyon non touchés.
- **Lot 9 : assiduité des élèves (21 septembre 2026)** : appel séance par séance, absences et retards, justificatifs, corrections encadrées, hors ligne. **Backend** `apps/api/src/attendance/` : `AttendanceCall` (un appel par séance de l'emploi du temps et par date, `@@unique([entryId, date])`, avec instantané de la classe, de la matière, de l'enseignant qui tenait réellement la séance, remplaçant compris, et des heures), `AttendanceRecord` (un enregistrement par élève inscrit, unique par appel et élève), `AttendanceCorrection` (historique jamais modifié : ancien et nouveau statut, motif, auteur), `AbsenceJustification` (un par enregistrement), `AbsenceReason` (motifs saisis par la Direction, aucun prérempli). **Règles** : par défaut tout le monde est présent, on ne liste que les exceptions (D60) ; `POST /attendance/calls` remplace l'état entier de l'appel, donc renvoyer la même saisie (synchronisation, réponse perdue, deux appareils) ne crée jamais de doublon ; le **statut est calculé par le serveur** d'après les minutes de retard et le seuil paramétré (RV05 : jusqu'au seuil retard, au-delà absent, un retard supérieur à la durée de la séance est refusé) ; on ne fait pas l'appel d'une séance annulée, d'un jour sans classe, d'une séance absente de l'emploi du temps en vigueur ce jour-là ni d'un jour futur. **Verrouillage (RV04, D59)** : l'auteur modifie son appel jusqu'à la fin de la journée dans le fuseau de l'établissement ; ensuite, ou pour tout autre utilisateur, seule la vie scolaire ou la Direction (`ATTENDANCE_CORRECT`) corrige, avec **motif obligatoire**, chaque changement de statut étant écrit dans `AttendanceCorrection` et l'audit (`ATTENDANCE_CALL_CORRECT`) ; un appel oublié se saisit après coup par la même voie. **Hors ligne** : l'appel porte `saisiLe` (heure de l'appareil, bornée à 45 jours comme les encaissements) ; s'il tombe le jour de la séance, l'appel arrivé après minuit reste accepté sans motif comme appel de l'auteur (à faire confiance à l'horloge de l'appareil, même limite qu'au Lot 6). **Justificatifs (D58)** : saisis et décidés sous `ATTENDANCE_CORRECT` (au nom du responsable, le portail parent vient au Lot 11) ; motif de la liste ou commentaire obligatoire ; déposé après le délai (jours de classe, hors jours sans classe du calendrier) il est enregistré et signalé « hors délai », pas refusé d'office ; une seule décision (accepter ou refuser). **Paramètres provisoires modifiables** (`GET/PATCH /pedagogy/settings`, onglet « Assiduité » de Vie scolaire) : `School.retardMaxMinutes` (15, D57) et `School.delaiJustificatifJours` (3, D58), jamais codés ailleurs. Lectures : `GET /attendance/day`, `/sheet`, `/absences` (filtres classe, période, justificatif), `/students/:id/history` (compteurs : séances appelées, absences, retards, excusées, non justifiées). **Permissions** (migration `20260921150000_lot9_assiduite`, nommée après `lot8` pour l'ordre d'application) : `ATTENDANCE_TAKE`, `ATTENDANCE_CORRECT`, `ATTENDANCE_READ` (données de mineurs, RV12) ; Administrateur et Direction les ont toutes, l'Auditeur lit seulement, la Secrétaire-caissière n'a rien. **Nouveau rôle `SURVEILLANT`** (vie scolaire) : lecture des élèves, de l'emploi du temps, et les trois permissions. Le rôle ENSEIGNANT n'existe pas encore : les enseignants n'ont pas de compte (Lot 7), donc l'appel est fait par le surveillant ; quand ils en auront, un rôle avec `ATTENDANCE_TAKE` sans `ATTENDANCE_CORRECT` se comporte déjà comme prévu (testé avec un rôle « appel seul »). Audit RG15 : création, mise à jour, correction, justificatif, décision, motifs. **Frontend** : page `/appel` (lien de menu sous `ATTENDANCE_READ`), onglet Appel (jour, classe, séances avec leur état, « En attente d'envoi » si l'appel est encore sur l'appareil) et feuille d'appel (tout présent, boutons Présent / Retard / Absent, minutes de retard avec avertissement au-delà du seuil, motif demandé pour une correction, message clair quand la modification est interdite) ; onglet Absences et justificatifs (liste filtrée, développable, justificatif, accepter ou refuser, export PDF et Excel) ; carte « Assiduité » dans la fiche de l'élève ; onglet « Assiduité » de Vie scolaire (règles et motifs). L'appel passe par la file d'attente hors ligne existante (`kind: "attendance"`, page Synchronisation : « Appel ») et « Préparer le mode hors ligne » copie les séances du jour et leurs feuilles d'appel. **Ne fait pas encore** : alertes et notifications (Lot 12 ; D61 : aucun seuil d'alerte inventé), portail parent et déclaration par le parent (Lot 11), compte enseignant, pointage des enseignants (Lot 10), appel par demi-journée (option de D60 non construite), justificatif avec pièce jointe. Le filtre de classe de l'écran Appel ne propose que les classes de l'année active. 21 tests e2e ajoutés (`attendance.e2e-spec.ts`, dates relatives à aujourd'hui, tous les jours en jours de classe), 202 au total ; vérifié dans le navigateur à 390 px (feuille d'appel, statuts, calcul du seuil, enregistrement, **appel créé sans Internet puis synchronisé sans doublon et marqué « hors ligne »**, modification hors ligne d'un appel existant, justificatif et acceptation, onglet Vie scolaire, carte de l'élève, aucun débordement) ; données de test supprimées de la base locale. **Déployé en production le 21 septembre 2026** : migration appliquée au démarrage de l'API (tables d'assiduité vides, paramètres à 15 minutes et 3 jours), permissions et rôle `SURVEILLANT` créés par un script ponctuel sans réinitialiser les autres permissions (vérifié en production que ce rôle n'existait pas), routes vérifiées (401 sans jeton), conteneurs Elyon non touchés. Un compte SURVEILLANT reste à créer par l'Administrateur (Utilisateurs & rôles).
- **Lot 10 : pointage des enseignants par QR code (21 septembre 2026)** : à la demande de la Direction, **D62 est modifiée** (`DECISIONS_PENDING.md`) : l'enseignant scanne lui-même, au lieu d'être pointé par le surveillant. **Deux modes par enseignant** (`Teacher.modePointage`, réglé dans Vie scolaire, Enseignants) : `SEANCE` (collège, lycée) scanne le **QR de la salle** au début et à la fin de chaque cours ; `JOURNEE` (maternelle, primaire) scanne le **QR de l'entrée de l'école** à l'arrivée et au départ. **Compte relié à une fiche** : `Teacher.userId` (unique : un compte, une fiche ; onglet Enseignants, liste `GET /teachers/linkable-users`) ; nouveau rôle **`ENSEIGNANT`** (`TIMETABLE_READ` et `TEACHER_CHECKIN_SELF` seulement : aucune donnée d'élève, l'appel par l'enseignant reste à décider). **QR codes** : modèle `PointageCode` (un par salle, un pour l'entrée), jeton aléatoire jamais renvoyé par une route ouverte (`GET /rooms` ne le contient pas : le lire à distance annulerait la preuve de présence), gérés sous `PEDAGOGY_MANAGE` (générer les manquants, **régénérer** : l'ancien QR cesse aussitôt de fonctionner, supprimer une salle supprime son QR) ; le QR encode `{origine}/pointage?c=jeton`, l'appareil photo du téléphone l'ouvre, le code imprimé dessous sert de secours. Limite honnête : un QR imprimé peut être photographié ; la protection est la validation par un tiers, les écarts signalés et la régénération (un QR tournant sur écran serait une évolution). **Règles** (`apps/api/src/teacher-checkins/`) : le serveur décide de la nature du scan, l'enseignant ne choisit pas. Par séance : parmi ses séances du jour (remplaçant compris, séances annulées exclues), un **début** est possible de la fenêtre d'ouverture jusqu'à la fin de la séance, une **fin** d'après le début jusqu'à la fenêtre après la fin ; on retient l'événement dont l'heure prévue est la plus proche, **à égalité la fin d'abord** (à la jonction de deux cours, un scan clôt le premier et le suivant ouvre le second) ; retard = début pointé moins l'heure prévue, **signalé au-delà de la tolérance** (D63) ; un QR d'une autre salle que celle prévue est accepté mais marqué « autre salle » ; un scan rejoué à l'identique ne change rien (idempotent) ; un double scan trop rapproché est refusé (« vous avez déjà pointé à HH:mm »). Par journée : premier scan = arrivée (retard comparé à la première séance du jour, **aucune heure d'attente inventée sans séance**), les suivants = départ (le dernier compte). QR d'un mauvais type, inconnu ou remplacé, compte non relié, fiche inactive, jour sans séance : refus explicites. **Validation (RV06)** : le pointage naît `EN_ATTENTE` ; un tiers (`TEACHER_CHECKIN_VALIDATE`) le valide ou le rejette (motif obligatoire), une seule décision ; **le serveur refuse qu'un validateur traite ou corrige son propre pointage** (testé avec un rôle qui cumule pointage et validation) ; la vie scolaire saisit ou corrige un pointage (enseignant sans téléphone, scan oublié) avec un **motif obligatoire**, validé d'office puisque posé par un tiers, tracé dans l'audit (`TEACHER_CHECKIN_MANUAL`, valeur avant et après). **Hors ligne** : le scan part avec l'heure de l'appareil (`scanneLe`, bornée à 45 jours comme les encaissements), la file d'attente existante (`kind: "checkin"`, page Synchronisation : « Pointage ») le rejoue dans l'ordre, le serveur résout la séance d'après cette heure ; l'écart entre heure du scan et heure de réception est affiché (« hors ligne »). **Heures effectuées (RV07, D64)** : `GET /teacher-checkins/summary?month=` calcule d'après les **seuls pointages validés** : par séance, séances tenues (début et fin validés) et leur **durée prévue**, séances en attente, rejetées, incomplètes, non pointées ; par journée, jours présents et heures entre arrivée et départ ; retards signalés ; le remplaçant est crédité de la séance qu'il tient, l'absent voit « séances confiées à un remplaçant » ; export PDF et Excel. **Permissions** (migration `20260921160000_lot10_pointage_enseignants`, nommée après `lot9`, générée par `prisma migrate diff` car `migrate dev` demandait une confirmation interactive) : `TEACHER_CHECKIN_SELF`, `TEACHER_CHECKIN_READ`, `TEACHER_CHECKIN_VALIDATE` ; Administrateur, Direction et Surveillant lisent et valident, l'Auditeur lit. **Paramètres provisoires modifiables** (`School`, `GET/PATCH /pedagogy/settings`, onglet « QR codes et règles ») : `pointageFenetreMinutes` 15, `pointageToleranceMinutes` 10, `pointageEcartMinMinutes` 5. **Frontend** : page `/pointage` (« Mon pointage », `TEACHER_CHECKIN_SELF`) avec scan caméra (`jsqr`, `components/qr-scanner.tsx`, caméra coupée après usage, saisie du code en secours), résultat clair, état du jour, scans en attente d'envoi ; un QR ouvert avec l'appareil photo arrive sur `/pointage?c=...` et **pointe aussitôt, y compris après connexion** (`/login?next=...` : l'adresse demandée est conservée, adresse interne seulement) ; page `/pointage-enseignants` (`TEACHER_CHECKIN_READ`) : pointages du jour (valider, rejeter, saisir, corriger), heures du mois (tableau et export), QR codes et règles (QR à l'écran, PDF de tous les QR, régénérer). Le préchargement hors ligne est devenu prudent pour un compte sans `STUDENT_READ` (il levait une erreur 403 non gérée). **Ne fait pas encore** : géolocalisation, QR tournant, appel des élèves par l'enseignant lui-même, paie. 23 tests e2e ajoutés (`teacher-checkins.e2e-spec.ts`, scans datés d'hier avec `scanneLe`), 225 au total ; vérifié dans le navigateur à 390 px (lien de QR ouvert sans être connecté, connexion puis début de séance pointé automatiquement, double scan refusé, fin de séance, arrivée d'un enseignant en mode journée **scannée sans Internet puis synchronisée**, validation par l'administrateur, heures du mois « 0 h 40 », QR à l'écran et PDF généré, réglage du mode et du compte dans Vie scolaire, aucun débordement) ; **le décodage de la caméra a été vérifié par un aller-retour QR généré puis décodé avec `jsqr`, mais la vraie caméra d'un téléphone n'a pas pu être testée ici** ; données de test supprimées de la base locale. **Déployé en production le 21 septembre 2026** : migration appliquée au démarrage de l'API (tables de pointage vides, paramètres à 15, 10 et 5 minutes), permissions et rôle `ENSEIGNANT` créés par un script ponctuel sans réinitialiser les autres permissions (vérifié en production que ce rôle n'existait pas), routes vérifiées (401 sans jeton), conteneurs Elyon non touchés. **Pour démarrer** : créer les comptes des enseignants (rôle Enseignant) dans « Utilisateurs & rôles », les relier à leur fiche et choisir leur mode dans Vie scolaire, Enseignants, puis générer et imprimer les QR dans « Pointage enseignants », onglet « QR codes et règles ».
- **Lot 11 : comptes parents et portail en lecture (21 septembre 2026)** : un responsable consulte, en lecture seule, l'emploi du temps, les absences et retards, la situation financière et les reçus de ses enfants. **Décision d'architecture principale : un compte parent n'est PAS un `User`.** Plusieurs routes du personnel (`GET /rooms`, `/school`, `/academic-years`, `/absence-reasons`...) n'exigent qu'un utilisateur connecté, sans permission : un parent bâti sur `User` y aurait eu accès, et une simple erreur de permission sur une route future aurait exposé des élèves d'autres familles. Le parent a donc sa **propre table** (`ParentAccount`, relié à un `Guardian` déjà enregistré), son **propre jeton** (clé dérivée de `JWT_ACCESS_SECRET` mais différente, marqueur `typ: parent`), sa **propre garde** (`ParentAuthGuard`, les routes `/portal/*` sont publiques pour la garde du personnel) et son **propre cookie** de renouvellement (`parent_refresh_token`, chemin `/portal`, httpOnly). Un jeton de parent n'ouvre aucune route du personnel et inversement ; c'est **testé sur douze routes du personnel** dont celles sans permission. **Activation (D66, D76)** : le secrétariat génère un code (`POST /parent-accounts/guardians/:id/activation-code`, 8 caractères sans lettres ambiguës, « K7MQ-2XPA »), **montré une seule fois** ; seule son empreinte SHA-256 est conservée (ni le code ni le mot de passe n'apparaissent jamais dans l'audit) ; un seul code valable à la fois ; durée de validité paramétrée (`School.parentCodeValiditeJours`, 7 par défaut) ; à usage unique ; **brûlé après 5 essais faux** ; les messages d'erreur ne distinguent jamais « numéro inconnu » de « code faux ». `POST /portal/activate` (numéro, code, mot de passe de 8 caractères au moins, consentement coché, version de la politique) crée le compte et **enregistre le consentement** (`ParentConsent`, une ligne par acceptation avec la version) ; la version `CONSENT_VERSION` de l'API et celle de la page web sont à garder identiques, une version périmée est refusée. Le numéro saisi se compare aux chiffres seuls (« +242 06 000 0001 » égale « 242060000001 »). Un nouveau code sur un compte existant **réinitialise le mot de passe et ferme les sessions ouvertes**. Connexion par numéro et mot de passe, verrouillage 15 minutes après 5 échecs, désactivation immédiate (`PARENT_ACCOUNT_MANAGE`). **Isolation (RV08, D67)** : chaque route `/portal/children/:id/...` vérifie que l'élève est rattaché à CE responsable avec l'accès non retiré (`assertChild`) ; sinon **404**, jamais 403 (on ne confirme pas l'existence de l'élève). `StudentGuardian.accesPortail` permet à la **Direction seule** (`PARENT_ACCESS_REVOKE`) de retirer ou rétablir l'accès d'un responsable pour UN enfant, avec motif obligatoire et audit, sans toucher aux autres responsables de cet enfant ni aux autres enfants du même responsable. **Ce que le parent reçoit** est réduit à l'essentiel : jamais le motif interne d'un changement d'emploi du temps, ni les corrections d'appel, ni le nom du validateur, ni les identifiants internes, ni le jeton de vérification des reçus (testé sur le contenu des réponses). Les libellés financiers sont adaptés (« Paiement en retard » y compris pour un impayé critique). **Permissions** (migration `20260921170000_lot11_comptes_parents`, générée par `prisma migrate diff`) : `PARENT_ACCOUNT_MANAGE` (Administrateur, Direction, Secrétaire-caissier), `PARENT_ACCESS_REVOKE` (Direction). Les parents n'ont ni rôle ni permission. **Frontend** : espace `/parents` (connexion, activation, enfants, fiche de l'enfant en trois onglets : emploi du temps par semaine, absences, finances et reçus, changement de mot de passe, politique de confidentialité), en français seulement (D68) ; page du personnel `/portail-parents` (« Comptes parents » : recherche, code d'activation affiché une fois avec message à copier, désactiver, réactiver, retrait d'accès par enfant) ; lien « Espace parents » sur la connexion du personnel. Le client du portail (`lib/portal-api.ts`) est distinct du client du personnel et ne garde aucune copie hors ligne. **Ne fait pas encore** : déclaration d'un justificatif par le parent (D58 prévoit qu'il le déclare : ce lot est en lecture seule, il remet un mot au secrétariat), notifications (Lot 12), messagerie (Lot 13), impression du reçu, récupération de mot de passe par SMS ou e-mail, anglais. **La politique de confidentialité affichée est un projet à faire valider par l'école ou un conseil juridique avant toute ouverture aux parents.** 19 tests e2e ajoutés (`parent-portal.e2e-spec.ts`), 244 au total ; vérifié dans le navigateur à 390 px (code généré côté secrétariat, activation avec numéro saisi sous une autre forme et case de consentement obligatoire, fiche de l'enfant : emploi du temps, absence excusée avec motif, situation financière et reçu, déconnexion, mauvais mot de passe puis connexion) ; données de test supprimées de la base locale.
  **Déployé en production le 21 septembre 2026** : migration `20260921170000_lot11_comptes_parents` appliquée au démarrage de l'API (tables parents vides, code valable 7 jours), `PARENT_ACCOUNT_MANAGE` et `PARENT_ACCESS_REVOKE` créées par un script ponctuel sans réinitialiser les autres permissions, routes vérifiées (`/portal/consent-info` 200 avec la version `2026-09-v1`, `/portal/me` et `/parent-accounts` 401 sans jeton, pages `/parents/connexion` et `/parents/confidentialite` 200), conteneurs Elyon non touchés. **Pour démarrer** : le secrétariat vérifie les numéros de téléphone des responsables sur les fiches élèves, puis génère un code par responsable dans « Comptes parents » et le lui remet en main propre. **Avant d'ouvrir aux parents : faire valider le texte de la politique de confidentialité par l'école.** Aucune donnée réelle n'a été utilisée pour les tests.
- **Lot 12 : notifications aux parents (21 septembre 2026)** : un responsable est prévenu quand une absence ou un retard est saisi pour son enfant, quand un cours de sa classe est annulé ou remplacé, et quand l'emploi du temps de sa classe change. **Canaux (D65)** : le message dans l'application est toujours créé ; l'alerte push est gratuite (Web Push, standard VAPID, bibliothèque `web-push`, aucun fournisseur payant, SMS et WhatsApp non activés) et s'ajoute si le parent l'a activée sur son téléphone. **Rien de sensible sur le canal externe (RV10, D69)** : l'alerte dit « Une absence a été signalée pour Alice. Ouvrez l'application pour le détail. », sans matière, heure, durée, motif, note ni montant (testé sur le contenu exact des alertes) ; le détail de la séance n'est visible que dans l'application connectée, le motif d'un changement ponctuel ou d'un justificatif n'est jamais transmis. Aucun type de notification financière n'existe (D70). **Ne bloque jamais la saisie** : les points d'entrée `NotificationsService.notifyAttendance/notifySessionChange/notifyTimetablePublished` attrapent toute erreur, les messages dans l'application sont créés tout de suite et l'envoi push part en arrière-plan (`idle()` l'attend dans les tests). Testé : service push qui refuse, qui lève une exception, mécanisme de notification entièrement en panne (l'appel est enregistré, la publication aboutit), appareil disparu (abonnement supprimé, la notification reste dans l'application). Le suivi d'envoi est conservé par notification (`pushStatut` : ENVOYE, ECHEC avec l'erreur, DESACTIVE, AUCUN_APPAREIL, GROUPE). **Qui reçoit (RV08, D67)** : seuls les responsables dont le compte est ACTIF et dont l'accès au portail n'est pas retiré pour CET enfant ; le retrait d'accès fait aussi disparaître de la liste les notifications déjà reçues sur cet enfant (elles contiennent son prénom). Testé, avec la mutation « filtre d'accès supprimé » qui fait échouer le test (un premier jet de ce test ne la voyait pas, à cause du regroupement : la notification existante absorbait la suivante, il marque désormais la première comme lue). **Événements** : ABSENCE et RETARD (uniquement ce qui vient de changer d'après l'état précédent de l'appel : un appel renvoyé à l'identique après une coupure ne renotifie personne), ENSEIGNANT_ABSENT (séance ANNULEE ou REMPLACEE), EMPLOI_DU_TEMPS_MODIFIE (changement de salle et publication d'une nouvelle version). Un changement sur un jour déjà passé n'alerte personne. Une annulation peut avoir une autre cause qu'une absence : le texte dit « cours annulé ou remplacé ». **Regroupement (D71)** : un enfant absent toute la journée ne fait pas six alertes : absences, retards et cours annulés du même enfant le même jour complètent une seule notification tant qu'elle n'est pas lue (une seule alerte push, le message dit « 2 absences ») ; les changements d'emploi du temps se regroupent pendant `School.notifGroupeMinutes` (60 minutes, provisoire, modifiable en base, pas encore d'écran de réglage). Une notification lue n'absorbe plus rien : l'événement suivant en crée une nouvelle et alerte. **Préférences** : par parent et par type d'événement (`ParentNotificationPreference`, absent = activé), respectées à l'envoi (`pushStatut = DESACTIVE`, le message reste dans l'application). **Appareils** (`ParentPushSubscription`, `endpoint` unique) : un téléphone partagé suit le parent qui vient de s'abonner, jamais le précédent, et la déconnexion retire l'appareil. **Modèle** (migration `20260921180000_lot12_notifications`, générée par `prisma migrate diff`, uniquement additive) : `ParentNotification`, `ParentNotificationPreference`, `ParentPushSubscription`, `School.notifGroupeMinutes`. Suppression en cascade avec le compte parent. Aucune nouvelle permission (les parents n'en ont pas). **Routes du portail** (`ParentAuthGuard`, jamais le jeton du personnel) : `GET /portal/notifications`, `GET /portal/notifications/unread-count`, `PATCH /portal/notifications/:id/read` (404 pour la notification d'une autre famille), `POST /portal/notifications/read-all`, `GET`/`PUT /portal/notification-preferences`, `GET /portal/push/public-key`, `POST /portal/push/subscriptions` (adresse https obligatoire ; un corps sans `keys` répondait 500, corrigé en 400), `POST /portal/push/unsubscribe`. Les accroches métier sont dans `AttendanceService.saveCall`, `OccurrencesService.createException` et `TimetablesService.publish`. **Variables d'environnement** : `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, documentées dans les deux `.env.example` et **déclarées dans le bloc `environment:` du service `academy-api` de `docker-compose.yml`** (sans cela elles n'atteignent pas le conteneur). Vides = aucune alerte push, les notifications restent dans l'application, sans erreur. Changer la paire de clés invalide tous les appareils déjà enregistrés. **Frontend** : cloche avec le nombre de non lues dans l'en-tête de l'espace parents (mise à jour toutes les minutes et à chaque changement de page), page `/parents/notifications` (liste, un clic ouvre la fiche de l'enfant et marque lue, « Tout marquer comme lu », réglage par type d'événement, activation des alertes sur ce téléphone avec l'explication d'installation sur iPhone/iPad et l'état « bloqué » du navigateur), gestionnaires `push` et `notificationclick` dans `public/sw.js`, `lib/push.ts`. **Ne fait pas encore** : écran de réglage de `notifGroupeMinutes`, vue du secrétariat sur les envois en échec (le suivi est en base), purge selon la durée de conservation (D77, aucune tâche planifiée), annonces (Lot 13), SMS et WhatsApp, notification quand un changement ponctuel est supprimé. 30 tests e2e ajoutés (`notifications.e2e-spec.ts`), 6 tests unitaires (`notification-texts.spec.ts`) ; 274 tests e2e et 6 tests unitaires du module passants, `tsc` propre sur les deux apps. Les tests utilisent un faux service push (`test/utils/fake-push-sender.ts`, branché par `createTestApp`) : aucun envoi réel en CI. **Vérifié dans le navigateur** (base jetable, supprimée ensuite) : activation d'un compte, absence et changement de salle saisis côté école, cloche à 2, page des notifications avec le détail et sans le motif interne, réglage « Absence signalée » désactivé et conservé en base, clic sur une notification (marquée lue, ouvre la fiche de l'enfant), déconnexion puis redirection vers la connexion, mise en page à 390 px sans défilement horizontal. **Preuve du vrai envoi** : un faux appareil FCM avec de vraies clés de chiffrement a été abonné, le vrai expéditeur (signature VAPID) a envoyé, le vrai service de Google a répondu que l'appareil n'existe pas, l'abonnement a été supprimé et la notification est restée dans l'application. **Non vérifié** : l'affichage d'une vraie alerte sur un vrai téléphone (le navigateur intégré refuse les notifications).
  **Déployé en production le 21 septembre 2026** : migration `20260921180000_lot12_notifications` appliquée au démarrage de l'API (tables vides, regroupement à 60 minutes), **paire de clés VAPID générée sur le serveur** (jamais affichée, écrite dans `.env.production` en mode 600, 3 lignes `VAPID_*`) : **ne plus la changer** une fois des parents abonnés, sinon tous leurs appareils devront se réabonner ; aucune nouvelle permission ; routes vérifiées (`/portal/notifications`, `/portal/notification-preferences` et `/portal/push/public-key` répondent 401 sans jeton de parent, `/parents/notifications` 200, `sw.js` servi avec les gestionnaires push), aucun avertissement « VAPID absentes » dans les logs, conteneurs Elyon non touchés. Aucun compte parent n'existe encore en production : les alertes n'ont donc pas pu être essayées avec un vrai téléphone. **Pour essayer** : activer un compte parent (Comptes parents), ouvrir l'espace parents sur un téléphone, page Notifications, « Activer les alertes sur ce téléphone », puis saisir une absence côté école. Sur iPhone, ajouter d'abord l'application à l'écran d'accueil.
- **Lot 13 : messagerie sécurisée et annonces (21 septembre 2026)** : les enseignants, la vie scolaire et la Direction échangent avec les responsables des élèves dans l'application, et publient des annonces de classe. **La règle centrale (RV09, D72) est vérifiée côté serveur à chaque ouverture ET à chaque envoi**, pas seulement à la création : un enseignant n'écrit qu'aux responsables d'un élève dont la CLASSE ACTUELLE lui est affectée, un parent n'écrit qu'aux enseignants affectés à la classe de SON enfant, jamais à un autre parent ; si l'enseignant perd son affectation, il ne peut plus écrire, le parent non plus (l'historique reste lisible et le parent est invité à écrire à l'école) ; on ne peut écrire qu'à un responsable au compte actif et à l'accès non retiré pour l'élève (le retrait d'accès de la Direction, D67, coupe la messagerie sur cet enfant des deux côtés et cache la conversation au parent). La vie scolaire et la Direction (`MESSAGE_DESK`) tiennent le « guichet de l'école » : conversation partagée entre elles, écriture à tout responsable, annonce pour toute classe. Un enseignant est relié à son compte par `Teacher.userId` (Lot 10) : sans ce lien il n'est pas proposé aux parents. Un test **casse volontairement la règle** (`teacherCanReach` qui répond toujours oui) : deux tests échouent. **Modèle** (migration `20260921190000_lot13_messagerie`, générée par `prisma migrate diff`, uniquement additive) : `MessageThread` (un responsable, un interlocuteur, un élève ; type ENSEIGNANT ou ECOLE ; dernière lecture de chaque côté), `Message` (retrait modéré : `retireAt/retireParId/retireMotif`, jamais supprimé), `MessageReport`, `Announcement` ; `School.messageDelaiReponseJours` (2) et `School.messageNumeroMinChiffres` (9), **valeurs provisoires modifiables en base, pas encore d'écran de réglage** ; deux types de notification (`MESSAGE_RECU`, `ANNONCE`). **Aucun numéro échangé ni affiché (RV09)** : aucune réponse de l'API ne contient de téléphone ni d'e-mail (testé sur treize réponses, y compris la supervision) ; un message qui contient une suite d'au moins 9 chiffres (« 06 12 34 56 78 », « +242 06 000 0001 », « 06.12.34.56.78 ») est refusé, alors qu'une date « 21.09.2026 », un montant ou un numéro de salle passent. Le contrôle ne voit pas un numéro écrit en toutes lettres. Un message refusé à l'ouverture ne laisse pas de conversation vide. **Supervision (D73, RV11)** : `MESSAGE_SUPERVISE`, Direction seule (ni un enseignant, ni la vie scolaire, ni l'administrateur : testé). La liste ne montre que des repères, jamais un extrait du texte ; **chaque ouverture d'une conversation est journalisée** (`MESSAGE_SUPERVISED_READ`, avec le nombre de messages, jamais leur texte). **Signalement et retrait (D75)** : un responsable ou un membre du personnel signale un message reçu (jamais le sien, une seule fois) ; la Direction traite les signalements et retire un message avec un motif obligatoire : le message reste en base, les participants voient « Ce message a été retiré par la Direction » sans le motif, la Direction voit le texte d'origine et le motif. Les annonces se retirent de la même façon (auteur ou Direction). Le journal ne contient jamais le texte d'un message. **Notifications (Lot 12 étendu)** : un nouveau message prévient SEUL le responsable destinataire (pas l'autre responsable de l'enfant), sans le contenu, ni sur l'alerte push (« Vous avez un nouveau message concernant Alice ») ni dans le titre de l'annonce ; plusieurs messages le même jour ne font qu'une notification et une alerte ; une annonce prévient les responsables des élèves de la classe (une notification par enfant, une seule alerte, regroupée par fenêtre) ; une panne de notification ne bloque pas l'envoi du message. Le personnel n'a pas d'alerte push : il voit un compteur dans la messagerie. **Permissions** : `MESSAGE_USE` (Enseignant, Direction, vie scolaire), `MESSAGE_DESK` (Direction, vie scolaire), `MESSAGE_SUPERVISE` (Direction). **Consentement** : la version de la politique passe à `2026-09-v2` (`CONSENT_VERSION` et page web, à garder identiques) : elle mentionne maintenant les notifications, la messagerie, la supervision par la Direction et le retrait modéré ; aucun compte parent n'existait en production, donc aucun parent n'a à reconfirmer. **Le texte reste un projet à faire valider par l'école ou un conseil juridique**, et l'information du PERSONNEL sur la supervision reste à intégrer à des conditions d'usage à rédiger. **Frontend** : espace parents `/parents/messages` (conversations, nouveau message avec choix de l'enfant puis de l'enseignant ou de l'école, avis de supervision et délai de réponse indicatif), `/parents/messages/[id]` (fil, réponse, signalement, lecture seule si l'enseignant n'est plus affecté), `/parents/annonces`, icône des messages non lus dans l'en-tête ; côté personnel `/messagerie` (conversations, nouveau message classe puis élève puis responsable, onglets Supervision et Signalements pour la Direction, avec l'avis « cette lecture est enregistrée ») et `/annonces`. **Ne fait pas encore** : pièces jointes (D75 : texte seul), écran de réglage du délai et du seuil de chiffres, alerte push pour le personnel, purge selon la durée de conservation (D77, aucune tâche planifiée), recherche dans le texte des messages, tableau de bord Direction (Lot 14). 32 tests e2e ajoutés (`messaging.e2e-spec.ts`) et 15 tests unitaires (`messaging.util.spec.ts`), 306 tests e2e au total, `tsc` et lint propres. **Vérifié dans le navigateur** (base jetable supprimée ensuite) : un parent refusé quand il écrit un numéro (message d'erreur clair), message envoyé à l'enseignant, l'enseignant le voit avec « 1 non lu » et répond, la Direction voit dans la supervision des repères sans texte, ouvre la conversation avec l'avis d'enregistrement (le journal contient bien `MESSAGE_SUPERVISED_READ` sans le texte), retire un message avec un motif (le parent voit « Ce message a été retiré par la Direction »), publie une annonce que le parent voit avec sa notification, et l'icône des messages non lus. **Non vérifié** : une vraie alerte sur un téléphone (déjà signalé au Lot 12).
  **Déployé en production le 21 septembre 2026** : migration `20260921190000_lot13_messagerie` appliquée au démarrage de l'API (tables vides, délai de réponse à 2 jours, seuil de chiffres à 9), permissions `MESSAGE_USE` (Enseignant, Direction, vie scolaire), `MESSAGE_DESK` (Direction, vie scolaire) et `MESSAGE_SUPERVISE` (Direction) créées par un script ponctuel sans réinitialiser les autres, routes vérifiées (`/messaging/*`, `/messaging/supervision/*`, `/portal/messages/*` et `/portal/announcements` répondent 401 sans jeton, `/portal/consent-info` renvoie la version `2026-09-v2`, pages `/messagerie` et `/parents/messages` 200), conteneurs Elyon non touchés. **Pour démarrer** : chaque enseignant doit avoir un compte relié à sa fiche (Vie scolaire, Enseignants) pour être joignable par les parents et pour écrire ; les responsables doivent avoir activé leur compte (Comptes parents). Aucune donnée réelle n'a été utilisée pour les tests. **Avant d'ouvrir la messagerie aux parents : faire valider la politique de confidentialité (`2026-09-v2`) et rédiger l'information du personnel sur la supervision par la Direction.**
- **Lot 14 : pilotage 360° (21 septembre 2026)** : tableau de bord de la Direction (`/pilotage`, permission `PILOTAGE_READ`, **Direction seulement** : ni l'administrateur, ni la vie scolaire, ni un enseignant, ni un auditeur, données de mineurs, RV12). **Règle d'or : aucun indicateur n'est saisi ni stocké**, tout est recalculé à chaque demande depuis les données sources (appels, justificatifs, pointages, emploi du temps) : le module n'a aucune table à lui, la seule migration ajoute un paramètre (`20260921200000_lot14_pilotage`). Testé : modifier un enregistrement à la source change aussitôt le tableau de bord, et consulter le tableau n'écrit rien (ni donnée, ni journal). **Indicateurs** (`GET /pilotage/dashboard?from&to&classId`, `PilotageService`) : **assiduité** (présences saisies, présents, retards, absents, taux de présence, d'absence et de retard, par classe et par jour, agrégés en SQL et non chargés en mémoire) ; **appels non faits** (séances prévues à l'emploi du temps, non annulées, sans appel ; aujourd'hui, une séance qui n'a pas encore commencé n'est pas comptée manquante) ; **ponctualité des enseignants** (séances ou jours prévus, tenus, non pointés, en attente de validation, retards au sens de la tolérance du Lot 10, heures effectuées, taux de présence et de ponctualité, par enseignant et au total ; les enseignants qui pointent à l'arrivée sont comptés en jours, les autres en séances, on additionne donc des « pointages attendus ») ; **alertes de décrochage**. Le récapitulatif mensuel du Lot 10 est généralisé à une période (`TeacherCheckinsService.summaryRange`, `summary` la réutilise : ses 23 tests passent inchangés). **Définition d'une absence « non justifiée » (à valider par la Direction)** : une absence est justifiée si son justificatif est accepté ; « en attente » si un justificatif attend une décision ; « dans le délai » si personne n'a encore justifié mais que le délai (`delaiJustificatifJours`, jours de classe hors fermetures, comme au Lot 9) n'est pas écoulé, aujourd'hui compris ; « non justifiée » si le justificatif est refusé, ou si le délai est écoulé sans justificatif. Ainsi un élève absent hier n'est pas signalé pour une absence qu'il peut encore justifier. Un test change ce délai à 1 jour et vérifie qu'une même absence bascule de « dans le délai » à « non justifiée ». Un test casse volontairement cette règle : quatre tests échouent. **Alertes de décrochage (D61 : aucun seuil inventé)** : `School.seuilAlerteAbsences` est vide par défaut et **les alertes sont désactivées** tant que la Direction n'en fixe pas un (champ dans Vie scolaire, onglet Assiduité, via `PATCH /pedagogy/settings`, `null` l'efface ; 0 est refusé). Un élève est signalé quand il atteint le seuil d'absences non justifiées sur la période affichée, avec ses absences en attente, dans le délai et ses retards. **Les alertes ne sont pas envoyées aux parents** : elles restent dans le tableau de bord. **Période** : par défaut le mois en cours jusqu'à aujourd'hui, au plus 92 jours (au-delà, le calcul séance par séance devient trop lourd : 400 avec un message clair), jamais un taux « 0 % » trompeur quand il n'y a rien à comparer (`null`, affiché « — »). Le filtre de classe s'applique à l'assiduité et aux alertes, pas aux enseignants (un enseignant enseigne dans plusieurs classes). **Exports** (`GET /pilotage/export/:kind`, `assiduite-classes`, `assiduite-jours`, `enseignants`, `alertes`) : CSV avec point-virgule et BOM UTF-8 (Excel), mêmes chiffres que l'écran, recalculés à l'export ; **chaque export est journalisé** (`PILOTAGE_EXPORT`, type et période, jamais les noms) puisqu'une liste nominative de mineurs sort de l'application ; l'export des alertes est refusé (409) tant qu'elles sont désactivées. **Bug de compilation évité** : `tsc --noEmit` acceptait un type non exporté (`AlertRow`) que la compilation de production (`nest build`, celle de l'image Docker) refusait ; corrigé, et `nest build` fait maintenant partie des vérifications avant un déploiement. **Frontend** : page `/pilotage` (période et classe, raccourcis 7, 30 et 90 jours, quatre indicateurs clés, alertes, assiduité par classe avec barres, taux d'absence par jour, appels non faits, tableau des enseignants, exports, rappel des règles utilisées), lien « Pilotage 360° » réservé à la permission, champ du seuil dans l'onglet Assiduité. **Ne fait pas encore** : envoi des alertes aux parents, comparaison entre deux périodes, tendance sur l'année, indicateurs financiers (ils existent déjà dans les pages Élèves insolvables et Tableau de bord), export PDF, tableau de bord par enseignant, période de plus de 92 jours. 19 tests e2e ajoutés (`pilotage.e2e-spec.ts`, jeu de données de quatre jours écrit en base dont chaque chiffre se compte à la main), 325 tests e2e au total, `tsc`, lint et `nest build` propres. **Vérifié dans le navigateur** (base jetable supprimée ensuite, même jeu de données que les tests) : les chiffres affichés sont ceux calculés à la main (46,2 % de présence, 7 absences dont 3 non justifiées, 5 appels), le seuil fixé depuis l'onglet Assiduité, l'alerte sur l'élève attendu (2 non justifiées, 1 en attente, 1 dans le délai), le téléchargement du CSV des alertes avec le bon nom de fichier et le bon contenu, aucun débordement horizontal à 375 px.
  **Déployé en production le 21 septembre 2026** : migration `20260921200000_lot14_pilotage` appliquée au démarrage de l'API (une colonne, **seuil d'alerte vide : alertes désactivées**, vérifié en base), permission `PILOTAGE_READ` attribuée au seul rôle Direction par un script ponctuel sans réinitialiser les autres, routes vérifiées (`/pilotage/dashboard` et `/pilotage/export/*` répondent 401 sans jeton, page `/pilotage` 200), conteneurs Elyon non touchés. Le commit du lot contient aussi du reformatage sans changement de logique sur quelques fichiers voisins (effet du lint automatique sur `src/pedagogy` et `src/teacher-checkins`). **Pour l'utiliser** : la Direction se connecte, ouvre « Pilotage 360° » ; pour activer les alertes de décrochage elle fixe un seuil dans Vie scolaire, onglet Assiduité. Les chiffres n'ont de sens qu'avec des appels et des pointages réellement saisis : aucune donnée réelle n'a été utilisée pour les tests. **Clôture de la vague 1** : les Lots 7 à 14 de l'addendum sont livrés ; restent, en attente de décision de la Direction, la validation légale de la politique de confidentialité et des conditions d'usage (D76), la durée de conservation avec une purge (D77), l'enquête d'équipement des parents avant tout canal payant (D78) et l'envoi éventuel des alertes de décrochage aux parents (D61).
- **Durcissement des rôles (22 septembre 2026)** : audit des 44 contrôleurs (chaque route sensible vérifie déjà sa permission côté serveur, les permissions sont relues en base à chaque requête), quatre défauts corrigés. **1. L'enseignant fait l'appel de ses séances (D60)** : le rôle `ENSEIGNANT` n'avait ni `ATTENDANCE_TAKE` ni `ATTENDANCE_READ`, donc aucun accès à l'appel malgré D60. Il reçoit `ATTENDANCE_TAKE` **sans** `ATTENDANCE_READ` ; nouveau décorateur `@RequireAnyPermission` (lecture ou saisie) sur `GET /attendance/day` et `/sheet`, et `AttendanceService.scopeOf(user)` : avec `ATTENDANCE_READ` (vie scolaire, Direction) la portée est toute l'école, sinon uniquement les séances dont l'enseignant effectif est celui du compte (`Teacher.userId`, remplacements compris : le remplaçant gagne l'accès, le remplacé le perd). Hors de sa portée : 403 sur la feuille et sur `POST /attendance/calls`. Un enseignant ne voit ni les absences de l'école, ni l'historique d'un élève (ATTENDANCE_READ), ni le motif ou le commentaire d'un justificatif (RV12, seulement son statut). Compte sans fiche enseignant reliée : 403. Écran : lien Appel visible avec l'une des deux permissions, onglet des absences réservé à `ATTENDANCE_READ`, copie hors ligne des séances de l'enseignant (le serveur applique la portée). **2. Pas d'élévation de droits** (`auth/reserved-permissions.ts`) : six permissions réservées à la Direction (approbation des remises, des sorties, des annulations de paiement, retrait d'accès des parents, supervision de la messagerie, pilotage). Modifier les permissions d'un rôle, créer un compte, changer le rôle d'un compte ou réinitialiser son mot de passe est refusé (403) si l'action met en jeu un de ces droits que l'acteur ne détient pas : l'Administrateur ne peut plus s'accorder ces droits, créer ou promouvoir un directeur, ni réinitialiser le mot de passe d'un directeur (désactiver ou renommer un compte reste permis). **La Direction reçoit `USER_MANAGE`** pour pouvoir nommer un directeur (sinon personne ne le pourrait) : à valider par la Direction ; son écran ne montre pas l'onglet Rôles (`ROLE_MANAGE`). Si plus aucun compte Direction n'existe, il faut passer par la base (ou le seed). **3. Finances séparées des dossiers (`FINANCE_READ`)** : `STUDENT_READ` ouvrait aussi paiements, factures, remises, situation financière, insolvables, tableau de bord financier (dont le solde de caisse) et exports. Nouvelle permission `FINANCE_READ` sur ces routes ; `STUDENT_READ` ne garde que les dossiers, les listes par classe et leur export. Rôles : Administrateur, Direction, Secrétaire-caissier, Comptable et Auditeur la gardent ; le surveillant et l'enseignant ne l'ont plus. Écrans : lien Élèves insolvables, carte de situation financière de la fiche élève et chiffres du tableau de bord réservés à `FINANCE_READ` ; le lien Élèves exige `STUDENT_READ`. **4. Pas d'auto-approbation** : celui qui a demandé une remise (RG06), enregistré une sortie (D25) ou encaissé un paiement (RG09) ne peut plus l'approuver ou l'annuler lui-même (403), même si son rôle en a le droit. **Déploiement** : la migration `20260922090000_hardening_roles` crée `FINANCE_READ`, la donne à tout rôle qui avait `STUDENT_READ` sauf `SURVEILLANT`, donne `ATTENDANCE_TAKE` à `ENSEIGNANT` et `USER_MANAGE` à `DIRECTION` (idempotente, ne retire rien, rejouée sur des rôles de production simulés) : le seed n'étant pas rejoué à chaque déploiement, sans elle toutes les routes financières auraient répondu 403. Le seed contient les mêmes valeurs. **Tests** : `hardening.e2e-spec.ts` (portée de l'enseignant, remplacement, motif masqué, finances par rôle, élévation refusée et permise pour qui détient le droit, auto-approbation d'une sortie) et un cas de plus dans `payments` et `fees-and-invoices` (paiement, remise) ; **mutations vérifiées** (sans la portée 2 tests échouent, sans la garde des droits réservés 5, sans FINANCE_READ 1 par route, sans chaque refus d'auto-approbation 1). Les suites qui créaient un compte Direction par l'API le créent désormais en base. 350 tests e2e (+25) et 21 tests unitaires passent, la migration a été rejouée sur des rôles de production simulés, et le parcours a été vérifié dans le navigateur (enseignant : seule sa séance, appel enregistré ; surveillant : ni finances ni lien insolvables ; Direction : finances et comptes sans onglet Rôles). **Piège de compilation trouvé au déploiement (le premier build de l'image a échoué)** : `nest build` a refusé `messaging.util.ts` (`Property 'replace' does not exist on type 'never'`) alors que ce fichier n'avait pas changé : avec `noImplicitAny` désactivé, `text.match(...) ?? []` est inféré `never[]` ou non selon l'ordre dans lequel le compilateur vérifie les fichiers, et ajouter du code dans `roles.service.ts` a suffi à le faire basculer. Corrigé par un type explicite (`const runs: string[]`). Les tests (jest, sans vérification de types) ne pouvaient pas le voir : **`nest build` doit être lancé avant tout déploiement**, et `tsc --noEmit -p tsconfig.build.json` peut être trompé par le cache incrémental (ajouter `--incremental false`). `tsc --noEmit` avec `tsconfig.json` (qui inclut les tests) signale en plus 3 erreurs de typage préexistantes dans `auth.e2e-spec.ts` (sans effet sur le build). **Reste à faire** : rôle plus fin pour la vie scolaire (le surveillant peut encore lire tous les dossiers), pages Années, Structure et Établissement encore visibles de tout compte (lecture seule côté serveur), accueil de l'enseignant qui appelle `/students` sans y avoir droit (erreur silencieuse).
- **Lot 15 : notes, évaluations et bulletins (22 septembre 2026, première brique de la vague 2, D51)** : l'enseignant saisit les notes de SES matières, la Direction voit les résultats de toute l'école, valide les bulletins d'un trimestre, les publie, et les parents les consultent. Décisions D79 à D88 dans `DECISIONS_PENDING.md` §18 ; choix du propriétaire : moyennes sur 20 avec lettres A à F pour la section anglophone, bulletin visible des parents seulement après validation ; **tout le reste est provisoire et paramétrable**. **Modèle** (migration `20260922120000_lot15_notes`, générée par `prisma migrate diff` puis complétée des permissions en SQL idempotent, sur le modèle de `hardening_roles`) : `Evaluation` (classe, matière, trimestre, barème, coefficient), `Grade` (note, ou statut `ABSENT` / `DISPENSE`), `GradeCorrection` (historique, ancien statut facultatif), `SubjectAppreciation`, `BulletinPeriod` (état d'un trimestre pour une classe), `Bulletin` (instantané figé, contenu en JSON), `GradeBand` (tranches de lettres) ; colonnes `SubjectLevel.coefficient` (1), `Section.affichageLettres` (faux), `School.baremeDefaut` (20), `School.moyennePassage` (vide), `School.bulletinAfficheRang` (faux) ; valeur `BULLETIN_DISPONIBLE` de `NotificationType`. `test/utils/clean-database.ts` complété. **Règles de calcul** (`apps/api/src/grades/grade-calc.util.ts`, fonctions pures, 14 tests unitaires) : chaque note est ramenée sur 20 ; moyenne d'une matière pondérée par le coefficient de l'évaluation ; **un absent ou un dispensé est exclu, jamais compté 0, et sans aucune note il n'y a pas de moyenne** ; moyenne générale pondérée par le coefficient de la matière pour le niveau ; arrondi à 2 décimales et rang « à la compétition » (1, 2, 2, 4) calculé sur les valeurs arrondies. Lettres et mention admis seulement si la Direction a saisi des tranches ou une moyenne de passage : **rien n'est inventé** (D61). **Droits** (`prisma/seed-data.ts` + migration) : `GRADE_ENTER` (ENSEIGNANT, sans `GRADE_READ`, donc limité par le serveur à ses affectations comme pour l'appel), `GRADE_READ` (Direction et Administrateur, pas le surveillant ni l'auditeur : RV12), `GRADE_CORRECT` et `BULLETIN_VALIDATE` (Direction seule, **ajoutés aux droits réservés** de `auth/reserved-permissions.ts`). Un enseignant ne voit que les colonnes de ses matières, sans moyenne générale ni rang. **Cycle d'un trimestre** (`BulletinsService`) : `OUVERT` (saisie) puis `VALIDE` (la Direction fige un instantané : moyennes, rangs, lettres, mention) puis `PUBLIE` (visible des parents, les responsables sont prévenus). Après validation l'enseignant ne saisit plus, ne crée plus, ne modifie plus ; la Direction corrige avec un **motif obligatoire** (historique gardé, on ne peut pas effacer une note) et le bulletin figé ne bouge pas : elle est avertie qu'il faut valider de nouveau, et **la publication est refusée tant que des notes ont été corrigées depuis la validation**. Elle peut aussi rouvrir avec un motif (le bulletin publié disparaît aussitôt du portail). L'identifiant d'un bulletin ne change pas à la revalidation, l'appréciation générale déjà saisie est conservée. Le journal ne contient jamais la valeur d'une note ni un texte d'appréciation. Une année clôturée n'accepte plus rien. **Routes staff** `/grades/*` : `context`, `evaluations` (liste, création, modification, suppression tant que ouvert), `evaluations/:id/sheet`, `POST evaluations/:id/notes` (**POST et non PUT : commande idempotente rejouable par la file hors ligne** ; seuls les élèves listés sont touchés, `AUCUNE` efface, réponse 200), `results`, `appreciations`, `period` (`validate`, `reopen`, `publish`), `bulletins`, `bulletins/:id/appreciation`, `settings` (`PEDAGOGY_MANAGE` : barème, moyenne de passage, tranches, lettres par section, rang aux parents). Le coefficient d'une matière se règle par `PUT /subjects/:id/levels` (renvoyer les niveaux sans coefficient garde celui qui est enregistré). **Portail parent** : `GET /portal/children/:id/bulletins` et `.../:bulletinId`, uniquement les bulletins publiés, `assertChild` (404 hors famille), **ni rang, ni effectif, ni moyenne de la classe par défaut**. Notification `BULLETIN_DISPONIBLE` : texte générique, jamais une note (RV10) ; politique de confidentialité mise à jour et **`CONSENT_VERSION` passée à `2026-09-v3`** (API et page web identiques, aucun compte parent en production donc aucun nouveau consentement à recueillir ; texte toujours à valider juridiquement, D76). **Écrans** : page `/notes` (onglets Évaluations et notes, Résultats avec export Excel et PDF, Bulletins, Réglages ; lien selon `GRADE_ENTER` ou `GRADE_READ`), saisie en cartes sur mobile avec contrôle de la borne et des décimales, **hors ligne** (type `grades` dans la file, préchargement des évaluations et feuilles de l'enseignant), page d'impression `/bulletins/[id]` et PDF produit dans le navigateur (`lib/bulletin-pdf.ts`, logo de l'école), onglet Bulletins du portail parent, notification parent. Le lien Élèves n'apparaît plus sans `STUDENT_READ` et l'accueil ne demande plus la liste des élèves à un enseignant (erreur 403 silencieuse corrigée). **Ne fait pas encore** : mention (félicitations, avertissement), conseil de classe, bulletin annuel et moyenne annuelle, format propre à la section anglophone, appréciation obligatoire, comparaison entre trimestres, notes visibles des parents au fil de l'eau, saisie de notes par la Direction hors correction (le rôle n'a pas `GRADE_ENTER`), coefficient par type d'évaluation. **Tests** : 32 tests e2e (`grades.e2e-spec.ts`, jeu de données de quatre élèves calculé à la main : 13,11 / 10,89 / 16,45 / 14,00, moyenne de la classe 13,61) et 14 tests unitaires, 382 tests e2e au total, `nest build` propre. **Mutations vérifiées** : sans la portée de l'enseignant 4 tests échouent, sans le verrouillage 2, sans la porte de publication 1, sans le refus de publier après correction 1, avec un parent qui voit un bulletin non publié ou d'un autre enfant 1 chacun, avec le rang toujours montré aux parents 1, avec un absent compté 0 4, avec la moyenne générale visible d'un enseignant 1. **Vérifié dans le navigateur** (base jetable supprimée ensuite) : un enseignant saisit une dictée (note de 21 refusée avec « Entre 0 et 20 », bouton bloqué, moyenne du devoir 13,50 affichée), la Direction voit les résultats identiques au calcul à la main, règle les lettres et la moyenne de passage, valide, saisit une appréciation, ouvre le bulletin (lettres D, B, C, « Admis », rang 3e sur 4) et télécharge un PDF valide, publie ; le parent se connecte, voit une alerte, ouvre le bulletin sans rang ; mobile 375 px sans débordement (`scrollWidth === clientWidth`), trimestre validé en lecture seule pour l'enseignant. **Non vérifié** : la file hors ligne des notes n'a pas été testée sans réseau, et le PDF n'a été contrôlé que par sa structure (pas relu visuellement). **Déployé en production le 22 septembre 2026** (commit 4048a51, sauvegarde de la base avant, migration appliquée au démarrage de l'API, rôles vérifiés en base, tranches de lettres vides, aucun compte parent ni trimestre en production, consentement `2026-09-v3` servi, routes 401 sans jeton, conteneurs Elyon non touchés). **Pour démarrer** : la Direction saisit les trimestres et les affectations (Vie scolaire), puis dans Notes et bulletins, onglet Réglages, les coefficients, les tranches de lettres, la moyenne de passage et la visibilité du rang ; chaque enseignant a besoin d'un compte relié à sa fiche enseignant.
- **Lot 16 : cahier de textes et devoirs (22 septembre 2026, vague 2, D51)** : l'enseignant note ce qui a été fait en cours et le travail à faire, les parents les voient dans leur espace. Décisions D89 à D95 dans `DECISIONS_PENDING.md` §19 ; choix du propriétaire : **visible des parents dès l'enregistrement**, **alerte générique regroupée**, **texte seulement**. **Modèle** (migration `20260922180000_lot16_cahier_textes`, générée par `prisma migrate diff` puis complétée des permissions en SQL idempotent) : `TextbookEntry` (classe, matière, enseignant titulaire à la création, date de la séance, `contenu?`, `devoirs?`, `dateEcheance?`, pas de clé vers l'emploi du temps) et la valeur `DEVOIR_DONNE` de `NotificationType`. **Règles** (`apps/api/src/textbook/`) : au moins un des deux textes, 2000 caractères au plus ; date dans l'année active et **jamais dans le futur** ; échéance jamais avant la séance (ignorée sans devoir) ; **seul l'auteur modifie ou supprime** son entrée ; année clôturée en lecture seule ; journal sans le texte. **Droits** : `TEXTBOOK_WRITE` (ENSEIGNANT, limité par le serveur à ses couples classe-matière comme pour les notes), `TEXTBOOK_READ` (Direction, Administrateur, surveillant : lecture seule de toute l'école) ; **un enseignant lit toutes les matières des classes où il enseigne** (charge de travail), jamais les autres classes ; ni l'auditeur, ni le secrétariat, ni le comptable. **Routes** `/textbook` : `context`, liste (fenêtre de 30 jours par défaut, filtres classe, matière, période), `POST`, `PATCH /:id`, `DELETE /:id` ; portail `GET /portal/children/:id/textbook` (cahier de la classe ACTUELLE de l'enfant : devoirs à rendre d'abord, puis les 14 derniers jours ; 404 hors famille). **Alerte** : `DEVOIR_DONNE`, générique et regroupée par fenêtre de temps comme les annonces (« Un devoir a été donné pour la classe de {prénom} »), jamais la matière ni le contenu dans l'alerte externe (RV10) ; dans l'application, la matière et l'échéance. **Seulement à la création** d'un devoir encore à rendre : rien pour un simple contenu de séance, une échéance déjà passée, ni à la modification. Politique de confidentialité mise à jour et **`CONSENT_VERSION` passée à `2026-09-v4`** (aucun compte parent en production). **Écrans** : page `/cahier-de-textes` (formulaire de création, liste avec modification et suppression pour l'auteur, lecture seule pour les autres profils, filtres), création **hors ligne** (type `textbook` dans la file, l'`Idempotency-Key` évite les doublons ; modification et suppression en ligne seulement), onglet « Devoirs » du portail parent, alerte dans les notifications. **Ne fait pas encore** : pièce jointe, accusé de lecture, rendu du devoir par l'élève, signature du parent, devoirs récurrents, cahier prévisionnel (date future), lien avec la séance de l'emploi du temps, purge automatique (D77). **Tests** : 16 tests e2e (`textbook.e2e-spec.ts`), 398 tests e2e au total, `nest build` propre. **Mutations vérifiées** : sans la portée d'écriture 1 test échoue, sans le contrôle de l'auteur 1, sans la portée de lecture d'un enseignant 2, avec un parent qui voit toutes les classes 1, avec une alerte pour un simple contenu 1, avec une date future acceptée 1. **Vérifié dans le navigateur** (base jetable supprimée ensuite) : un enseignant crée une entrée avec devoirs (bouton bloqué tant qu'il n'y a aucun texte, dates bornées à l'année et à aujourd'hui), la modifie ; la Direction la lit sans formulaire ni bouton de modification ; le parent reçoit deux alertes (Alice et Brice, sans le texte du devoir), voit le devoir dans l'onglet Devoirs et le cahier de la classe ; mobile 375 px sans débordement. **Non vérifié** : la file hors ligne du cahier n'a pas été testée sans réseau. **Déployé en production le 22 septembre 2026** (commit a393447, sauvegarde de la base avant, migration appliquée au démarrage de l'API, droits vérifiés en base : ENSEIGNANT en écriture, Direction, Administrateur et surveillant en lecture, aucune entrée ni compte parent, consentement `2026-09-v4` servi, route 401 sans jeton). **Pour démarrer** : chaque enseignant a besoin d'un compte relié à sa fiche enseignant et d'affectations (Vie scolaire).
- **Lot 17 : paiement des frais par les parents, Mobile Money (23 septembre 2026, vague 2, D51)** : un parent paie une tranche de scolarité depuis son espace. Décisions D96 à D101 dans `DECISIONS_PENDING.md` §20 ; choix du propriétaire : **PawaPay** (comme Elyon), **frais du fournisseur à la charge de l'école**, **aucun rappel** (D70 intact). **Principe** : une **tentative** (`OnlinePayment`, migration `20260923080000_lot17_paiement_en_ligne`) est distincte du **paiement** (`Payment`), qui ne naît qu'à la **confirmation signée** du fournisseur : une tentative échouée ou abandonnée ne consomme aucun numéro de reçu (RG10), et aucune requête financière existante n'est touchée (toutes filtrent `statut = VALIDE`). `Payment.recuParUserId` devient **facultatif** (un paiement en ligne n'a pas d'encaisseur ; les écrans lisent `recuParUser?`), `School.paiementEnLigneActif` est l'interrupteur (éteint par défaut, réglé par l'Administrateur dans « Établissement »). **Règles** (`apps/api/src/online-payments/`) : un responsable rattaché à l'enfant (`assertChild`, 404 sinon) paie une tranche (ligne de facture émise) pour tout ou partie du solde ; une seule tentative en attente par tranche (409) ; montant entier XAF plafonné au solde relu après revérification ; numéro normalisé (9 chiffres ou 242 + 9), l'opérateur est déterminé par PawaPay (`predict-provider`) ; la tentative est créée AVANT l'appel au fournisseur (le retour doit pouvoir la retrouver) et passe `ECHOUE` sur refus. **Confirmation** : dans une transaction, prise conditionnelle `EN_ATTENTE|ECHOUE` vers `CONFIRME` (un retour rejoué ne fait rien, un `COMPLETED` après un échec ambigu à l'initiation est accepté : le fournisseur fait foi), contrôle du solde, **numéro de reçu attribué dans la même transaction** (`numberSequence.upsert` sur le client transactionnel, sinon un échec de transaction laisserait un trou), `Payment` Mobile Money avec `referenceExterne = depositId`, audit `PAYMENT_ONLINE_CONFIRM` sans utilisateur. **Solde disparu entre-temps** (encaissement au guichet) : `A_TRAITER`, aucun reçu ; la Direction clôture avec un motif (`PAYMENT_ONLINE_RESOLVE`) après remboursement fait hors de l'application. **Retour perdu** : une tentative en attente depuis plus de 2 minutes est revérifiée (`GET /deposits/{id}`) à la lecture par le parent, à la nouvelle demande de paiement, ou au bouton « Vérifier » du personnel (503 si le fournisseur ne répond pas), car il n'y a pas de planificateur. **Routes** : webhook `POST /webhooks/payment/pawapay/deposit` (`@Public()`, 403 sans signature valide, dépôt inconnu ou état intermédiaire : 200, `payout` et `refund` vérifiés et journalisés seulement) ; portail `finance` étendu (`paiementEnLigne`, `tranches` avec `trancheId`, jamais l'identifiant interne de facturation), `POST /portal/children/:id/payments`, `GET /portal/payments/:id`, `GET /portal/children/:id/payments/:paymentId/receipt` ; personnel `GET /online-payments` et `POST /online-payments/:id/reconcile` (`FINANCE_READ`), `POST /online-payments/:id/resolve` (`PAYMENT_CANCEL_APPROVE`, réservée à la Direction). **Signature RFC 9421** (portée depuis Elyon, à ne pas simplifier) : `Content-Digest` SHA-512 recalculé sur le **corps brut** (d'où `rawBody: true` dans `main.ts` et `test-app.ts`) ET signature `ecdsa-p256-sha256` en DER (celle de l'exemple officiel PawaPay, pas l'encodage brut de la RFC) ; la clé publique est lue une fois par `keyid` et rechargée en cas d'absence (rotation) ; le schéma de l'adresse signée vient de `X-Forwarded-Proto` derrière Caddy. **Mise en service, à faire par le propriétaire, rien ne s'affiche aux parents sans cela** : (1) un compte PawaPay **propre à l'école** (le retour signé d'un compte est envoyé à l'adresse déclarée dans ce compte, jamais celui d'Elyon) ; (2) `PAWAPAY_API_TOKEN` et `PAWAPAY_ENVIRONMENT` (`sandbox` puis `production`) dans le `.env.production` du serveur **et** déjà déclarées dans le bloc `environment:` du service `academy-api` de `docker-compose.yml` (leçon d'Elyon : sinon elles n'atteignent pas le conteneur) ; jamais collées dans une conversation ; (3) dans le tableau de bord PawaPay : adresse de retour `https://api-academy.lobima.online/webhooks/payment/pawapay/deposit` et **« Signed Callbacks » activé** (désactivé par défaut, cause de l'échec de la première mise en service d'Elyon : les retours arrivent sans signature et sont refusés à raison) ; (4) un essai au bac à sable avec un numéro de test ; (5) l'Administrateur allume l'interrupteur. Politique de confidentialité des parents mise à jour (le numéro saisi est transmis au prestataire) et **`CONSENT_VERSION` passée à `2026-09-v5`** (aucun compte parent en production). **Écrans** : onglet Finances du portail (carte « Payer en ligne » : une ligne par tranche, formulaire montant et numéro, attente « Confirmez sur votre téléphone » avec sondage toutes les 5 s pendant 2 minutes, résultat, la ligne reste affichée après paiement), page de reçu du parent imprimable, page `/paiements-en-ligne` du personnel (filtre, « Vérifier », « Clôturer »), interrupteur dans « Établissement ». **Ne fait pas encore** : remboursement, frais à la charge du parent, montant minimum inventé, reçu par SMS, rappels, plusieurs tranches en un paiement, autre moyen que Mobile Money (D101). **Limite connue** : deux demandes simultanées sur la même tranche pourraient passer toutes deux le contrôle « une seule en attente » (aucune contrainte partielle en base) ; la revérification du solde à la confirmation protège l'argent (la seconde passerait `A_TRAITER` si le solde manque). **Tests** : 24 tests e2e (`online-payments.e2e-spec.ts`, faux fournisseur et paire de clés de test fixe, aucun appel réel), 422 tests e2e au total, `nest build` propre. **Mutations vérifiées** : sans la vérification de signature 1 test échoue, confirmation non idempotente 1, imputation sans contrôle du solde 1, plusieurs tentatives en attente par tranche 1, rattachement du responsable non contrôlé 1, numéro de reçu consommé en cas d'échec 1. **Non vérifié** : le vrai PawaPay (aucun compte de l'école n'existe encore : ni l'initiation, ni le format exact du retour, ni la lecture de `GET /deposits/{id}` n'ont été essayés contre le vrai service ; les formes utilisées viennent de l'intégration d'Elyon et de la documentation). **Écrans web non vérifiés dans un navigateur** (contrôle de type et lint propres seulement) : carte « Payer en ligne », page de reçu, page du personnel et interrupteur ; à essayer au bac à sable avant d'allumer l'interrupteur.
  **Déployé en production le 21 septembre 2026** (commit `14d4d96`, sauvegarde `/root/backups/academy-pre-lot17-20260921-212836.dump`, migration `lot17_paiement_en_ligne` appliquée au démarrage, API et web sains) : vérifié de l'extérieur, le retour PawaPay sans signature répond 403, le portail et la liste du personnel sans jeton 401, la politique de confidentialité v5 est en ligne. `paiementEnLigneActif` est faux et aucune clé PawaPay n'est configurée : **rien n'est visible des parents** tant que les étapes de mise en service ci-dessus ne sont pas faites.
  **Écrans du Lot 17 vérifiés dans un navigateur (21 septembre 2026)** sur une base jetable avec un faux fournisseur et des retours PawaPay signés avec la paire de test : attente puis confirmation par retour signé (reçu `REC-000001`, situation mise à jour, la ligne reste affichée), page de reçu, échec avec la phrase du motif, mobile 375 px sans débordement, page du personnel (liste, « Vérifier », bandeau « à traiter », pas de bouton « Clôturer » pour un Administrateur), interrupteur de « Établissement ». **Deux défauts trouvés et corrigés** : après un échec il n'y avait aucun moyen de réessayer sans recharger la page (bouton « Réessayer ») ; un numéro trop court donnait le message anglais de la validation (« telephone must be longer... ») au lieu de la phrase française du service (messages du DTO en français, le format exact est contrôlé par le service). Toujours **non vérifié** : le vrai PawaPay.
- **Points ouverts de l'audit des rôles (23 septembre 2026)** : deux choix du propriétaire (D102 et D103, `DECISIONS_PENDING.md` §21) et deux vérifications dans un navigateur.
  **Fiche complète du responsable** : nouveau droit `GUARDIAN_DETAIL_READ` (e-mail, adresse, profession). Sans lui, un dossier d'élève montre du responsable seulement le nom, le lien et le téléphone : c'est le cas du **surveillant**, qui garde le reste du dossier (identité, classe) et peut appeler un parent. `students/guardian-redaction.util.ts` (`redactStudentGuardians`) remet ces trois champs à `null` sur `GET /students/:id` et `GET /students/search` (seules routes qui renvoient des responsables à un simple lecteur ; les écritures exigent `ENROLLMENT_MANAGE`, que le surveillant n'a pas). Porté par Administrateur, Direction, secrétariat, comptable et auditeur.
  **Menu des réglages** : nouveau droit `SETTINGS_READ` pour Administrateur, Direction et Auditeur ; les liens Années scolaires, Structure académique et Établissement (`components/app-shell.tsx`) l'exigent. **Ne protège que le menu** : les routes de lecture de ces données de référence restent ouvertes à tout compte connecté (elles alimentent reçus, listes et formulaires).
  **Migration** `20260923100000_audit_roles_guardian_detail_settings_read` (idempotente, ne retire rien) : tout rôle qui lisait les dossiers reçoit `GUARDIAN_DETAIL_READ` sauf `SURVEILLANT`, et les trois rôles de supervision plus tout rôle sur mesure qui gère déjà une de ces pages reçoivent `SETTINGS_READ`. Le seed (`AUDIT_ROLES_PERMISSIONS`, `ROLES`) est aligné. Le test rejoue le SQL de la migration sur une base sans ces droits.
  **Files hors ligne vérifiées sans réseau** (API arrêtée pendant que la page est ouverte, base jetable) : les **notes** saisies hors ligne partent au retour du réseau, une seule évaluation, 4 notes exactes, une clé d'idempotence, aucun doublon ; le **cahier de textes** avait un **vrai défaut** : la préparation hors ligne ne chargeait ni `/textbook/context` ni la page pour un enseignant (compte sans `STUDENT_READ`), donc la page affichait une erreur et aucune entrée ne pouvait être saisie à froid. Corrigé dans `lib/offline-warmup.ts` (`warmTextbook`, page `/cahier-de-textes` précachée pour `TEXTBOOK_WRITE`) ; la liste des entrées n'est toujours pas copiée hors ligne, `textbook-panel.tsx` l'annonce calmement au lieu d'une erreur au-dessus d'un formulaire qui marche. Vérifié : entrée saisie hors ligne, envoyée au retour du réseau, une seule ligne en base. **Non vérifié** : le rechargement complet de la page hors ligne (le service worker de développement n'est pas fiable, voir l'entrée PWA), la perte de la seule réponse du serveur (couverte par les tests e2e de synchronisation).
  **PDF du bulletin relu visuellement** (rendu par pdf.js du fichier réellement produit) : une page A4 (en-tête, identité, tableau, moyenne générale, lettre, rang, moyenne de la classe, mention, signatures), chiffres identiques à la page et au calcul à la main (maths (17,5 + 2 x 18) / 3 = 17,83 ; générale 17,22), puis avec des appréciations très longues : texte replié dans la colonne (mot de 30 caractères sans espace compris), accents et apostrophes corrects, aucune coupure ni chevauchement. **Non vérifié** : un bulletin de plusieurs pages (beaucoup de matières) et la présence d'un logo.
  **Menu vérifié** : un enseignant ne voit plus les pages de réglages (menu réduit à ses outils), la Direction les voit. **Tests** : 9 tests e2e (`audit-roles.e2e-spec.ts`), 431 tests e2e au total. **Mutations vérifiées** : droit toujours vrai 3 échecs, recherche non réduite 1, migration qui donne la fiche au surveillant 1, migration qui oublie les rôles gestionnaires 1.
  **Déployé en production le 21 septembre 2026** (commit `10cb762`, sauvegarde `/root/backups/academy-pre-audit-roles-20260921-224948.dump`, migration appliquée au démarrage, API et web sains, aucune erreur dans les logs) : vérifié en base, `GUARDIAN_DETAIL_READ` porté par Administrateur, Auditeur, Comptable, Direction et Secrétariat (pas le surveillant), `SETTINGS_READ` par Administrateur, Auditeur et Direction. Aucun compte parent ni enseignant n'existe encore en production : l'effet sur le surveillant n'a été vérifié que par les tests.
- **Lot 18 : mise en service des parents (23 septembre 2026, vague 2)** : lancer les comptes parents classe par classe, remettre les codes et suivre l'avancement. Premier lot d'une feuille de route décidée par le propriétaire : **mise en service des parents, puis documents officiels, puis discipline, puis préinscription en ligne**. Décisions D104 à D107 dans `DECISIONS_PENDING.md` §22 ; choix du propriétaire : **lettre imprimée et WhatsApp**, **validité 30 jours réglable**, **lettre bilingue français et anglais**.
  **Contraintes de l'existant qui structurent tout** : seule l'**empreinte** d'un code est conservée, donc un code ne peut être affiché qu'à sa génération ; générer un code **invalide l'ancien**, donc régénérer en lot rendrait inutilisables des lettres déjà imprimées.
  **Backend** (`apps/api/src/parent-portal/`, migration `20260923140000_lot18_mise_en_service_parents`) : `POST /parent-accounts/bulk-codes` (`PARENT_ACCOUNT_MANAGE`, `{classId?, regenerer?, validiteJours?}`) génère les codes de tous les responsables d'un enfant à accès portail inscrit dans l'année **active** (dans la classe si donnée) **sans compte** ; **exclut** un compte existant et un code encore valable (sauf `regenerer`) ; **un code par responsable**, la lettre liste tous ses enfants inscrits cette année, quelle que soit leur classe ; plafond `BULK_CODES_MAX_FAMILIES` (500, 422 au-delà, rien n'est généré) ; toutes les générations dans **une transaction** ; un seul audit `PARENT_CODES_BULK` (classe, nombre, identifiants, échéance, **jamais un code**). `GET /parent-accounts/summary` (familles concernées, activées, désactivées, codes en attente, sans code, `sansAcces`, et le même décompte par classe) ; `GET /parent-accounts` gagne `classId` et `etat` (`SANS_COMPTE`, `CODE_EN_ATTENTE`, `ACTIF`), plafond porté à 300. `School.parentCodeValiditeJours` : défaut **30** (la migration ne change une école que si elle est restée à 7), modifiable (`UpdateSchoolDto`, 1 à 90). Le filtre par classe ne retient que les responsables avec un accès au portail : un responsable dont l'accès est retiré n'y figure pas.
  **Web** : `components/parents/onboarding-panel.tsx` (chiffres, avancement par classe, fenêtre de génération avec durée préremplie, case « inclure ceux qui ont déjà un code » avec avertissement), `components/parents/activation-letters.tsx` (fenêtre plein écran montée dans `document.body` par un portail, lettres A4 bilingues avec logo, enfants, code, échéance, étapes, QR code ; impression par CSS qui masque tout le reste de la page, une lettre par page ; un lien `wa.me` par famille avec le message prêt, **envoi manuel**, aucun lien si le numéro est inutilisable), `lib/parent-activation.ts` (numéro WhatsApp : 9 chiffres complétés par 242, international gardé, sinon aucun lien ; message bilingue ; adresse d'activation). Le QR code et le lien portent téléphone et code dans le **fragment** (`#tel=...&code=...`), que le navigateur n'envoie jamais à un serveur ; `app/parents/activer/page.tsx` le lit puis le retire de l'adresse (`history.replaceState`). Les codes n'existent que dans l'état de la page : **fermer la fenêtre les fait disparaître** (confirmation demandée). Réglage de la durée dans « Établissement » (carte « Codes d'activation des parents »).
  **Tests** : 14 tests e2e (`parent-onboarding.e2e-spec.ts` : résumé et avancement, filtres, une lettre par famille avec les enfants des deux classes, le code remis active réellement le compte et seule son empreinte est stockée, validité par défaut puis modifiée puis choisie et bornée, exclusions (compte existant, code en attente, accès retiré, non inscrit, autre année), un second clic ne génère rien et les codes restent valables, `regenerer` annule l'ancien code, plafond de 500 avec 501 familles créées, journal sans code, droits, jeton de parent refusé, migration), 445 tests e2e au total, `node scripts/check-parent-activation.mjs` côté web. **Mutations vérifiées** : code valable écrasé sans demande 2 échecs, compte existant qui reçoit un code 1, codes en clair dans le journal 1, plafond supprimé 1, classe ignorée 6.
  **Vérifié dans le navigateur** (base jetable, une école de 4 familles) : chiffres exacts (dont la famille aux trois enfants comptée dans deux classes), génération pour une classe, trois lettres bilingues (aperçu visuel), liens WhatsApp corrects (numéro complété en 242, message bilingue avec le code, `rel=noopener`, jamais de lien pour le numéro `12345`), fermeture avec confirmation, **seconde génération bloquée** tant que la case « inclure » n'est pas cochée (avec l'avertissement), lien d'activation préremplie puis **activation réelle** (le parent voit ses trois enfants), suivi mis à jour (1 activé, 25 %), filtres classe et état, mobile 375 px sans débordement, réglage de la durée. **Non vérifié** : l'impression papier réelle et le rendu du CSS d'impression (seule la fenêtre à l'écran a été vue), l'ouverture réelle de WhatsApp, un envoi à des centaines de familles.
  **Déployé en production le 21 septembre 2026** (commit `b15d435`, sauvegarde `/root/backups/academy-pre-lot18-20260921-234330.dump`, migration appliquée au démarrage, API et web sains, aucune erreur dans les logs) : la validité des codes est passée à 30 jours en base (l'école était restée à 7), `/parents/activer` répond 200, les deux nouvelles routes répondent 401 sans jeton. La production compte **5 responsables et 0 compte parent** : la mise en service peut commencer par une classe.
- **Lot 19 : documents officiels (24 septembre 2026, vague 2)** : attestation de scolarité et carte d'élève, avec numéro et QR code de vérification. Deuxième lot de la feuille de route (parents, **documents officiels**, discipline, préinscription en ligne). Décisions D108 à D115 dans `DECISIONS_PENDING.md` §23 ; choix du propriétaire : **attestation et carte avec QR**, **numéro séquentiel et QR de vérification**, **secrétariat et parent pour son enfant**, **nom du directeur, ville et signature en image**, **photo téléversée par le secrétariat**, **lieu de naissance facultatif**.
  **Backend** (`apps/api/src/documents/`, migration `20260924080000_lot19_documents_officiels`) : table `issued_documents` (type, numéro, `verificationToken` aléatoire, `snapshot` JSON figé, émetteur `STAFF` ou `PARENT`, annulation) ; `POST /students/:id/documents` (`DOCUMENT_ISSUE`, `{type, renouveler?}`) est **idempotent** : un seul document actif par élève, type et année, garanti par un verrou `FOR UPDATE` sur la ligne élève (deux demandes simultanées ne créent qu'un document), redemander renvoie le même numéro ; refus 422 sans inscription active dans l'année active, pour un élève inactif, ou (attestation) sans nom de directeur et ville. `GET /students/:id/documents`, `POST /documents/:id/annuler` (`DOCUMENT_CANCEL`, **droit réservé Direction**, motif obligatoire, 409 si déjà annulé), `POST /documents/class-cards/:classId` (cartes manquantes d'une classe, idempotent, plafond `CLASS_CARDS_MAX` 200, un audit `DOCUMENT_CARDS_BULK`), `GET /documents/verify/:token` **public** qui ne renvoie que ce qui est imprimé. Portail : `POST /portal/children/:id/attestation` (`assertChild`, `emisParType = PARENT`, audit sans utilisateur du personnel). Numéros `ATT-année-000001` / `CAR-année-000001` par `NumberSequenceService`. École : `directeurNom`, `directeurTitre` (défaut « Le Directeur »), `ville`, `signatureUrl` (`POST /school/signature`, PNG ou JPEG, 1 Mo, publique comme le logo) ; élève : `lieuNaissance` (facultatif, chaîne vide l'efface). **Photo d'élève privée** : `POST /students/:id/photo` (`ENROLLMENT_MANAGE`, JPEG/PNG/WebP, 2 Mo, extension déduite du type accepté) stockée dans `private-uploads/students` (**volume Docker `academy_private_uploads`**, jamais dans `uploads`), `GET /students/:id/photo` authentifié (`Cache-Control: private, no-store`), l'ancien fichier est supprimé au remplacement et un fichier reçu pour un élève inconnu n'est pas conservé. Droits ajoutés par la migration (idempotente) : `DOCUMENT_ISSUE` (Administrateur, Direction, secrétariat), `DOCUMENT_CANCEL` (Direction).
  **Web** : `lib/documents.ts` (types, textes accordés au sexe et sans lieu de naissance si absent, dates FR/EN), `lib/documents-pdf.ts` (PDF produits dans le navigateur avec jsPDF et `qrcode` à partir du contenu **figé** : attestation A4 avec logo, signature en image, QR et adresse de vérification ; carte au format carte bancaire, recto avec photo recadrée 3:4 ou initiales, verso avec QR, échéance et coordonnées de l'école), `components/documents/student-documents-card.tsx` (dossier élève : attestation français ou anglais, carte, historique, réimprimer, renouveler, annuler pour la Direction), `student-photo.tsx` (photo lue avec le jeton, jamais depuis une adresse publique), `class-cards-panel.tsx` (liste des élèves : cartes d'une classe en un PDF, recto et verso), `components/parents/documents-tab.tsx` (onglet Documents du portail), `app/verifier-document/[token]/page.tsx` (page publique bilingue, valide ou annulé), carte « Documents officiels » dans Établissement, champ « Lieu de naissance » au dossier et à l'inscription ; `api.blob()` lit un fichier protégé avec le jeton.
  **Tests** : 24 tests e2e (`documents.e2e-spec.ts` : refus sans directeur ou ville, contenu figé même si l'école change, idempotence et numéros sans trou, quatre demandes simultanées pour un seul document, renouvellement, refus sans inscription active ou pour un élève inactif ou inconnu, journal seulement à la création, **vérification publique sans donnée en trop** (clés exactes, aucune date ni matricule ni responsable), document annulé signalé, jeton inconnu ou numéro refusés, droits par rôle, annulation réservée à la Direction, droits posés par la migration, portail (même document que le secrétariat, autre famille 404, carte interdite, jeton du personnel refusé, messages clairs), cartes d'une classe (idempotent, 201 élèves refusés, classe hors année active), photo (hors dossier public, 401 sans jeton, mauvais format, trop lourde, remplacement, élève inconnu sans fichier, droits), signature, réglages, lieu de naissance), 469 tests e2e au total, `node scripts/check-documents.mjs` côté web. **Mutations vérifiées** : la vérification qui expose la date de naissance 1 échec, émission non idempotente 7, photo dans le dossier public 1, annulation sans droit réservé 1, portail sans contrôle de famille 1.
  **Vérifié dans le navigateur** (base jetable) : signataire, ville et signature dans Établissement ; photo téléversée depuis le dossier puis lue avec le jeton ; attestation en français et en anglais (**PDF ouverts et rendus en image**, texte accordé, signature, QR, adresse de vérification) ; carte recto et verso (photo recadrée, verso corrigé après contrôle visuel : légende du QR coupée) ; réimpression sans nouveau numéro ; historique sans bouton Annuler pour l'Administrateur ; page de vérification à 420 px (valide, puis annulée, puis jeton inconnu) ; cartes d'une classe (2 cartes, la déjà émise gardant son numéro, 4 pages) ; portail : le parent obtient une attestation (nouveau numéro après annulation de celle du secrétariat) en anglais, sans débordement à 420 px ; message clair pour un élève sans inscription. **Non vérifié** : l'impression papier ou sur carte PVC (gabarit d'imprimeur), le rendu de la photo d'un vrai élève, le scan du QR code avec un téléphone (le texte de l'adresse encodée a été contrôlé, pas le scan).
  **Déployé en production le 22 septembre 2026** (commit `fba68e9`, sauvegarde `/root/backups/academy-pre-lot19-20260922-011658.dump`, transfert des 44 fichiers du commit par archive git puis `docker compose --env-file .env.production build` et `up -d academy-api academy-web`, migration appliquée au démarrage, API et web sains, aucune erreur dans les logs) : 31 élèves et 27 paiements inchangés, `DOCUMENT_ISSUE` (Administrateur, Direction, secrétariat) et `DOCUMENT_CANCEL` (Direction) présents, volume privé `academy_private_uploads` monté et inscriptible, `/documents/verify/inconnu` répond 404, `/students/x/photo` 401 sans jeton, une photo demandée par `/uploads` 404, `/verifier-document/x` 200. **Le serveur n'est pas un dépôt git** : le code y arrive par transfert de fichiers (`git archive HEAD <fichiers du commit> | ssh ... tar -x -C /docker/shakespeare-academy`), après avoir comparé les fichiers modifiés à leur version précédente pour exclure toute dérive propre au serveur. **Avant d'émettre la première attestation en production**, renseigner dans Établissement le nom du directeur, la ville (aujourd'hui vides : toute attestation est refusée) et, si voulu, la signature. **Suites e2e** : pilotage et messagerie recevaient le délai par défaut de 5 s et le dépassaient sur une machine chargée (échec sans rapport avec ce lot, un test différent à chaque exécution) ; elles ont maintenant un délai de 30 s.
- **Lot 20 : discipline (25 septembre 2026, vague 2)** : signaler un incident ou une valorisation, décider et publier une sanction, convoquer une famille, avec un historique par élève. Troisième lot de la feuille de route (parents, documents officiels, **discipline**, préinscription en ligne). Décisions D116 à D124 dans `DECISIONS_PENDING.md` §24 ; choix du propriétaire : **incidents et sanctions, convocations et valorisations**, **l'enseignant et le surveillant signalent, la Direction décide**, **le parent voit la sanction décidée après publication**, **alerte générique**.
  **Backend** (`apps/api/src/discipline/`, migration `20260925080000_lot20_discipline`) : modèles `DisciplineType`, `SanctionType` (catalogues de l'école, aucune valeur préchargée), `DisciplineRecord` (classe et année figées, description sensible), `DisciplineRecordRevision` (historique append-only avec motif), `Sanction` (DECIDEE puis PUBLIEE puis ANNULEE), `DisciplineConvocation` ; `NotificationType.DISCIPLINE`. Quatre droits : `DISCIPLINE_REPORT` (Direction, surveillant, enseignant), `DISCIPLINE_READ` et `DISCIPLINE_CONVOKE` (Direction, surveillant), `DISCIPLINE_DECIDE` (**Direction seule, droit réservé** : décider, publier, annuler, corriger après le jour, annuler un signalement). **Portée enseignant appliquée par le serveur** : il ne signale que pour un élève inscrit (année active) dans une classe où il a une affectation (fiche enseignant active), ne relit que ses signalements et ne voit ni le message à la famille ni une sanction non publiée. Routes : `POST/GET/PATCH /discipline/records` (+ `/annuler`, `/classer`), `POST /discipline/records/:id/sanctions`, `GET /discipline/sanctions`, `POST /discipline/sanctions/:id/publier|annuler`, `POST/GET /discipline/convocations` (+ `/annuler`, `/issue`), `GET /discipline/students/:id/history` (**chaque ouverture est journalisée** `DISCIPLINE_STUDENT_READ`, sans contenu), sélecteurs `my-classes` et `classes/:id/students` (l'enseignant n'a pas `STUDENT_READ`), catalogues `types` et `sanction-types` (`PEDAGOGY_MANAGE`). **Verrouillage (RV04)** : l'auteur corrige le jour même, ensuite la Direction avec un motif et une révision conservée. **Journal** : identifiants, types, dates et motifs, **jamais** la description, le message à la famille, le lieu ni l'objet d'une convocation. Les textes libres refusent les suites de chiffres du seuil `School.messageNumeroMinChiffres` (RV09). **Portail** : `GET /portal/children/:id/discipline` est une **liste blanche** (sanctions publiées ou retirées avec type, dates et message ; convocations ; valorisations avec type et date) et `POST .../discipline/convocations/:id/accuser` (idempotent), toujours derrière `assertChild`. **Alerte** : `NotificationsService.notifyDiscipline` (texte générique, regroupement par fenêtre, ne lève jamais) à la publication d'une sanction et à l'envoi d'une convocation, jamais pour une valorisation. **Consentement** passé à `2026-09-v6` (API `parent-auth.util.ts`, page `confidentialite`, et les six suites e2e qui l'écrivaient en dur).
  **Web** : page `/discipline` (menu selon les droits ; onglets Signalements ou Mes signalements, À traiter pour la Direction, Convocations, Catalogues), `components/discipline/` (`student-picker` classe puis élève, `report-form`, `records-panel` avec décision, correction, classement sans suite et annulation, `todo-panel` sanctions à publier, `convocations-panel`, `catalogues-panel`, `student-discipline-card`), carte « Vie scolaire (discipline) » dans le dossier élève **chargée seulement à l'ouverture** (pour que le journal reflète les vraies lectures), onglet « Vie scolaire » du portail (`components/parents/discipline-tab.tsx`, bouton « J'ai pris connaissance »), type de notification dans `parents/notifications`, texte de la politique de confidentialité mis à jour (ce que voit le parent, qui lit ces données, alertes, conservation). `lib/discipline.ts` (types et libellés).
  **Tests** : 24 tests e2e (`discipline.e2e-spec.ts` : catalogues sans doublon et type désactivé refusé, droits (Administrateur, Auditeur, comptable, sans jeton), portée enseignant (autre classe 403, sans fiche ou inactif 403, sélecteurs), droits attribués par la migration et **droit réservé** (l'Administrateur ne peut ni l'accorder ni le retirer), validations (gravité, description, date future, nature, élève sans inscription, numéro de téléphone), relecture limitée de l'enseignant, verrouillage et révision motivée, classement sans suite et annulation, décision réservée à la Direction, publication et annulation, sanctions à publier, portail (avant et après publication, retirée, annulée avant publication, **liste blanche**, autre famille 404, accès retiré), valorisations sans alerte, **alerte générique** (texte exact et absence de toute mention), destinataires limités à la famille, service push en panne, convocations (validation, alerte, accusé idempotent, annulation, issue une seule fois), dossier et journal (chaque ouverture journalisée, aucun texte sensible dans le journal)), 493 tests e2e au total. **Mutations vérifiées** (treize, chacune fait échouer au moins un test) : description exposée au parent, sanction visible avant publication, enseignant hors portée, alerte détaillée, droit réservé retiré, verrouillage supprimé, lecture du dossier non journalisée, description dans le journal, enseignant qui relit tout, publication sans alerte, décision ouverte à un simple signaleur, accusé sans contrôle de famille, numéro de téléphone accepté.
  **Déployé en production le 22 septembre 2026** (commit `107e1b8`, sauvegarde `/root/backups/academy-pre-lot20-20260922-023053.dump`, transfert des 41 fichiers du commit par archive git puis `docker compose --env-file .env.production build` et `up -d academy-api academy-web`, migration appliquée au démarrage, API et web sains, aucune erreur dans les logs) : 31 élèves inchangés, les huit droits attribués exactement comme prévu (`DISCIPLINE_REPORT` à la Direction, au surveillant et à l'enseignant ; `DISCIPLINE_READ` et `DISCIPLINE_CONVOKE` à la Direction et au surveillant ; `DISCIPLINE_DECIDE` à la Direction seule), aucun catalogue préchargé (le règlement est celui de chaque école), les routes protégées répondent 401 sans jeton. **Aucun signalement n'a encore été créé en production** : les catalogues de types d'incident, de valorisation et de sanction restent à saisir par chaque école avant un premier usage réel.
- **Lot 21 : préinscription en ligne (25 septembre 2026, vague 2)** : une famille dépose une demande sans compte, le secrétariat l'examine et, en l'acceptant, la convertit en élève et inscription réels. Quatrième et dernier lot de la feuille de route (parents, documents officiels, discipline, **préinscription en ligne**). Décisions D125 à D130 dans `DECISIONS_PENDING.md` §25 ; choix du propriétaire : **demande, examen, conversion, sans frais de dossier**, **même droit que l'inscription** (`ENROLLMENT_MANAGE`, aucune permission nouvelle), **suivi par référence et téléphone, sans compte**.
  **Constat qui structure tout** : aucune route publique n'exposait la structure académique, et `StudentsService.create()`/`EnrollmentsService.create()` faisaient déjà tout le travail réutilisable (doublon D33, matricule, facture, refus d'une année clôturée RG01) — ce lot n'en réimplémente rien, il ajoute une porte d'entrée publique et une file d'examen par-dessus.
  **Backend** (`apps/api/src/pre-registrations/`, migration `20260926080000_lot21_preinscription`) : modèle `PreRegistration` (`reference` unique `PREINS-année-000123` par `NumberSequenceService`, enfant, `levelId` souhaité, responsable, message facultatif, `statut` `EN_ATTENTE`/`ACCEPTEE`/`REJETEE`, `studentId`/`enrollmentId` en simples identifiants informatifs sans relation Prisma une fois convertie). **Public** (`@Public()`) : `GET /preinscriptions/niveaux` (arbre Section→Cycle→Level, id et nom seulement, jamais un effectif ni un tarif), `POST /preinscriptions` (bloque une deuxième demande **en attente** pour le même enfant, nom/prénom/date de naissance normalisés comme D33 ; une demande déjà refusée n'empêche pas une nouvelle tentative), `GET /preinscriptions/suivi?reference=&telephone=` (les deux exigés, 404 générique sans jamais révéler lequel des deux est faux — la référence est séquentielle donc devinable, D129). **Personnel** (`ENROLLMENT_MANAGE`, réutilisé tel quel, aucune migration de droits) : liste filtrée par statut, détail, `POST :id/accepter {classId, forcerCreation?}` (charge la classe réelle choisie par le secrétariat parmi les années non clôturées — jamais par le visiteur, D128 — puis `StudentsService.create()` et `EnrollmentsService.create()` en séquence, même limite acceptée que l'assistant d'inscription du dashboard : un échec de la seconde étape laisse un élève sans inscription, rattrapable depuis son dossier), `POST :id/rejeter {motif}` (motif obligatoire). Les deux refusent si la demande n'est plus `EN_ATTENTE` (409). Audit `PREREGISTRATION_CREATE`/`ACCEPT`/`REJECT` : identifiants et types seulement, jamais le message libre de la famille.
  **Web** : `/preinscription` (formulaire public, enfant + niveau en cascade + responsable + message facultatif, référence affichée en évidence à la soumission), `/preinscription/suivi` (référence + téléphone → carte de statut, motif si refusée), `app/(dashboard)/preinscriptions/page.tsx` (onglets par statut, nouveau lien de menu `ENROLLMENT_MANAGE`, `ClassPicker` — cascade Année→Section→Cycle→Niveau→Classe restreinte aux années non clôturées, préremplie avec le niveau demandé — et reprise du doublon D33 en cas de 409 avec confirmation `forcerCreation`, même patron que `NewStudentForm`).
  **Tests** : 13 tests e2e (`pre-registrations.e2e-spec.ts` : arbre public sans donnée sensible, validation (date future, niveau inconnu), doublon de demande en attente bloqué puis autorisé après refus, suivi exige les deux paramètres sans révéler lequel est faux, journal sans le message libre, droits (Direction, comptable, surveillant, enseignant refusés sur liste, détail, refuser **et accepter**, 401 sans jeton), acceptation réelle (élève et inscription créés, matricule et facture générés, année active correctement choisie), doublon D33 puis `forcerCreation`, classe d'une année clôturée refusée et classe inconnue 404, refus exige un motif, ni accepter ni refuser deux fois, inventaire des droits (aucune permission `PREINSCRIPTION_*`, `ENROLLMENT_MANAGE` inchangé)), 506 tests e2e au total. **Défaut de couverture trouvé et corrigé en le construisant** : la première version du test de droits ne vérifiait jamais `accepter` pour un rôle non privilégié — une mutation qui retirait `@RequirePermission` de cette seule route ne faisait échouer aucun test ; corrigé en ajoutant les 403 et la vérification que le statut reste `EN_ATTENTE` après. **Mutations vérifiées** (six, chacune fait échouer au moins un test) : doublon en attente non bloqué, suivi sans vérifier le téléphone, accepter ou refuser sans revérifier le statut, décision ouverte à tout le personnel (après le correctif ci-dessus), niveau non vérifié contre l'école.
  **Vérifié dans le navigateur** (base jetable) : dépôt d'une demande (référence `PREINS-2026-000001`), suivi refusé avec un mauvais téléphone (message générique) puis accepté avec le bon (« En attente »), acceptation par le secrétariat avec la cascade préremplie sur le niveau demandé (année active, section, cycle, niveau) et une classe choisie, élève réel visible dans « Élèves » avec le bon matricule, la bonne classe et le bon responsable ; deuxième demande pour le même enfant : doublon D33 détecté à l'acceptation, confirmé avec « Confirmer malgré le doublon » ; troisième demande refusée avec un motif (`window.prompt` du navigateur, patché en JavaScript pour le test automatisé), motif visible ensuite sur la page de suivi ; mobile 375 px sans débordement sur les deux pages publiques.
  **Déployé en production le 22 septembre 2026** (commit `d39ca17`, sauvegarde `/root/backups/academy-pre-lot21-20260922-022836.dump` vérifiée lisible avant transfert, dérive des fichiers déjà présents sur le VPS écartée par comparaison d'empreinte insensible aux fins de ligne avant tout transfert, 18 fichiers du commit transférés par archive git puis `docker compose --env-file .env.production build` et `up -d academy-api academy-web`, migration `20260926080000_lot21_preinscription` appliquée au démarrage, API et web sains, aucune erreur dans les logs) : 31 élèves inchangés, table `pre_registrations` vide, `ENROLLMENT_MANAGE` toujours porté par Administrateur et Secrétaire-caissier seulement, aucune permission `PREINSCRIPTION_*` créée. Vérifié depuis l'extérieur en HTTPS : `GET /preinscriptions/niveaux` 200, `/preinscriptions/suivi` sans paramètres 400, avec une référence et un téléphone inconnus 404 (jamais 500), `GET /preinscriptions` sans jeton 401, `/preinscription` et `/preinscription/suivi` 200 côté web. Conteneurs Elyon (autre projet du même VPS) non touchés, `app.lobima.online` toujours 200. **Aucune donnée réelle n'a été utilisée pour vérifier ce lot.**
- **Copyright de l'éditeur, page « Liens utiles », guide d'utilisation public (22 septembre 2026)** : demande explicite — mention légale INFRAONE SYSTEMS dans l'application, un emplacement listant les adresses de chaque acteur (parents, enseignants, personnel) avec description, copie du lien et partage WhatsApp, et un guide d'utilisation. `components/copyright-footer.tsx` (nouveau, partagé) affiche « © INFRAONE SYSTEMS {année}. Tous droits réservés. » — distinct du nom de l'établissement (Shakespeare Academy), qui restait déjà affiché ailleurs (panneau de marque de `/login`) ; année calculée, jamais figée en dur. Posé dans `AppShell` (donc sur toutes les pages du personnel), `/login`, l'espace parents (`app/parents/layout.tsx`), et les quatre pages publiques (`/preinscription`, `/preinscription/suivi`, `/verifier-recu/[token]`, `/verifier-document/[token]`).
  **`/liens-utiles`** (nouvelle page du personnel, `app/(dashboard)/liens-utiles/`) : quatre adresses réellement distinctes dans l'application — Personnel de l'école (`/login`, un seul lien pour Direction/secrétariat/comptabilité/surveillance **et** enseignants, qui partagent la même page de connexion et n'y voient ensuite que les écrans propres à leur permission : inventer une URL par rôle aurait été trompeur, il n'y en a qu'une), Espace parents (`/parents/connexion`), Préinscription en ligne (`/preinscription`), Guide d'utilisation (`/guide`). Chaque carte affiche l'adresse complète (calculée depuis `window.location.origin`, jamais un domaine codé en dur — fonctionne aussi bien en local qu'en production), un bouton « Copier le lien » (`navigator.clipboard`, même patron que la page Comptes parents — un échec du presse-papier reste silencieux, l'adresse affichée reste sélectionnable à la main) et un lien `wa.me/?text=...` (titre + description + lien, envoi manuel comme tous les autres partages WhatsApp de ce projet). **Aucune permission requise** : la page ne contient que des adresses publiques et un texte d'aide, jamais une donnée sensible — même principe d'ouverture que le Tableau de bord ou la page Synchronisation, déjà accessibles sans permission particulière.
  **`/guide`** (nouvelle page **publique**, hors du groupe `(dashboard)`, aucun compte requis — même principe que `/preinscription`) : sélecteur de rôle (Direction, Secrétariat & comptabilité, Vie scolaire/surveillance, Enseignant, Parent), chaque rôle avec des étapes numérotées reprenant le vocabulaire exact des écrans réels (mêmes libellés que `components/app-shell.tsx` et la nav de l'espace parents), pour qu'un lecteur reconnaisse immédiatement l'écran décrit. Contenu volontairement condensé (l'application compte 21 lots) : les workflows principaux de chaque acteur, pas une documentation exhaustive de chaque écran. Liens vers `/login` et `/parents/connexion` en pied de page, réutilisable directement comme adresse à partager (listée elle-même dans « Liens utiles »).
  **Revue des droits demandée par l'utilisateur après ces ajouts** : audit complet effectué (catalogue de permissions `apps/api/prisma/seed-data.ts`, chaque décorateur `@RequirePermission`/`@RequireAnyPermission` de l'API, la liste des droits réservés `auth/reserved-permissions.ts`, et les affectations par rôle) comparé point par point aux décisions déjà documentées dans ce fichier (Lots 15/16/19/20, durcissement du 22 septembre, audit des rôles du 23 septembre) — **aucune dérive trouvée**, chaque permission existante est portée exactement par les rôles annoncés, `SETTINGS_READ` confirmé comme un garde-fou d'affichage de menu uniquement (jamais vérifié côté serveur, par conception déjà documentée), les droits réservés à la Direction inchangés et à jour (y compris `GRADE_CORRECT`/`BULLETIN_VALIDATE`/`DOCUMENT_CANCEL`/`DISCIPLINE_DECIDE` des lots récents). Les deux nouvelles pages ne modifient ce système en rien : `/liens-utiles` est ouverte à tout compte authentifié (comme `/` et `/hors-ligne`), `/guide` est entièrement publique.
  `npm run build` et `npm run lint` propres sur `apps/web` ; `npx nest build` propre sur `apps/api` (aucun fichier backend modifié par ce lot, vérifié par prudence avant tout déploiement, conformément à la leçon du 22 septembre sur le cache incrémental de `tsc`). Vérifié manuellement de bout en bout dans le navigateur (base de dev locale, API et web relancés sur des ports dédiés pour cette vérification) : guide public accessible sans connexion, sélecteur de rôle fonctionnel (contenu et étapes vérifiés pour Direction et Parent), copyright affiché sur `/guide` et `/login` ; connexion réelle en tant qu'Administrateur puis `/liens-utiles` affichant les quatre cartes avec la bonne adresse (dérivée de l'origine réelle du navigateur), les quatre liens `wa.me` contrôlés directement dans le DOM (texte et adresse corrects), le pied de page de l'espace personnel affichant le copyright ; mobile 375 px sans débordement horizontal sur `/guide`, `/liens-utiles` et `/login`. **Piège d'environnement rencontré et contourné, propre à cette session d'outillage, pas au code** : le port 3000 par défaut renvoyait, via l'outil de navigateur, le contenu d'un tout autre projet (Elyon) resté ouvert sur ce port dans une session antérieure — un `curl` direct confirmait pourtant que le vrai serveur Shakespeare Academy y répondait correctement, isolant la cause au routage interne de l'outil de prévisualisation plutôt qu'au code ; contourné en faisant tourner l'API et le web sur des ports dédiés (3001/3020, `WEB_ORIGIN` de l'API ajusté en conséquence), déjà le même type de contournement documenté plus haut dans ce fichier pour un souci de port similaire.
- **Date de naissance et responsable légal facultatifs à l'enregistrement (22 septembre 2026)** : demande explicite — « pour faciliter les enregistrements » — de ne plus exiger la date de naissance de l'élève ni aucune information du responsable légal à la création, complétables plus tard via les corrections déjà existantes (`PATCH /students/:id`, `PATCH /students/guardians/:id`). **Portée volontairement limitée au personnel de l'école** (`POST /students`, `POST /students/:id/guardians`, dossier élève) — le formulaire public de préinscription (`/preinscription`) garde ses champs obligatoires : D129 (cadrage §25) s'appuie explicitement sur le téléphone du responsable comme second facteur anti-énumération pour le suivi d'une demande (`GET /preinscriptions/suivi`), l'affaiblir y aurait été une régression de sécurité, jamais demandée.
  **Schéma** (migration `20260926090000_make_student_guardian_fields_optional`, purement additive — `DROP NOT NULL` sur trois colonnes, aucune perte de données) : `Student.dateNaissance` devient `DateTime?`, `Guardian.nom`/`prenom`/`telephone` et `StudentGuardian.lien` deviennent facultatifs. `Guardian.telephone` reste unique par établissement (`@@unique([schoolId, telephone])`) : Postgres traite chaque `NULL` comme distinct dans un index unique, donc plusieurs responsables sans numéro coexistent sans jamais entrer en conflit.
  **`CreateStudentDto.dateNaissance`/`CreateGuardianDto.{nom,prenom,telephone,lien}`** : `@IsOptional()` ajouté, la validation de format reste appliquée dès qu'une valeur est fournie (`@IsDateString()`/`@MinLength(1)`) — un champ doit être **omis**, jamais envoyé en chaîne vide (qui reste refusée en 400, testé explicitement). `StudentsService.create()` : sans date de naissance, la détection de doublon D33 est **sautée entièrement**, jamais remplacée par un rapprochement sur le seul nom (trop de faux positifs dans une école — deux « Grace Moukala » sans date connue ne sont jamais signalées entre elles). `upsertOrCreateGuardian()` (nouvelle fonction partagée, utilisée par `create()` **et** `attachGuardian()`, jamais dupliquée) : le rapprochement R9-like par téléphone (`guardian.upsert`) n'a lieu que si un téléphone est fourni ; sans lui, toujours `guardian.create()` — un rapprochement sur le seul nom aurait pu confondre deux responsables homonymes réellement différents. `StudentsService.search()` corrigé au passage (`sg.guardian.telephone?.includes(query)`, un responsable sans numéro n'y plantait jamais avant mais aurait dû : trouvé en construisant, pas un bug préexistant signalé par l'utilisateur).
  **Document officiel** (`DocumentsService.issueForEnrollment`) : une attestation ou une carte élève refuse désormais explicitement (422, avant même la garde existante sur le directeur/la ville) tant que la date de naissance manque — jamais un document officiel imprimé avec un blanc à la place d'une information légale, même garde-fou que celle déjà en place pour le nom du directeur (cadrage Lot 19). Un bulletin (`BulletinsService`), lui, s'imprime quand même, simplement sans cette ligne (`dateNaissance: null`) — moins formel, bien plus fréquent, un blocage y aurait été disproportionné.
  **Portail parent** : `samePhone()` (`parent-auth.util.ts`) refuse tout de suite un responsable sans téléphone (`if (!a) return false`) — un tel responsable ne peut jamais s'authentifier, cohérent avec le fait que l'activation se fait par numéro. `ParentAccountsService.generateCode()` (génération d'un code pour UN responsable) refuse désormais explicitement (422) sans téléphone — un code généré aurait été définitivement inutilisable, jamais un piège silencieux pour le secrétariat. La génération **en lot** (Lot 18) exclut de la même façon les responsables sans téléphone, comptés dans une nouvelle catégorie `sansTelephone` au même titre que `avecCompte`/`codeEnAttente`/`sansAcces` (déjà existantes) — visible côté écran de mise en service, pour que la Direction comprenne pourquoi certaines familles restent bloquées.
  **Web** : `lib/types.ts` (`Student.dateNaissance`, `Guardian.nom/prenom/telephone`, `StudentGuardianLink.lien` tous `| null`), `lib/format.ts::formatDate()` accepte désormais `null`/`undefined` et renvoie « Non renseignée » plutôt que de calculer silencieusement le 1er janvier 1970 (`new Date(null)`) — piège trouvé en propageant les types, corrigé une seule fois ici plutôt que dans chacun des six écrans qui l'utilisaient déjà. Formulaires (`eleves/inscription`, dossier élève, rattachement d'un responsable) : `required` retiré des champs concernés, un champ vide est omis du corps de la requête plutôt qu'envoyé en chaîne vide (qui aurait été rejetée en 400) ; le responsable entier est omis si aucun de ses champs n'est rempli. Les libellés précisent désormais « facultatif, à compléter plus tard si inconnu ». Affichage d'un responsable incomplet : jamais littéralement « null » (repli « Responsable sans nom », « lien non précisé », « téléphone non renseigné »).
  **Tests** : 9 tests e2e nets ajoutés (`students.e2e-spec.ts` : création sans date ni responsable, deux homonymes sans date jamais signalés doublon, chaîne vide toujours refusée, responsable partiel, deux responsables sans téléphone restent deux `Guardian` distincts, `attachGuardian` sans téléphone ne rapproche jamais, recherche ne plante pas ; `documents.e2e-spec.ts` : attestation **et** carte refusées sans date de naissance, acceptées une fois complétée ; `parent-portal.e2e-spec.ts` : génération de code refusée sans téléphone ; `parent-onboarding.e2e-spec.ts` : génération en lot exclut et compte `sansTelephone`) — 515 tests e2e passants au total (+9), 35 tests unitaires inchangés. `npx nest build`/`tsc --noEmit --incremental false` et `npx tsc --noEmit`/`npm run build` côté web propres, lint propre des deux côtés.
  **Deux suites e2e sans rapport avec ce lot corrigées au passage** (`academic-years.e2e-spec.ts`, `roles.e2e-spec.ts`) : `jest.setTimeout(30000)` ajouté, même correctif déjà appliqué à `pilotage`/`messaging` — un `beforeEach` (nettoyage + seed + connexion) dépassait parfois les 5 s par défaut de Jest sur une exécution de la suite complète enchaînant les 32 suites, confirmé par un ré-essai isolé de chacune (6/6 et 14/14 vertes seules) : une vraie machine chargée, pas une régression de ce lot.
  **Vérifié manuellement dans le navigateur** (base de dev locale, API et web relancés sur des ports dédiés — même piège de résidu Elyon que la vérification précédente, cette fois sur le port 3001, un deuxième processus Elyon distinct arrêté) : formulaire « Nouvel élève » avec les champs date de naissance et responsable confirmés non obligatoires (`required: false` sur les cinq champs) ; élève créé avec seuls nom/prénom → `dateNaissance: null` confirmé côté API, dossier affichant « né(e) le Non renseignée » (jamais une date fausse) ; responsable rattaché avec seul le nom rempli → `prenom`/`telephone`/`lien` tous `null` en base, affiché « ResponsablePartiel — lien non précisé · téléphone non renseigné ». Élève et responsable de test supprimés de la base de dev locale après vérification (suppression Prisma directe, hors API : RG18 interdit la suppression physique via l'application, sans rapport avec l'hygiène d'une base de développement).
- **Vue 360° du dossier élève (22 septembre 2026)** : demande explicite après la clôture de la feuille de route « parents, documents, discipline, préinscription » — « avoir une fiche de synthèse d'un élève avec vue 360° ». Deux décisions actées avec l'utilisateur avant de coder : format **web ET PDF** (pas l'un ou l'autre), et le seul domaine manquant à ajouter au dossier existant est **les notes et bulletins** (pas l'emploi du temps de la classe, pas le cahier de textes — restent hors périmètre).
  **Backend** : `BulletinsService.forStudent(studentId)` (nouveau, `bulletins.service.ts`) — tous les bulletins d'un élève quel que soit leur statut (VALIDE ou PUBLIE ; un `Bulletin` n'existe qu'à partir de la validation), contrairement à `publishedForStudent` (portail parent) qui ne montre que les publiés : le personnel voit l'avancement d'un trimestre encore non publié, jamais une famille. `GET /students/:id/bulletins` (`students.controller.ts`, `GRADE_READ` — Direction et Administrateur seulement, comme le reste des notes) délègue à cette méthode ; `StudentsModule` importe désormais `GradesModule` (qui exporte déjà `BulletinsService`) plutôt que de réinstancier le service comme `FinancialStatusService` — plus propre, aucun risque de dépendance circulaire vérifié (aucun module de la chaîne `GradesModule` → `AuditModule`/`SchoolModule`/`NotificationsModule` n'importe `StudentsModule`).
  **Web** : `StudentGradesCard` (`components/notes/student-grades-card.tsx`, même patron que `AttendanceHistoryCard` — auto-chargée, pliable) ajoutée au dossier élève entre Documents et Parcours annuel, gardée par `hasPermission("GRADE_READ")` ; chaque ligne pointe vers la page d'impression déjà existante (`/bulletins/[id]`), jamais un second visualiseur dupliqué. `StudentSummaryStrip` (`components/student-summary-strip.tsx`) : un bandeau de trois cartes (`StatCard`, déjà utilisé au tableau de bord) en haut du dossier — moyenne actuelle (dernier bulletin), assiduité (taux de présence calculé depuis les compteurs déjà exposés par `GET /attendance/students/:id/history`), situation financière (réutilise le badge `STATUS_META` de `FinancialStatusCard`, désormais exporté). **La discipline en est délibérément absente** : `StudentDisciplineCard` ne charge son contenu qu'à l'ouverture explicite de la carte, car chaque lecture du dossier de vie scolaire d'un élève est journalisée (cadrage Lot 20) — l'inclure dans un bandeau affiché à chaque simple consultation du dossier aurait forcé cette lecture (et sa trace) en silence, jamais voulu. Chaque indicateur reste absent (jamais un zéro ou un badge inventé) si la permission manque ou si la donnée n'existe pas encore.
  **PDF** : `lib/student-summary-pdf.ts::downloadStudentSummaryPdf()`, même patron que `lib/bulletin-pdf.ts` (jsPDF + jspdf-autotable, générée entièrement dans le navigateur) — identité, responsables, situation financière, assiduité, notes et bulletins, sur une page. **Discipline exclue de la même façon que du bandeau**, pour la même raison. Le bouton « Fiche de synthèse (PDF) » (en-tête du dossier) refait systématiquement les quatre appels (établissement, situation financière, assiduité, bulletins) au moment du clic plutôt que de réutiliser ce que le bandeau a déjà chargé — délibéré : la fiche exportée doit toujours refléter l'instant de sa génération, jamais un état resté affiché depuis un moment.
  **Tests** : 3 tests e2e ajoutés dans `grades.e2e-spec.ts` (vide avant toute validation ; VALIDE visible au personnel mais invisible au parent tant que non publié, PUBLIE visible aux deux ; droits — 403 sans `GRADE_READ`, Direction et Administrateur seuls ; liste vide pour un élève sans aucun bulletin, jamais une erreur), 519 tests e2e passants au total (+3 nets, malgré la fermeture de deux suites sans rapport détaillée dans l'entrée précédente). `nest build`/`tsc --noEmit` et `npx tsc --noEmit`/`npm run build`/lint côté web tous propres.
  **Vérifié manuellement dans le navigateur** (base de dev locale, API et web relancés sur des ports dédiés) avec un vrai élève de production locale (Ruth Bakala, `000008`, une vraie facture en retard de 40 000 FCFA) : bandeau de synthèse affichant correctement « Aucun bulletin pour l'instant » (pas de fausse moyenne), « 0 absence(s), 0 retard(s) sur 0 séance(s) » avec un ton neutre plutôt qu'un vert de succès injustifié (corrigé en le vérifiant : le ton par défaut était « success » même sans aucune donnée), et la vraie situation financière « En retard, 40 000 restant dû » ; carte « Notes et bulletins » affichée correctement vide ; bouton « Fiche de synthèse (PDF) » cliqué avec succès (vrai PDF de 7 122 octets généré, capturé et vérifié sans déclencher le téléchargement réel), aucune erreur console liée à cette fonctionnalité (quatre appels réseau en parallèle tous 200). **Le scénario avec un vrai bulletin publié n'a pas été revérifié visuellement dans le navigateur** (aurait exigé de reconstruire tout le parcours notes — évaluation, saisie, validation — pour un seul contrôle visuel) : ce cas précis est déjà prouvé exactement par les tests e2e (moyenne 13,11, rang 3 sur 4, distinction VALIDE/PUBLIE), jugé suffisant. Élève et données réels de production locale non touchés, aucune donnée de test créée pour cette vérification.
- **Correctif : bandeau de synthèse comprimé sur tablette (22 septembre 2026)** — trouvé en vérifiant explicitement le rendu tablette (768 px) à la demande de l'utilisateur, après les vérifications mobile et mode sombre. `StudentSummaryStrip` utilisait `sm:grid-cols-3` (640 px) : à 768 px, la barre latérale du tableau de bord (256 px, toujours visible à partir de `md`) grignote l'espace réel, comprimant les trois cartes à 139 px chacune — texte qui se scinde sur deux lignes (« Situation financière », « En retard »), jamais de débordement ni de troncature, mais peu lisible. Corrigé en alignant le seuil sur `lg:grid-cols-3` (1024 px), exactement celui déjà utilisé par la grille des cartes détaillées plus bas sur cette même page, pour la même raison. Revérifié : tablette (768 px) — une carte par ligne, pleine largeur, texte sur une seule ligne ; desktop (1280 px) — trois colonnes à 310 px chacune, inchangé. `tsc`/lint/build propres des deux côtés (changement de classe CSS uniquement, aucun test e2e concerné).
- **Reverse proxy commun, sortie du réseau d'Elyon (22 septembre 2026, préparé, pas encore basculé sur le VPS)** : le VPS accueille un troisième projet (Maatri) ; le Caddy d'Elyon est remplacé par une pile commune séparée, `lobima-infra` (`D:7lobima-infra`, son README contient la procédure de bascule et de retour arrière). Dans `docker-compose.yml`, le réseau `proxy` pointe désormais vers le réseau partagé `proxy` (plus `elyon-restaurant-mvp_default`) ; les noms `academy-api`/`academy-web` restent uniques et inchangés, aucun port publié. Les deux blocs `academy`/`api-academy.lobima.online`, jusqu'ici ajoutés à la main dans le `Caddyfile` d'Elyon sur le VPS, vivent maintenant dans celui de `lobima-infra`, domaines en variables (`ACADEMY_WEB_DOMAIN`/`ACADEMY_API_DOMAIN`) pour faciliter un futur changement de nom de domaine. Configuration validée (`docker compose config`) ; bascule réelle à faire par l'utilisateur (environ une minute de coupure pour Shakespeare). ⚠ Le fichier `Identifiant  admin@shakespeareacade.txt` à la racine n'est pas ignoré par git : ne jamais faire `git add -A` sans l'exclure.
- **Correctif d'ergonomie : bouton « Ajouter » remonté au-dessus des listes de saisie (23 septembre 2026, signalé par l'utilisateur)** : sur l'onglet Matières de Vie scolaire, le bouton « Ajouter la matière » restait après la liste des matières déjà créées — avec plusieurs matières, il fallait défiler jusqu'en bas pour l'atteindre à chaque ajout, alors que l'écran sert justement à en saisir plusieurs à la suite. Plutôt qu'ajouter un second bouton dupliqué en haut (état à synchroniser, risque d'incohérence), le formulaire entier est remonté **avant** la liste, juste après l'introduction de la carte — la liste s'allonge alors sous le formulaire, jamais entre lui et le haut de l'écran. Vérification demandée explicitement sur les autres formulaires de saisie du même genre : sept écrans suivaient exactement le même schéma (liste d'abord, formulaire d'ajout après, jamais revu depuis leur construction aux Lots 7 et 3) — `components/vie-scolaire/matieres-tab.tsx` (matières), `salles-tab.tsx` (salles), `enseignants-tab.tsx` (enseignants), `horaires-tab.tsx` (créneaux horaires — le formulaire pré-remplit l'heure de début avec la fin du créneau précédent, pensé pour une saisie en rafale, donc le cas le plus concerné), `calendrier-tab.tsx` (trimestres **et** vacances/jours fériés, deux formulaires distincts sur la même page), et `app/(dashboard)/tarifs/page.tsx::FeeTypesPanel` (types de frais) — tous corrigés à l'identique. `components/discipline/catalogues-panel.tsx` avait déjà le bon ordre (non touché) ; `components/notes/settings-tab.tsx` (tranches de lettres A à F, au plus 6 lignes) et le formulaire dédié « Configurer un tarif » des grilles tarifaires (carte séparée, jamais précédée d'une longue liste) ne présentaient pas le problème, laissés tels quels. Les messages d'état vide (« Ajoutez la première matière ci-dessous ») sont corrigés en conséquence (« ci-dessus »). Purement présentationnel, aucune route ni logique métier touchée. `tsc --noEmit`, lint et `npm run build` propres sur `apps/web`. **Vérifié dans le navigateur** (base de dev locale) : 8 matières créées via l'API pour reproduire une liste longue — le formulaire et son bouton restent immédiatement visibles en haut de la carte, sans défiler ; même contrôle visuel sur Enseignants, Salles, Horaires (créneaux), Calendrier (trimestres et vacances/fériés) et Tarifs (types de frais), tous corrects ; données de test supprimées de la base locale après vérification.
  **Déployé en production le 23 septembre 2026** (commit `9a72d43`, aucune migration : les 7 fichiers `apps/web` comparés à leur version précédente sur le VPS avant transfert pour écarter toute dérive, transférés par archive git, `docker compose --env-file .env.production build academy-web` puis `up -d academy-web` — API non touchée, aucun rebuild nécessaire) : conteneur `academy-web` sain sans erreur au démarrage, `/vie-scolaire` et `/tarifs` répondent 200, `/school` de l'API répond 401 sans jeton (comportement normal), conteneurs Elyon non touchés (tous `healthy`/`Up`, `app.lobima.online` toujours 200).
- **Suppression exceptionnelle de 8 élèves de test en production, en base directement (23 septembre 2026, demande explicite de l'utilisateur)** : Mylia Heaven MBEYE (000030), Kimia Sublime Sneell BOLENA (000031), Ravie MBEYE (000032), Luena Yvana BAMBI (000033), Auréa Audrey LEBENG NTSAMA (000034), Kyliam Brayann MBEMBA MOUNZENZE (000036) — les 6 élèves alors listés dans « Élèves insolvables » — ainsi que Saint Patrick LEBENG (000002) et Steve Emmanuel DJANI TABI (000004), tous confirmés par l'utilisateur comme des données de test créées pendant la configuration de l'école, jamais de vrais élèves. **Contredit délibérément RG18** (aucune suppression physique d'élève, inscription, facture ou transaction en production) : l'application n'offre et n'offrira jamais de bouton « Supprimer un élève » pour cette raison précise — cette suppression n'a été possible qu'en écrivant directement dans la base, jamais via l'application, dont le garde-fou reste intact pour l'avenir (même principe que l'approbation des 51 remises lors de l'import initial, §"Import initial de 25 élèves" plus haut).
  **Vérifié avant suppression** (dossier de chacun des 8, dans le navigateur) : Saint Patrick LEBENG et Steve Emmanuel DJANI TABI avaient chacun un **vrai paiement validé avec un numéro de reçu séquentiel** (REC-000002, REC-000003, 45 000 FCFA chacun, encaissé en espèces le 17 septembre 2026) — signalé explicitement à l'utilisateur avant d'agir, qui a confirmé vouloir les supprimer malgré le trou définitif que cela crée dans la numérotation (RG09 : « une transaction validée est immuable » ; RG10 : « les numéros de reçu sont uniques, séquentiels »). Les 6 autres n'avaient aucun paiement (0 FCFA payé), une seule remise en attente (« Fratrie », jamais approuvée) sur Auréa Audrey LEBENG NTSAMA, et aucune donnée d'assiduité, de notes, de documents officiels ni de discipline.
  **Exécution** : sauvegarde complète de la base (`pg_dump -F c`) sur le VPS immédiatement avant toute écriture. Suppression en une seule transaction SQL (`BEGIN`/`COMMIT`), dans l'ordre exigé par les contraintes de clés étrangères du schéma (aucune de ces relations n'a de `onDelete: Cascade` vers `Student`, par conception, précisément pour qu'une suppression ne puisse jamais être un simple geste involontaire) : paiements et remises de chaque ligne de facture d'abord (2 paiements, 1 remise), puis les factures (8, dont les 32 lignes disparaissent automatiquement par cascade), puis les inscriptions (8), puis les liens élève-responsable (7 — Kyliam n'avait aucun responsable enregistré), puis les élèves eux-mêmes (8), puis les responsables devenus orphelins (6, aucun n'était partagé avec un élève réel restant — vérifié par une clause `NOT EXISTS` avant suppression, jamais par une simple absence de vérification). Comptages confirmés après coup : 26 élèves restants (34 − 8), séquences `MATRICULE` (36) et `RECEIPT` (29) inchangées — REC-000002/REC-000003 et les matricules 000030/000031/000032/000033/000034/000036 restent définitivement vacants, jamais réutilisés, exactement comme RG10 l'exige pour tout ce qui a existé.
  **Contrainte d'outillage rencontrée** : l'environnement d'exécution de l'assistant bloque par défaut toute commande shell reconnue comme touchant à une suppression de données de production, y compris une requête de lecture seule pourtant anodine (comptage sans aucune donnée personnelle) — un garde-fou de la session d'outillage, pas une règle du projet. Contourné en obtenant l'autorisation explicite de l'utilisateur pour ce type d'action précis ; la sauvegarde (écriture seule, sans lecture de contenu) n'était elle-même jamais bloquée.
  **Vérifié dans le navigateur après coup** : `/insolvables` affiche « Aucun élève insolvable. Tous les frais exigibles sont couverts. » ; la recherche de « Lebeng » et « Djani » dans `/eleves` renvoie toutes deux « Aucun élève ne correspond à cette recherche. ». Aucune autre donnée de production touchée, conteneurs non redémarrés (seule la base a été modifiée, jamais le code).
  **Point resté ouvert** : l'utilisateur avait aussi signalé l'absence de tout moyen de supprimer un élève jamais inscrit — aucun des 8 élèves ci-dessus n'était dans ce cas (tous avaient une inscription active), donc il s'agit d'un besoin distinct, pas couvert par cette suppression. À clarifier avec l'utilisateur (quel élève précisément) avant d'envisager une éventuelle fonctionnalité limitée aux élèves sans aucune inscription/facture/paiement — ce qui resterait sans effet sur RG18 en pratique, mais est une décision produit à documenter (`DECISIONS_PENDING.md`) avant construction, jamais supposée.
- **Aide contextuelle, lot pilote (23 septembre 2026, demande explicite : « tout utilisateur doit utiliser aisément sans être formé »)** : trois décisions actées avec l'utilisateur avant de coder (icône « ? » cliquable plutôt qu'un bandeau permanent ou masquable ; une nouvelle page `/aide` authentifiée plutôt qu'enrichir `/guide`, publique et généraliste ; un lot pilote de 5 écrans avant un déploiement sur les 24 pages restantes).
  **Architecture** : `lib/help-content.ts` — source unique de vérité (identifiant, titre, 2 à 5 tips courts et actionnables, `permission`/`anyPermission` repris tels quels des codes déjà utilisés par la navigation, `components/app-shell.tsx`) — jamais deux textes à maintenir en double entre la puce d'aide et la page `/aide`. `components/help-tip.tsx` (`HelpTip`) : icône discrète, jamais un bandeau qui s'ajoute à demeure sur 29 écrans ; s'ouvre au clic, se ferme au clic extérieur, au défilement ou au redimensionnement ; chaque panneau se termine par « Voir le guide complet » vers `/aide#<id>`. `PageTitle` (`components/ui.tsx`) accepte une nouvelle prop `helpId` optionnelle, qui affiche la puce à côté du titre — une seule ligne à ajouter par page.
  **`/aide`** (nouvelle page authentifiée, lien ajouté à la navigation) : distincte de `/guide` (restée publique, généraliste, pour présenter l'application à l'extérieur) — ici, seules les rubriques auxquelles le compte connecté a réellement accès apparaissent (mêmes permissions que le menu, jamais une nouvelle règle de visibilité), chacune avec une ancre (`id={entry.id}`, `scroll-mt-6`) pour le lien direct depuis une puce.
  **5 écrans du pilote câblés** : Élèves, Inscription/réinscription, Tarifs & facturation, Vie scolaire (via `helpId` sur `PageTitle`), et la carte « Situation financière » du dossier élève — placée spécifiquement sur les paiements (demande explicite), affichée seulement carte ouverte pour rester pertinente au contexte visible.
  **Bug réel trouvé et corrigé en vérifiant** : la première version positionnait le panneau en `absolute` relatif à l'icône ; sur la carte « Situation financière », dont le titre se trouve loin à droite de l'écran, le panneau débordait hors du viewport dès que la fenêtre n'était pas très large — repéré en vérifiant à 1024 px, pas seulement en plein écran. Corrigé en calculant la position en `fixed` à l'ouverture (mesure du bouton via `getBoundingClientRect`, bornée à 16 px des bords), revérifié à 1024 px et 375 px, panneau entièrement visible dans les deux cas. Un deuxième défaut lié a été trouvé et corrigé au même moment : les écouteurs de fermeture (défilement, redimensionnement) posés immédiatement à l'ouverture se déclenchaient parfois sur un défilement encore en cours (ex. `scrollIntoView` amenant le bouton à l'écran), refermant le panneau aussitôt affiché ; posés désormais après un court délai (150 ms).
  `tsc --noEmit`, lint et `npm run build` propres. **Vérifié dans le navigateur** (connexion réelle) : icône cliquée sur les 5 écrans, contenu correct, lien « Voir le guide complet » menant à la bonne ancre de `/aide` avec défilement automatique, panneau correctement positionné après les deux correctifs (testé à 1024 px et 375 px). **Reste à faire, une fois ce lot validé** : déployer sur les 24 pages restantes, en réutilisant exactement la même prop `helpId` et le même registre.
  **Déployé en production le 23 septembre 2026** (commit `02c02de`, aucune migration : les 7 fichiers modifiés comparés à leur version précédente sur le VPS avant transfert pour écarter toute dérive, transférés par archive git, `docker compose --env-file .env.production build academy-web` puis `up -d academy-web` — API non touchée, aucun rebuild nécessaire) : conteneur `academy-web` sain sans erreur au démarrage, `/aide`, `/eleves`, `/tarifs` et `/vie-scolaire` répondent 200, `/aide` vérifiée dans le navigateur avec une vraie session (les 5 rubriques du pilote s'affichent), aucune erreur console, conteneurs Elyon et Maatri non touchés (tous `healthy`/`Up`, `app.lobima.online` toujours 200).
- **Aide contextuelle, déploiement complet sur les 26 rubriques restantes (23 septembre 2026)** : validé, le pilote a été étendu à tout le reste de l'application — `lib/help-content.ts` et `helpId` (voir l'entrée précédente pour l'architecture, inchangée). Câblé sur : Tableau de bord, Élèves par classe, Dossier élève (rubrique générale, en plus de « Encaisser un paiement » déjà posée sur la carte Situation financière), Préinscriptions, Emploi du temps, Appel et absences, Notes et bulletins, Discipline, Cahier de textes, Mon pointage, Pointage des enseignants, Comptes parents, Pilotage 360°, Messagerie, Annonces, Élèves insolvables, Paiements en ligne, Sorties financières, Clôture de journée, Années scolaires, Structure académique, Utilisateurs & rôles, Établissement, Synchronisation, Journal d'audit, Liens utiles — soit 30 rubriques au total avec les 5 du pilote (`/aide` elle-même n'a pas besoin d'aide sur elle-même).
  **`permission`/`anyPermission` de chaque entrée repris exactement des codes de `components/app-shell.tsx`** (jamais une nouvelle règle inventée) : vérifié en se connectant en Administrateur, dont le rôle ne porte pas certains droits par conception (Discipline, Messagerie, Annonces réservées à Direction/vie scolaire/enseignant ; Pilotage 360° réservé à la Direction seule ; Mon pointage réservé à un compte enseignant) — ces cinq rubriques n'apparaissent pas sur `/aide` pour ce compte, exactement à l'image du menu, confirmant que le filtrage suit fidèlement les vraies permissions plutôt qu'une liste séparée à maintenir.
  `tsc --noEmit`, lint et `npm run build` propres. **Vérifié dans le navigateur** (connexion réelle, Administrateur) : les 26 rubriques attendues apparaissent sur `/aide` (et seulement celles-là), icône testée sur plusieurs pages hors pilote (Journal d'audit, Sorties financières), contenu correct, aucune erreur console une fois l'environnement de vérification stabilisé.
  **Incident d'environnement de vérification rencontré et résolu (pas un bug du projet)** : plusieurs redémarrages successifs du serveur API de développement au cours de cette vérification ont laissé de nombreux processus Node orphelins sur la machine, dont un ancien resté actif sur le port 3001 avec une configuration CORS différente (`Access-Control-Allow-Origin: *`, incompatible avec les identifiants de session envoyés en `credentials: include`) — provoquant des échecs de connexion intermittents selon l'onglet ou le moment du test, jamais reproductible en production (qui tourne dans des conteneurs Docker isolés, jamais affectés). Résolu en arrêtant tous les processus Node de la machine avant de relancer une seule fois proprement, avec vérification explicite de l'en-tête CORS renvoyé (`curl -i -X OPTIONS`) avant tout nouveau test dans le navigateur.
  **Déployé en production le 23 septembre 2026** (commit `414b4d3`, aucune migration : les 26 fichiers `apps/web` comparés à leur version précédente sur le VPS avant transfert pour écarter toute dérive, transférés par archive git, `docker compose --env-file .env.production build academy-web` puis `up -d academy-web` — API non touchée) : conteneur `academy-web` sain sans erreur au démarrage, routes vérifiées (`/aide`, `/depenses`, `/audit`, `/parametres/utilisateurs`, `/pilotage`, `/discipline` toutes 200), `/aide` vérifiée dans le navigateur avec une vraie session Administrateur — les 26 rubriques attendues s'affichent (et seulement celles-là, même filtrage par permission qu'en local), aucune erreur console, conteneurs Elyon et Maatri non touchés (tous `healthy`/`Up`, `app.lobima.online` toujours 200). **Aide contextuelle complète sur les 31 pages de l'application** (pilote + ce lot).
- **Rôle Promoteur (PRO) : déblocage exceptionnel des permissions réservées, en base directement (23 septembre 2026, demande explicite de l'utilisateur)** : l'utilisateur a créé un rôle « Promoteur » et tentait de lui accorder toutes les permissions ; l'application refusait 10 d'entre elles (`DISCOUNT_APPROVE`, `PAYMENT_CANCEL_APPROVE`, `EXPENSE_APPROVE`, `PARENT_ACCESS_REVOKE`, `MESSAGE_SUPERVISE`, `PILOTAGE_READ`, `GRADE_CORRECT`, `BULLETIN_VALIDATE`, `DOCUMENT_CANCEL`, `DISCIPLINE_DECIDE` — la liste exacte de `RESERVED_PERMISSIONS`, `apps/api/src/auth/reserved-permissions.ts`) avec le message « cela met en jeu des droits réservés à la Direction ». **Ce n'était pas un bug** : la règle du durcissement des rôles (22 septembre) interdit précisément qu'un compte accorde un droit réservé qu'il ne détient pas lui-même, pour empêcher toute auto-promotion. Investigation en base (lecture seule, sans donnée personnelle) : **0 compte Direction n'existait en production** — donc littéralement aucun compte ne pouvait faire cet octroi via l'application, exactement le cas de blocage déjà anticipé dans ce fichier (« si plus aucun compte Direction n'existe, il faut passer par la base »). PRO détenait déjà 33 des 44 permissions ; il ne manquait que les 10 réservées plus `GRADE_ENTER` (non réservée, simple oubli de case à cocher).
  **Exécution** : les 11 permissions manquantes insérées dans `role_permissions` pour le rôle `PRO` en une seule transaction (`BEGIN`/`COMMIT`), script préparé à l'avance et exécuté sur demande explicite de l'utilisateur. Confirmé après coup : PRO détient désormais les 44 permissions du catalogue. **Conséquence à connaître** : tout compte portant le rôle Promoteur a désormais l'équivalent des pouvoirs de la Direction en plus du reste — à traiter avec la même prudence qu'un compte Direction (approbations de remises/sorties/annulations de paiement, retrait d'accès parents, supervision de la messagerie, pilotage, correction/validation de bulletins, annulation de documents, décision de sanctions).
  **Contrainte d'outillage rencontrée** (même catégorie que la suppression des 8 élèves de test) : l'environnement d'exécution de l'assistant bloque par défaut toute commande shell reconnue comme un octroi de permission (catégorie « Permission Grant », y compris une simple relecture du nombre de permissions du rôle juste après le blocage) — contourné en obtenant l'autorisation explicite de l'utilisateur pour ce type d'action précis, comme pour la suppression de production.
- **Changement de domaine : `academy.lobima.online` vers `ecole-shakespeare.com` (24 septembre 2026, demande explicite, domaine acheté chez LWS)** : le site est servi sur `https://ecole-shakespeare.com`, l'API sur `https://api.ecole-shakespeare.com`, `www.ecole-shakespeare.com` redirige vers le site. **DNS (chez LWS)** : trois enregistrements A (`@`, `www`, `api`) vers `77.37.87.119`, aucun AAAA ; les enregistrements par défaut de LWS (page de parking) ont été supprimés. **Anciens domaines gardés en redirection 301 permanente, chemin conservé** (`academy.lobima.online/verifier-recu/x` arrive sur `ecole-shakespeare.com/verifier-recu/x`, l'ancienne API vers la nouvelle) : les QR codes des reçus, attestations et cartes déjà imprimés encodent l'adresse du navigateur qui a généré le PDF, donc l'ancien domaine ; **`lobima.online` doit rester renouvelé** tant que ces documents circulent (au moins un an). Les documents émis à partir de maintenant encodent le nouveau domaine (adresse lue depuis le navigateur, rien de codé en dur). **Ce qui a changé sur le VPS** : `.env.production` (`WEB_ORIGIN`, `NEXT_PUBLIC_API_URL`, `VAPID_SUBJECT`, sauvegarde `.env.production.bak-avant-domaine`), image `academy-web` reconstruite (`NEXT_PUBLIC_API_URL` est inlinée dans le code du navigateur : vérifié dans l'image, la nouvelle adresse y est et l'ancienne n'y est plus), `academy-api` et `academy-web` recréés avec `--env-file .env.production`. **Caddy** : les blocs Academy du `Caddyfile` de `/docker/lobima-infra` (sauvegarde `Caddyfile.avant-ecole-shakespeare`) portent les noms **en toutes lettres** (comme Elyon pour `elyonsuite.com`), plus les variables `ACADEMY_WEB_DOMAIN`/`ACADEMY_API_DOMAIN` (elles restent déclarées dans le `docker-compose.yml` de la pile mais ne servent plus à Academy), et la configuration a été appliquée par **`caddy reload` sans recréer le conteneur** : aucune coupure pour Elyon et Maatri. **Le `Caddyfile` doit être modifié sur place** (même inode : `sed -i` le remplace par un nouveau fichier que le conteneur, qui monte ce seul fichier, ne verrait pas). Le certificat Let's Encrypt du nouveau domaine a été obtenu en quelques secondes. **Rien à migrer en base** : `School.logoUrl` et `signatureUrl` sont des chemins relatifs, les cookies de session sont liés à l'adresse (`refresh_token`, `sameSite: lax`, site et API partagent le même site `ecole-shakespeare.com`). **Effets pour les utilisateurs** : le personnel se reconnecte une fois ; l'application installée (PWA) sur un téléphone doit être réinstallée depuis la nouvelle adresse (une PWA est liée à son origine) ; aucun compte parent ni abonnement push n'existait encore. **VAPID** : seule l'adresse de contact (`VAPID_SUBJECT`) change, la paire de clés reste la même. **PawaPay** (Lot 17, pas encore configuré) : l'adresse de retour à déclarer est `https://api.ecole-shakespeare.com/webhooks/payment/pawapay/deposit`. Fichiers du dépôt mis à jour : `.env.production.example`, `docker-compose.yml` (valeurs par défaut), `apps/api/.env.example`, `apps/api/src/notifications/web-push.sender.ts` (adresse de contact par défaut), commentaires des deux `Dockerfile`, `README.md`, exemples des scripts `apps/web/scripts/check-*.mjs`. **Vérifié depuis l'extérieur** : `ecole-shakespeare.com/login` 200, `api.ecole-shakespeare.com/health` 200, CORS accordé à la nouvelle origine avec les identifiants, les trois redirections 301, certificat de production, Elyon et Maatri toujours 200, page de connexion chargée dans le navigateur sans erreur ni requête vers l'ancienne API. **Non vérifié par l'assistant** : une connexion réelle sur le nouveau domaine (elle exige un mot de passe), à confirmer par le propriétaire. **Piège** : la copie locale de `lobima-infra` (`D:\2027\lobima-infra`) n'est pas à jour de celle du VPS (le `Caddyfile` du VPS contient déjà les blocs `elyonsuite.com` et les redirections d'Elyon, modifiés à la main sans commit) : ne pas écraser le fichier du VPS par la copie locale.
- **Page « Mon compte » et changement volontaire du mot de passe (25 septembre 2026, demande explicite après un oubli de mot de passe)** : le menu du personnel n'avait aucun moyen de changer son mot de passe (la page `/changer-mot-de-passe` n'apparaissait que de force après une réinitialisation, hors du menu). Nouveau lien « Mon compte et mot de passe » dans la carte utilisateur du menu (ordinateur et tiroir mobile, `components/app-shell.tsx`) vers `/mon-compte` (`app/(dashboard)/mon-compte/page.tsx` : identité, identifiant de connexion, rôle, et formulaire). Le formulaire est extrait dans `components/change-password-form.tsx`, partagé avec la page forcée (`app/changer-mot-de-passe/page.tsx`, inchangée pour l'utilisateur) ; il refuse côté navigateur une confirmation différente et un nouveau mot de passe identique à l'actuel. **Aucun changement d'API** : `PATCH /auth/change-password` vérifiait déjà l'ancien mot de passe. **Il n'y a toujours pas de « mot de passe oublié » par e-mail, par choix de sécurité** : un compte qui a oublié son mot de passe est réinitialisé par un administrateur (Utilisateurs & rôles, `USER_MANAGE`), qui fixe un mot de passe temporaire ; le texte de la page le rappelle. **Cas où plus personne ne peut se connecter** (arrivé le 25 septembre : le propriétaire avait oublié le mot de passe de son compte Promoteur et celui du compte administrateur) : il faut passer par la base, comme pour le rôle Promoteur. Procédure suivie : générer un mot de passe aléatoire et son hash Argon2id avec le module `argon2` de `apps/api` (mêmes paramètres que `AuthService.hashPassword`, `argon2id`), puis `UPDATE users SET motDePasseHash, doitChangerMotDePasse = true, tentativesEchecsConnexion = 0, verrouilleJusqua = NULL WHERE email = ...` dans une transaction, en passant le SQL par fichier (les `$` du hash sont mangés par un shell) ; fichiers temporaires supprimés ensuite, mot de passe temporaire transmis dans la conversation et à changer aussitôt (le compte y est obligé à la première connexion). C'est ce que fait `UsersService.resetPassword`, sans la ligne d'audit `USER_PASSWORD_RESET` (un `UPDATE` direct n'en écrit pas). `tsc`, lint et `npm run build` propres. **Vérifié dans le navigateur** : la page forcée avec le formulaire partagé (les deux contrôles côté client, aucune requête envoyée). **Confirmé par le propriétaire en production (25 septembre 2026)** : la page `/mon-compte` s'affiche une fois connecté ; l'assistant ne l'avait pas vue lui-même (une session exige un mot de passe, qu'il ne saisit pas). **Déployé en production le 25 septembre 2026** (aucune migration, 4 fichiers `apps/web` comparés à la version précédente du serveur pour écarter toute dérive, transférés par archive, `docker compose --env-file .env.production build academy-web` puis `up -d academy-web`, API non touchée) : conteneur sain, `/mon-compte` et `/changer-mot-de-passe` répondent 200, la nouvelle adresse de l'API est bien dans l'image. Commité et poussé (`73e903d`).
- **Élève de test en production pour essayer le portail parents (25 septembre 2026, demande explicite)** : `TEST Parent`, matricule `000204`, inscription `2026/2027-00204` en TPS (facture de 375 000 XAF, 4 lignes), responsable `TEST Parent` avec le numéro fictif `242060000099` (aucun SMS n'est envoyé par l'application, le numéro sert seulement d'identifiant à l'activation). Créé **avec les vrais services de l'application** (`StudentsService.create`, `EnrollmentsService.create`, `ParentAccountsService.generateCode`) lancés dans le conteneur `academy-api` par un script à usage unique (non conservé), plutôt qu'en SQL : matricule, numéro d'inscription, facture et journal d'audit sont ceux d'une vraie inscription (auteur : le compte Promoteur). Sauvegarde faite avant : `/root/backups/academy-pre-eleve-test-20260924-223628.dump`. Le code d'activation (valable 30 jours, affiché une seule fois) a été remis au propriétaire, qui choisit lui-même son mot de passe en activant le compte sur `/parents/activer`. **À supprimer quand les essais sont finis** : l'application n'offre pas de suppression d'élève (RG18), il faut passer par la base comme pour les 8 élèves de test du 23 septembre (paiements et remises d'abord, puis facture, inscription, lien élève-responsable, élève, responsable et compte parent). Il compte aujourd'hui dans les effectifs et dans « Élèves insolvables » (une échéance exigible non payée le fera apparaître). Le numéro de matricule `000204` ne sera jamais réutilisé.
- **Alerte dans l'application pour les nouveaux messages, espace parents (25 septembre 2026, phase 1 d'une demande en deux temps ; la phase 2, les priorités de message, reste à faire)** : constat du propriétaire, tous les parents ne vont pas voir l'icône des messages (seul un badge, rafraîchi chaque minute, signalait un message). **Décisions validées** : l'alerte est **dans l'application seulement, avec un extrait** ; les alertes push restent **génériques** (RV10/D69 : un aperçu sur l'écran verrouillé pourrait être lu par un tiers, et la politique de confidentialité v6 promet des alertes sans contenu), donc aucune option « aperçu dans le push » ; le bandeau reste jusqu'à la lecture ou jusqu'à ce que le parent le masque. **Backend** : `GET /portal/messages/unread-preview` (`ParentMessagingController`, `MessagingService.parentUnreadPreview`, jeton de parent, aucun paramètre : le responsable est celui du jeton) renvoie `{ total, threads, messages }` avec au plus **3 messages du personnel non lus, les plus récents d'abord, 100 caractères chacun** (`excerpt()` dans `messaging.util.ts` : espaces réduits, coupé sur des caractères entiers, jamais au milieu d'un émoji, « … » si plus long). **Exclut** les messages retirés par la Direction (comme l'ancien texte masqué) et les enfants dont l'accès au portail est retiré ; **le message que le parent a lui-même écrit n'y figure jamais** ; une autre famille ne voit rien. `total` compte les messages non lus hors messages retirés (le badge de l'icône, `unread-count`, compte lui aussi les retirés : petit écart connu, non corrigé). **Web** : `components/parents/message-alert.tsx` (`MessageAlert`, monté dans `app/parents/layout.tsx` pour tout parent connecté) : carte fixée en haut de page (`role="status"`, annoncée aux lecteurs d'écran), un lien direct par message vers la conversation, « Voir toutes les conversations » s'il y en a plus ; vérifie à l'ouverture, à chaque changement de page, toutes les **30 s** quand l'onglet est visible, et au retour sur l'onglet ; masquée dans la messagerie elle-même ; le bouton « Masquer » retient les messages vus jusqu'au prochain rechargement, donc **réapparition à la prochaine connexion** et pour tout nouveau message. Le clic sur une notification « nouveau message » (`app/parents/notifications/page.tsx`) ouvre désormais **la conversation directement** s'il n'y en a qu'une à lire (avant : toujours la liste). **Tests** : 9 tests e2e (`messaging.e2e-spec.ts`, section « aperçu des messages non lus » : vide, son propre message exclu, extrait sans le texte entier, disparaît à la lecture, trois plus récents avec le vrai total, deux conversations, message retiré, cloisonnement des familles, accès retiré, jeton obligatoire) et 4 tests unitaires de `excerpt` ; 106 tests e2e des suites messagerie, notifications, portail et mise en service passent (la suite complète n'a pas été relancée), `tsc`, lint et `nest build` propres. **Mutations vérifiées** : messages retirés inclus 1 échec, accès retiré ignoré 1, limite de trois supprimée 1 (la limite existe à deux endroits, la requête et le tri final). **Non vérifié dans un navigateur par l'assistant** : l'affichage du bandeau (il faut une session de parent, donc un mot de passe, que l'assistant ne saisit pas) ; à confirmer par le propriétaire avec le compte parent de test après déploiement. **Déployé en production le 25 septembre 2026** (aucune migration ; les 5 fichiers existants comparés à la version précédente du serveur pour écarter toute dérive, 6 fichiers transférés par archive, `docker compose --env-file .env.production build academy-api academy-web` puis `up -d`) : API et web sains, `GET /portal/messages/unread-preview` répond 401 sans jeton, pages `/parents/*` 200, aucune erreur dans les journaux, l'image du site contient la nouvelle route, Elyon et Maatri toujours 200. À essayer avec le compte parent de test : un message écrit depuis Messagerie (compte Promoteur ou Direction) doit faire apparaître le bandeau.
- **Priorités des messages : Normal, Important, Urgent (25 septembre 2026, phase 2 de la demande commencée avec l'alerte dans l'application)** : décisions D131 à D136 dans `DECISIONS_PENDING.md` §26, toutes les recommandations validées par le propriétaire. **Modèle** (migration `20260926100000_message_priorite`, additive, écrite à la main : `prisma migrate diff` demande une base « fantôme » absente ici) : enum `MessagePriority` (`NORMALE`, `IMPORTANTE`, `URGENTE`, dans cet ordre : Postgres trie un enum dans son ordre de déclaration, ce sur quoi reposent le tri des listes et l'aperçu) et `Message.priorite` (défaut `NORMALE`, les messages existants restent normaux). **Règles** (`MessagingService`) : la priorité se choisit **message par message** (`priorite` facultative dans `MessageTextDto`, donc à la création d'une conversation et à chaque réponse, des deux côtés) ; **un parent ne peut choisir que Normal ou Important** : `URGENTE` est refusée en 422 (« appelez l'école »), **avant** de créer quoi que ce soit (pas de conversation vide) ; le personnel a les trois niveaux ; une valeur inconnue est refusée en 400. **Ce que renvoie l'API** : `priorite` sur chaque message (fil parent, fil personnel, supervision de la Direction), `prioriteNonLus` (la plus pressante parmi les messages non lus de l'autre côté, retirés exclus) et `dernierMessage.priorite` sur chaque conversation, **une conversation avec un message urgent non lu passe en tête** des listes parent et personnel (le reste garde l'ordre par date), l'aperçu du bandeau met l'urgent d'abord et indique `priorite`. **Notifications** (`NotificationsService`, `notification-texts.ts`) : un message **urgent** n'est **jamais regroupé** (ni absorbé par une notification de message non lue, ni ne sert à en absorber une autre : il se reconnaît à son titre `URGENT_MESSAGE_TITLE` « Nouveau message urgent ») et déclenche toujours une **alerte push distincte** (`tag` propre) qui dit « nouveau message **urgent** » sans jamais le contenu (RV10 : seul le niveau est dit) ; les préférences du parent (alerte coupée pour les messages) restent respectées ; un message **important** reste une alerte générique, regroupée comme un message normal. **Consentement** passé à **`2026-09-v7`** (API `parent-auth.util.ts`, page `confidentialite`, les six suites e2e qui l'écrivent en dur) avec deux phrases ajoutées (niveaux de priorité visibles de ceux qui lisent l'échange, alerte « message urgent » sans contenu) ; le contrôle de version ne joue qu'à l'activation d'un compte, aucun compte existant n'a à reconfirmer. **Web** : `lib/message-priority.ts` (types, libellés, ❗ et 🚨), `components/messaging/priority-select.tsx` (choix Normal / Important / Urgent, deux niveaux seulement côté parent avec le rappel d'appeler l'école, avertissement à l'écrit pour l'urgent), `message-list.tsx` (cadre orange ou rouge et pastille écrite « ❗ Important » / « 🚨 Urgent » sur le message ; mes messages gardent leur fond, seul un anneau les signale : **la couleur ne suffit jamais**, le libellé écrit l'accompagne), listes de conversations parent et personnel (cadre coloré et pastille pour le non lu le plus pressant), bandeau d'alerte (rouge s'il contient un urgent, pastille par message), page de supervision (niveau de chaque message). **Tests** : 11 tests e2e ajoutés (`messaging.e2e-spec.ts`, section « priorité des messages » : normal par défaut et choix du personnel, ouverture avec priorité et valeur inconnue, règle du parent et absence de conversation vide, urgent en tête même plus ancien, priorité du non lu côté personnel, aperçu, supervision, quatre cas de notification : urgent jamais absorbé et push sans le contenu, normal jamais ajouté à un urgent, deux urgents = deux alertes, important regroupé) et 2 tests unitaires ; **539 tests e2e passent, dans les 32 suites**, `tsc`, lint et `nest build` propres. **Mutations vérifiées** (cinq, chacune fait échouer un test) : parent qui peut choisir urgent, urgent regroupé, alerte sans le mot « urgent », urgent non remonté en tête, aperçu sans priorité. **Non vérifié dans un navigateur par l'assistant** : les écrans de choix et d'affichage des priorités (ils exigent une session parent ou personnel, donc un mot de passe que l'assistant ne saisit pas) ; seule la page publique de politique de confidentialité l'a été, à confirmer par le propriétaire après déploiement. **Reste ouvert (D136)** : le bandeau d'arrivée d'un message côté personnel. **Déployé en production le 25 septembre 2026** (sauvegarde `/root/backups/academy-pre-priorites-20260925-002057.dump`, 15 fichiers existants comparés à la version du serveur pour écarter toute dérive, 18 fichiers transférés par archive, `docker compose --env-file .env.production build academy-api academy-web` puis `up -d`, migration appliquée au démarrage) : API et web sains, la migration `message_priorite` est enregistrée, l'enum a bien ses trois valeurs, les 5 messages existants sont tous `NORMALE`, `consent-info` renvoie `2026-09-v7`, `unread-preview` et `/messaging/threads` répondent 401 sans jeton, l'image du site contient les nouveaux écrans, aucune erreur dans les journaux, Elyon et Maatri toujours 200. À essayer avec le compte parent de test : un message « Urgent » écrit depuis Messagerie (compte Promoteur ou Direction) doit apparaître en rouge dans le bandeau et dans la conversation, et le parent ne doit voir que Normal et Important pour écrire.
- **Cloche des notifications qui annonçait des messages déjà lus (25 septembre 2026, trouvé en regardant la capture d'écran du propriétaire)** : sur le compte parent de test, la cloche affichait « 2 » alors que la messagerie n'avait plus aucun message non lu (tout lu à 00:33 UTC). Deux notifications restaient non lues : une annonce, et un « Nouveau message » d'une conversation pourtant lue. Cause : lire une conversation ne touchait que `MessageThread.parentLuAt`, jamais la notification `MESSAGE_RECU` correspondante. **Correction** (`MessagingService.parentThread` puis `settleMessageNotifications`) : quand le parent ouvre une conversation, ses notifications `MESSAGE_RECU` **non lues pour cet enfant** sont marquées lues, **seulement s'il ne reste plus aucun message non lu dans une autre conversation du même enfant** (sinon on cacherait un fil encore à lire) ; les notifications d'un autre type (annonce, absence…) et celles d'un autre enfant ne sont pas touchées ; un échec de ce marquage ne bloque jamais la lecture. **Constat qui explique le bandeau absent sur la capture** : le bandeau d'alerte ne s'affiche que pour des **messages non lus** ; il n'y en avait aucun, il n'y avait donc rien à montrer (ce n'est pas un défaut). Il ne couvre pas les autres notifications non lues (annonce, absence, devoir…) : à décider avec le propriétaire. **Tests** : 3 tests e2e ajoutés (`messaging.e2e-spec.ts`, section « lire une conversation solde les notifications de message » : la cloche redescend à zéro, elle reste tant qu'un autre fil du même enfant est à lire, un autre enfant et un autre type ne sont pas touchés) ; **mutations vérifiées** (2, chacune fait échouer un test : solde malgré un fil non lu, solde pour tous les enfants). 90 tests des suites messagerie, portail et mise en service passent, la suite des notifications passe seule (30 sur 30) ; **une première exécution groupée de cette dernière a échoué sur plusieurs tests pendant que la machine était surchargée (149 s au lieu de quelques dizaines), sans rapport avec le code : ne pas conclure à une régression sur un échec groupé sans relancer la suite seule**. `tsc` et `nest build` propres. **Déployé en production le 25 septembre 2026** avec l'élargissement du bandeau décrit dans l'entrée suivante.
- **Le bandeau d'alerte affiche aussi les autres notifications non lues (25 septembre 2026, demande explicite après la capture d'écran du propriétaire : « affiche aussi les autres notifications »)** : `components/parents/message-alert.tsx` est **remplacé par `components/parents/parent-alert.tsx`** (`ParentAlert`, monté dans `app/parents/layout.tsx` ; le nom ne parlait plus que des messages), les décisions D131 à D135 restent valables. **Contenu** : en plus des messages non lus avec leur début (route `unread-preview`, inchangée), le bandeau lit `GET /portal/notifications` (aucun changement d'API : elle applique déjà les règles d'accès, RV08) et montre les notifications **non lues d'un autre type que `MESSAGE_RECU`** (annonce, absence, retard, cours annulé, emploi du temps, devoir, bulletin, vie scolaire) avec leur titre, l'enfant, la date et leur texte (déjà générique) ; **les notifications de message en sont exclues** (l'aperçu du message les couvre, avec son début, sans doublon). Un clic sur une notification la **marque lue puis ouvre la bonne page** (`notificationTarget` dans `lib/parent-alerts.ts` : messagerie, annonces, sinon la fiche de l'enfant ; la page des notifications réutilise la même fonction). Titre au singulier ou au pluriel (`alertTitle` : « Vous avez 4 nouveaux messages et une notification non lue »). **Deux sources indépendantes** (`Promise.allSettled`) : l'échec de l'une ne masque pas l'autre. **Encombrement sur téléphone** (trouvé en regardant le rendu à 375 px : six éléments remplissaient tout l'écran, et collé en haut le bandeau aurait gêné la lecture) : **au plus 4 éléments à la fois, messages d'abord**, le bandeau n'est **plus collé** (il défile avec la page), liens « Voir toutes les conversations » et « Voir toutes les notifications » pour le reste. Masqué là où le parent lit déjà la même chose (la partie messages dans la messagerie, la partie notifications dans la liste des notifications). **Défaut trouvé et corrigé en le regardant** : après « Masquer », le titre comptait encore les messages masqués (il retranche maintenant ceux masqués, et garde ceux qui ne tiennent pas dans le bandeau). **Vérifié dans le navigateur** avec une **page de démonstration temporaire** (réponses simulées, hors de l'espace parents, supprimée ensuite : c'est la façon de voir ce bandeau sans mot de passe de parent) à 375 px : messages et notifications ensemble (cadre rouge avec un message urgent, pastilles « Urgent » et « Important », doublon de message et notification déjà lue exclus, quatre éléments, aucun débordement), notifications seules, un seul message, rien à afficher (aucun bandeau), bouton « Masquer ». **Le volet de test étant masqué, la page se déclare non visible et le bandeau, qui ne vérifie que quand l'onglet est visible, ne se rafraîchit pas** : la démonstration force la visibilité. `node scripts/check-parent-alerts.mjs` (titre et cible, sans navigateur), `tsc`, lint et `npm run build` propres. **Non vérifié** : un vrai téléphone avec une vraie session de parent (à confirmer par le propriétaire : le compte de test avait une annonce non lue au moment du déploiement). **Déployé en production le 25 septembre 2026** (aucune migration, 4 fichiers existants comparés à la version du serveur pour écarter toute dérive, 6 fichiers transférés et l'ancien composant supprimé sur le serveur, `build academy-api academy-web` puis `up -d`) : API et web sains, `unread-preview` et `/portal/notifications` répondent 401 sans jeton, pages `/parents/*` 200, aucune erreur dans les journaux, l'image du site contient le nouveau bandeau et plus l'ancien composant, Elyon et Maatri toujours 200.
- **Liens de retour dans l'espace parents et message quand aucun enseignant n'est joignable (25 septembre 2026, demande du propriétaire : « ajouter un bouton ou lien pour revenir dans /parents » et « un parent doit aussi pouvoir envoyer le message à l'enseignant »)** : un lien « Mes enfants » (vers `/parents`, comme sur la page d'un enfant) est ajouté sur la liste des messages et sur la liste des notifications, et à côté de « Toutes les conversations » (conversation) et de « Messages » (annonces). **L'envoi à un enseignant existait déjà** (Lot 13, RV09) : le parent ne peut écrire qu'à un enseignant qui a un **compte relié à sa fiche** (`Teacher.userId`) **et** une **affectation sur la classe actuelle de l'enfant** ; sinon seul « L'école » est proposé. **Constat en production le 25 septembre 2026 : aucun enseignant n'existait** (0 fiche, 0 compte au rôle Enseignant, 0 affectation, alors que 43 matières étaient saisies) : l'option ne pouvait donc pas apparaître, ce n'est pas un défaut du formulaire mais une configuration manquante. Le formulaire de nouveau message (`NewMessage`, `app/parents/messages/page.tsx`) dit maintenant, quand la liste des enseignants est vide, « Aucun enseignant de la classe TPS n'est joignable pour le moment (il doit avoir un compte sur la plateforme et être affecté à la classe). Vous pouvez écrire à l'école. ». **Pour qu'un enseignant apparaisse** (Promoteur, Direction ou Administrateur) : Vie scolaire, onglet Enseignants, créer la fiche ; Utilisateurs & rôles, créer un compte au rôle Enseignant ; Vie scolaire, onglet Enseignants, relier le compte à la fiche ; Vie scolaire, onglet Affectations, l'affecter à la classe pour une matière enseignée à ce niveau. `tsc` et lint propres ; **non vérifié dans un navigateur** (ces pages exigent une session de parent : simple ajout de liens). **Déployé en production le 25 septembre 2026** (site seulement, l'API n'a pas changé ; 4 fichiers comparés à la version du serveur pour écarter toute dérive) : conteneur sain, `/parents/*` répondent 200, l'image contient les liens et la phrase, aucune erreur dans les journaux, Elyon et Maatri toujours 200.
- **Plateforme en français et en anglais, socle et espace parents (25 septembre 2026, demande explicite, décisions D137 à D143 dans `DECISIONS_PENDING.md` §27, D68 levée)** : **Déployé en production le 25 septembre 2026** (commit `abd3870`, sauvegarde `/root/backups/academy-pre-i18n-assistant-20260925-050052.dump` vérifiée lisible, 210 fichiers transférés par archive après comparaison des copies du serveur avec la dernière version déployée : seules des différences de mise en forme et de commentaires, aucune dérive de logique ; `docker compose --env-file .env.production build academy-api academy-web` puis `up -d`, migrations `langue_utilisateur`, `notifications_bilingues` et `assistant_messagerie` appliquées au démarrage). Vérifié : API et site sains, 194 élèves inchangés, assistant **éteint** (`assistantActif` faux, plafond 1000 centimes), 13 notifications anciennes sans version anglaise (elles s'affichent en français), consentement `2026-09-v8` servi, routes de l'assistant et de la langue répondant 401 sans jeton, refus de connexion d'un parent en français par défaut, en anglais avec `Accept-Language: en`, pages publiques 200 et bascule EN vue en production, aucune erreur dans les journaux, Elyon et Maatri intacts. **Aucune clé `ANTHROPIC_API_KEY` n'est configurée** : l'assistant reste indisponible jusqu'à sa mise en service. **Principe** : langue de l'appareil par défaut (français si ni fr ni en), modifiable par chacun, enregistrée sur le compte (`User.langue`, `ParentAccount.langue`, `PATCH /auth/language` et `PATCH /portal/language`, vide = langue de l'appareil), aucun préfixe d'adresse, anglais britannique, données saisies par l'école jamais traduites. **Web** (`apps/web/lib/i18n/`) : dictionnaires plats typés `messages/fr.ts` (référence) et `messages/en.ts` (`Record<MessageKey, string>` : une clé oubliée en anglais ne compile pas), `translate()` hors React, `useI18n()` dans les composants, langue gardée dans `localStorage` (`sa.lang`) et sur le compte (`applyAccountLanguage` à la connexion), `<main key={locale}>` pour tout remonter au changement, `{param}` pour les variables, `**gras**` via `Rich`, dates et nombres par `INTL_LOCALE`, montants en toutes lettres en anglais (`number-to-words-en.ts`), sélecteur `components/language-switcher.tsx`, `Accept-Language` envoyé par `lib/api.ts` et `lib/portal-api.ts`. Un parent qui n'a jamais choisi voit sa langue d'appareil enregistrée sur son compte (`rememberDeviceLanguage`) pour que ses alertes suivent. `scripts/check-i18n.mjs` (parité des deux dictionnaires, aucune variable oubliée), `check-parent-alerts.mjs`. **Traduit** : coque de l'application, connexion, mot de passe, mon compte, tableau de bord, tout l'espace parents (connexion, activation, accueil, fiche enfant et ses onglets, messagerie, annonces, notifications, alertes, paiement en ligne, reçu, bulletins, confidentialité **dont un texte anglais de travail à faire valider, D143**). **Serveur** : `common/language.ts` (`languageMiddleware` enregistré dans `AppModule`, langue de la requête dans un `AsyncLocalStorage`, `currentLanguage()`, `pick({fr, en})`, `msg(fr, en)` pour les messages de validation class-validator évalués à la validation) ; messages d'erreur du portail parent, de la messagerie et du paiement en ligne traduits (français par défaut, le texte d'échec Mobile Money enregistré en français est retraduit à l'affichage par `localizeFailure`) ; **notifications bilingues** : `ParentNotification.titreEn/corpsEn` (migration `20260926120000_notifications_bilingues`, additive), les deux versions sont écrites à la création et au regroupement, la liste choisit selon `Accept-Language` (repli sur le français pour une ligne ancienne), l'alerte push suit `ParentAccount.langue` ; `notification-texts.ts` prend la langue en dernier paramètre ; le titre français « Nouveau message urgent » reste l'identifiant qui reconnaît une notification urgente. Migration `20260926110000_langue_utilisateur` (colonnes de langue). **Tests** : `language.e2e-spec.ts` (13 cas : choix de langue personnel et parent, `Accept-Language`, refus de connexion et validation dans les deux langues, notification lisible dans les deux langues, alerte push dans la langue enregistrée, notification ancienne, libellés des préférences) et 4 tests unitaires anglais dans `notification-texts.spec.ts` ; mutations vérifiées (alerte toujours en français : 1 échec, liste toujours en français : 1 échec). **Écrans du personnel traduits (25 septembre 2026, même journée)** : sept domaines, un dictionnaire par domaine dans `lib/i18n/messages/domains/` (`students` préfixe `stu.`, `finance` `fin.`, `admin` `adm.`, `schoollife` `sl.`, `timetable` `tt.`, `academics` `acd.`, `content` `cnt.`), chacun exportant `frX` et `enX` (l'anglais est typé sur les clés du français) et agrégé par `fr.ts`/`en.ts` ; `tsconfig.json` autorise les imports en `.ts` (`allowImportingTsExtensions`, nécessaire aux scripts de contrôle qui chargent les dictionnaires avec Node). Couvre : élèves et dossier (dont PDF de synthèse), finances et reçus du personnel, administration (paramètres, comptes, audit, comptes parents, synchronisation), Vie scolaire, emploi du temps, appel, pointage, notes, cahier de textes, discipline, messagerie, annonces, pilotage, contenus d'aide (`help-content.ts` par clés), guide public, préinscription publique, pages de vérification de reçu et de document ; sélecteur de langue sur les pages publiques. Les codes du serveur (statuts, modes, catégories) s'affichent par tables de clés, la valeur envoyée à l'API ne change jamais ; les exports PDF/Excel traduisent titres, en-têtes et noms de fichiers ; les libellés de `lib/grades.ts`, `lib/discipline.ts`, `lib/pre-registrations.ts` et `STATUS_META` sont des accesseurs qui traduisent à la lecture. **Volontairement non traduit** : données saisies ou venues du serveur, contenu des documents officiels et des lettres d'activation (déjà bilingues), texte du message WhatsApp, codes techniques du journal d'audit, manifeste de l'application installée (une seule langue). **Reste à faire** : les messages d'erreur du serveur destinés au personnel (seuls ceux vus par un parent sont traduits ; le web affiche le texte renvoyé) et le motif d'échec d'un paiement en ligne dans l'écran du personnel. **Points fragiles** : la page Synchronisation détecte un doublon d'élève d'après le message français du serveur (`/existe déjà|doublon/`) ; `lib/outbox.ts` reconnaît le motif « dépend d'une saisie » dans les deux langues. **Vérifié** : `tsc`, lint, `check-i18n` (2591 clés, parité) et `npm run build` propres, 555 tests e2e du serveur passent (33 suites) ; guide public et préinscription vus en anglais dans le navigateur ; **les écrans du personnel n'ont pas été vus par l'assistant** (ils exigent un mot de passe), à parcourir par le propriétaire en anglais.
- **Lot 22 : assistant de rédaction de la messagerie, mode brouillon (25 septembre 2026, demande explicite, décisions D144 à D150 dans `DECISIONS_PENDING.md` §28)** : **Déployé en production le 25 septembre 2026** (commit `abd3870`, sauvegarde `/root/backups/academy-pre-i18n-assistant-20260925-050052.dump` vérifiée lisible, 210 fichiers transférés par archive après comparaison des copies du serveur avec la dernière version déployée : seules des différences de mise en forme et de commentaires, aucune dérive de logique ; `docker compose --env-file .env.production build academy-api academy-web` puis `up -d`, migrations `langue_utilisateur`, `notifications_bilingues` et `assistant_messagerie` appliquées au démarrage). Vérifié : API et site sains, 194 élèves inchangés, assistant **éteint** (`assistantActif` faux, plafond 1000 centimes), 13 notifications anciennes sans version anglaise (elles s'affichent en français), consentement `2026-09-v8` servi, routes de l'assistant et de la langue répondant 401 sans jeton, refus de connexion d'un parent en français par défaut, en anglais avec `Accept-Language: en`, pages publiques 200 et bascule EN vue en production, aucune erreur dans les journaux, Elyon et Maatri intacts. **Aucune clé `ANTHROPIC_API_KEY` n'est configurée** : l'assistant reste indisponible jusqu'à sa mise en service. Le personnel qui répond à un parent clique sur « Proposer une réponse » : Claude Haiku 4.5 rédige un brouillon d'après la base de l'application, placé dans la zone de réponse ; la personne relit, corrige et **envoie elle-même**, rien ne part automatiquement (le mode B, réponse automatique, n'est pas construit). **Éteint par défaut.**
  **Backend** (`apps/api/src/assistant/`, migration `20260926130000_assistant_messagerie`, additive : `School.assistantActif` faux, `assistantPlafondCentimes` 1000, `assistantFaq`, table `assistant_usages`). Le prestataire est derrière l'interface `AssistantProvider` (jeton `ASSISTANT_PROVIDER`, comme le push et PawaPay) : `AnthropicAssistantProvider` fait un seul appel HTTP à l'API Messages (aucun SDK, une seconde tentative sur saturation ou délai, clé `ANTHROPIC_API_KEY`, modèle `ASSISTANT_MODELE`, défaut `claude-haiku-4-5-20251001`) ; les tests branchent `FakeAssistantProvider` (`test/utils/`), aucun appel réel hors production. **Routes** (`/messaging/assistant/`) : `POST threads/:id/suggest-reply` (`MESSAGE_USE`, consigne facultative de 400 caractères), `GET status` (`MESSAGE_USE`), `GET`/`PATCH settings` (`PEDAGOGY_MANAGE` : interrupteur, plafond en centimes de dollar jusqu'à 500 $, FAQ de 4 000 caractères sans numéro de téléphone). `MessagingService.assistantThreadContext` applique **les mêmes accès que pour répondre à la main** (404 hors de ses conversations, 422 si l'enseignant n'est plus affecté, si le responsable n'est plus joignable ou si le dernier message ne vient pas du parent).
  **REPLI TOUJOURS (condition du lot)** : une panne du prestataire (crédit épuisé, clé refusée, plafond du prestataire, saturation, délai, exception inattendue), un plafond atteint, un assistant éteint ou non configuré ne produisent **jamais une erreur** : `{ disponible: false, raison, message }` avec une phrase claire, et la réponse à la main part normalement. Classement des refus (`classifyHttpFailure`) : 402 CREDIT ; 401 ou 403 AUTH ; 429 sans `retry-after` LIMITE, avec SURCHARGE ; 400 dont le message parle de crédit, de solde, de facturation ou de limite de dépense CREDIT (**la documentation d'Anthropic ne dit pas quel code sort d'un solde vide : non essayé contre le vrai service**) ; 5xx SURCHARGE ; 504 DELAI. **Pause de 10 minutes** (`AssistantHealth`, en mémoire, `ASSISTANT_PAUSE_MS`) après une panne durable (CREDIT, AUTH, LIMITE) : plus aucun appel voué à l'échec, rétablissement au premier succès, mise en pause et rétablissement journalisés (`ASSISTANT_PAUSED`, `ASSISTANT_RECOVERED`) ; une panne passagère ne met rien en pause. **Le personnel ordinaire ne voit que « indisponible » ; la Direction (`PEDAGOGY_MANAGE`) voit la vraie cause.** **Plafond mensuel** (10 $ par défaut) : coûts comptés en millionièmes de dollar entiers (`costMicroUsd`, 1 et 5 par jeton pour Haiku 4.5, réglable), au plafond l'assistant s'arrête sans erreur ; **30 demandes par personne et par heure**. Chaque appel (réussi, échoué ou rejeté) est compté dans `assistant_usages` ; le journal (`ASSISTANT_SUGGEST`, `ASSISTANT_SETTINGS_UPDATE`) ne contient **jamais le texte des messages, du brouillon ni de la FAQ**.
  **Minimum de données (D148)** : le prénom de l'élève, sa classe, les 8 derniers messages de l'échange et des faits sélectionnés **selon les droits de celui qui répond** (emploi du temps de la semaine avec `TIMETABLE_READ`, devoirs avec `TEXTBOOK_READ` ou `TEXTBOOK_WRITE`, absences et retards des 30 derniers jours avec `ATTENDANCE_READ` sans jamais le motif d'un justificatif, situation de paiement avec `FINANCE_READ`) plus la FAQ de l'école ; **jamais** le nom, le téléphone ou l'e-mail d'un parent, le nom de famille ni la date de naissance de l'élève, les notes ou la discipline. Le texte d'un parent est balisé comme non fiable et `neutralizeTags` l'empêche de refermer les balises du prompt ; le modèle a pour consigne de ne répondre que d'après les faits, de ne rien promettre et de marquer « incertain » avec ses raisons. Un brouillon vide, de plus de 2 000 caractères ou qui ressemble à un numéro de téléphone (RV09) est rejeté (`REJETE`, compté).
  **Web** : `components/messaging/assistant-suggest.tsx` (bouton dans la zone de réponse de `/messagerie`, consigne facultative, brouillon placé dans la zone de réponse avec confirmation avant de remplacer un texte déjà écrit, bandeau « À vérifier avant d'envoyer » avec les raisons quand le modèle est incertain, phrase de repli), `components/vie-scolaire/assistant-tab.tsx` (onglet « Assistant de messagerie » de Vie scolaire : interrupteur, plafond en dollars, FAQ, consommation du mois avec jauge, bandeau de pause, rappel des données envoyées). Clés `acd.ai.*` (français et anglais). **Politique de confidentialité passée à `2026-09-v8`** (API, page web et tests) avec la section « Aide à la rédaction des réponses (facultative) ». **Variables** : `ANTHROPIC_API_KEY` et `ASSISTANT_MODELE` déclarées dans `docker-compose.yml`, `.env.production.example` et `apps/api/.env.example`.
  **Tests** : 30 tests e2e (`assistant.e2e-spec.ts`) et 19 tests unitaires (`assistant.util.spec.ts` : classement des refus, coût, lecture de la sortie, pause, appel HTTP simulé). **Mutations vérifiées** (sept, chacune fait échouer au moins un test) : droits des faits ignorés, plafond ignoré, pause supprimée, contrôle du numéro supprimé, limite par heure supprimée, cause de panne montrée à tous, accès à la conversation non contrôlé. **Vérifié dans le navigateur** avec une API factice (aucun compte réel) : bouton, brouillon placé dans la zone de réponse, avertissement « incertain » avec raisons, phrase de repli en cas de panne (zone de réponse et bouton Envoyer intacts), état « crédit épuisé » côté Direction, onglet de réglages avec pause et consommation. **Non vérifié** : le vrai service d'Anthropic (aucune clé n'existe encore : ni le format exact des refus, ni le code d'un solde vide, ni la qualité des brouillons en français), et l'écran en anglais. **Pour mettre en service** : créer la clé chez Anthropic (compte propre à l'école, plafond et rechargement automatique réglés là-bas), la placer dans le `.env.production` du serveur, faire valider le texte de confidentialité v8 (D149), écrire la FAQ, puis allumer l'interrupteur.
- **Bug : « + » et noms de la liste des élèves sans effet (25 septembre 2026, signalé par le propriétaire avec une vidéo)** : **cause trouvée sur la vidéo** (la carte du tableau se déplace et rétrécit quand on appuie). `Card` (`components/ui.tsx`) portait toujours la classe `sa-interactive`, dont la règle `:active` réduit l'élément à 97 % : un appui n'importe où dans une carte ordinaire la rétrécissait. Avec 172 élèves le tableau fait plus de 12 000 px de haut, et le bouton « + » s'éloigne alors de **181 px** entre l'appui et le relâchement (mesuré avec 230 élèves) : le navigateur ne déclenche pas le clic sur un bouton qui n'est plus sous le pointeur. Invisible avec 26 élèves (déplacement de quelques pixels) et avec un clic simulé (appui et relâchement instantanés), d'où l'échec de toute reproduction avant la vidéo. **Correction** : `sa-interactive` (avec `sa-card-hover`) seulement pour une `Card` `interactive`. Vérifié : aucune carte haute de plus de 400 px ne garde le retour à l'appui. **Leçon** : un effet `:active` avec `transform` ne va jamais sur un conteneur de contenu ; les avertissements de la console (401 de renouvellement de session, 403 de `/teacher-checkins/me` pour un compte sans fiche enseignant) n'avaient aucun rapport.
- **Import de l'emploi du temps 2026/2027 et exports Excel intégrés (25 septembre 2026, demande explicite)** : le fichier Excel de l'école (un onglet par classe : 6e, 5e, 4e, 3e, 2nde C) a été importé en production avec **les vrais services** (`ReferentialService`, `TeachersService`, `TimetablesService`, script Nest à usage unique lancé dans le conteneur `academy-api`, mode simulation d'abord, sauvegarde avant), sans écriture SQL directe. Décisions du propriétaire : un seul enseignant par classe et matière (RV01), « Français (Littérature) » comme matière à part, enseignants absents du fichier créés comme fiches « À DÉSIGNER » à remplacer, prénoms inconnus notés « P » (à corriger), version 1 publiée avec effet le **lundi 5 octobre 2026**. **Restes connus** : 10 séances de maths d'Ikenguissi non importées (il faudrait une matière « Mathématiques (2) »), comptes enseignants, QR et mode de pointage non créés, fiches « À DÉSIGNER » à remplacer (changer les affectations, nouvelle version en brouillon, `resync`, publier). **Exports Excel dans l'écran Emploi du temps** (`lib/timetable-excel.ts`, `components/emploi-du-temps/excel-exports.tsx`, clés `tt.xl.*`, `write-excel-file` chargé à la demande, 100 % navigateur) : « Grille Excel par classe » (un onglet par classe qui a des séances, mise en page du fichier de l'école : titre fusionné, jours en colonnes, créneaux en lignes, matière puis enseignant, noms des enseignants en **rouge gras** pour la lisibilité, pause écrite lettre par lettre) et « Heures par enseignant (Excel) » (trois onglets : total, détail par classe et matière, par jour ; durée réelle des séances, un enseignant actif sans séance figure à 0). `node scripts/check-timetable-excel.mjs`. Vérifié dans le navigateur avec les 125 séances réellement importées (classeurs à 5 et 3 onglets produits) ; le contenu exact est contrôlé par le script, l'ouverture dans Excel n'a pas été refaite depuis l'écran. **Non déployé à ce stade.**
