# Shakespeare Academy — contexte projet

Document de référence complet : `docs/Shakespeare_Academy_Cahier_de_Cadrage_v1.0.docx`.
**En cas de doute ou de divergence, ce cahier de cadrage fait foi — pas cette page, qui n'en est qu'un résumé opérationnel.**
Décisions métier encore non validées par l'école : `DECISIONS_PENDING.md` — ne jamais inventer une valeur pour un point qui y figure comme `OUVERT`.

## Doctrine

Critère central de réussite : sécuriser toute la chaîne de l'inscription à la clôture journalière, avec une traçabilité financière totale. Une opération financière validée n'est **jamais** modifiée ni supprimée : toute correction passe par une annulation contrôlée et une contre-écriture (RG09, RG18).

Avant d'ajouter une fonctionnalité, se demander : *est-elle nécessaire pour sécuriser l'inscription, la facturation, l'encaissement ou la clôture d'un élève, sans ressaisie ni incohérence ?* Si non, elle n'est pas prioritaire (cf. §1.2 du cadrage, hors périmètre initial : notes, emplois du temps, cantine, transport, paie, portail parent, mobile money, appli mobile).

## Règles non négociables (voir cadrage §5 pour le détail complet)

- **RG01** — Un élève a un matricule permanent et une seule inscription active par année scolaire.
- **RG02** — Une classe appartient à un niveau, une section et une année scolaire.
- **RG03** — Les frais sont versionnés par année scolaire ; modifier un tarif ne modifie jamais les factures déjà émises.
- **RG08** — Un trop-perçu est interdit (sauf compte d'avance explicitement activé — non activé au MVP).
- **RG09** — Une transaction validée est immuable. L'annulation crée une transaction inverse liée à l'originale.
- **RG10** — Les numéros de reçu sont uniques, séquentiels et générés côté serveur.
- **RG13** — La solvabilité est **calculée**, jamais saisie manuellement.
- **RG15** — Toutes les actions sensibles sont journalisées (utilisateur, horodatage, ancienne/nouvelle valeur) — voir `AuditService` (`apps/api/src/audit/`), append-only par conception.
- **RG16** — Les montants XAF sont des entiers, aucun calcul financier en flottant.
- **RG18** — Aucune suppression physique d'élève, inscription, facture ou transaction en production.
- **Permissions fines, jamais par rôle seul** (§3, §14) : chaque action sensible est gardée par une permission précise (`@RequirePermission('CODE')`), vérifiée côté serveur même pour un appel API direct — masquer un bouton côté client n'est jamais une sécurité.
- **Le Secrétaire-caissier ne valide jamais ses propres annulations, remises exceptionnelles ou écarts de caisse** — seule la Direction approuve (à construire au Lot 4).

## Ordre de développement (cadrage §17, détaillé dans le message de validation du plan)

0. Cadrage — décisions métier, nomenclatures, tarifs, formats de reçus (`DECISIONS_PENDING.md`).
1. **Socle** — auth, utilisateurs/rôles/permissions, années scolaires, structure académique. **FAIT, voir état actuel ci-dessous.**
2. **Élèves, responsables, inscriptions et réinscriptions (sans paiement). FAIT, voir état actuel ci-dessous.**
3. **Tarifs, factures, trois tranches, remises, solvabilité calculée. FAIT, voir état actuel ci-dessous.**
4. **Paiements et reçus (encaissement, réimpression, autres frais). FAIT, voir état actuel ci-dessous — la caisse au sens strict (ouverture/clôture de session, D20-D23) n'est pas construite.**
5. **Sorties financières, clôture de journée (rapport). FAIT partiellement, voir état actuel ci-dessous — rapports/exports plus larges (comptables, rapprochements) non construits.**
6. Import initial, formation, pilote, sauvegarde, mise en production.

**Extension de périmètre « vie scolaire 360° » (21 septembre 2026, décidée par le propriétaire du projet, décision D50 tranchée).** Lots 7 à 14 **proposés** : 7 référentiel pédagogique et personnel, 8 emploi du temps, 9 assiduité des élèves, 10 pointage des enseignants, 11 comptes parents et portail, 12 notifications, 13 messagerie sécurisée et annonces, 14 pilotage 360°. Détail, règles RV01 à RV12 et critères d'acceptation : `docs/Addendum_Cadrage_v1.1_Vie_Scolaire_360.md` ; décisions D50 à D78 : `DECISIONS_PENDING.md` §17. **Tant que ces décisions ne sont pas validées par la Direction, ne coder que ce qui repose sur une valeur paramétrable, jamais une valeur codée en dur** (horaires, seuils de retard, délais, rétention). Le critère de la doctrine ci-dessus (sécuriser l'inscription, la facturation, l'encaissement) reste celui des Lots 1 à 6 ; les Lots 7 et suivants suivent l'addendum, qui ne modifie aucune règle financière (RG08, RG09, RG10, RG16, RG18).

## Stack

Next.js (apps/web, scaffoldé — aucune page métier encore construite, le Lot 1 est backend uniquement, comme l'a été le socle multi-tenant d'Elyon) · NestJS 11 (apps/api) · PostgreSQL 16 / Prisma 6.19.3 (apps/api/prisma/schema.prisma) · Docker Compose (Postgres dev + test uniquement pour l'instant).

**Choix de version Prisma délibéré** : Prisma 7/8-rc introduisent une rupture majeure (plus d'URL dans `schema.prisma`, adapters obligatoires, `prisma.config.ts`) — jugée trop récente et complexe pour ce socle. Rester sur Prisma 6.x (LTS stable) tant qu'une raison concrète n'impose pas la migration.
**`@nestjs/config` et `@nestjs/jwt` fixés en version pré-ESM** (`4.0.4` et `11.0.2` respectivement, `--save-exact`) : leurs versions plus récentes (12.x) publient du pur ESM, incompatible avec la transformation CommonJS de `ts-jest` utilisée par la suite e2e (`Must use import to load ES Module`). Revoir seulement si toute la chaîne de test bascule vers un runner nativement ESM.

## État actuel du scaffold

- **Lot 1 (socle) fait et vérifié** : `apps/api` compile (`npm run build`), démarre (`npm run start:dev`) et sert du trafic réel — vérifié manuellement via `curl` (login réel, création de section réelle, token JWT valide) en plus des tests automatisés.
- `apps/api/prisma/schema.prisma` : uniquement les entités du Lot 1 — `School`, `AcademicYear`, `Section`/`Cycle`/`Level`/`Class`, `User`/`Role`/`Permission`/`RolePermission`, `RefreshToken`, `AuditLog`. Les entités de facturation/paiement/caisse (Lots 3-5) sont **volontairement absentes** : plusieurs règles dont elles dépendent restent `OUVERT` dans `DECISIONS_PENDING.md` (granularité des remises, gestion du trop-perçu, etc.) — les modéliser maintenant risquerait de figer une hypothèse fausse.
- **Mono-établissement, pas multi-tenant** : une seule ligne `School` existe. `schoolId` est présent sur les entités scopées (anticipation d'une extension multi-site future, cadrage §11) mais aucun mécanisme de sélection de tenant n'est construit — `SchoolService.getDefaultId()` (`apps/api/src/school/`) résout systématiquement l'unique établissement. Ne pas confondre avec le modèle multi-tenant d'Elyon (autre projet, header `x-tenant-id`) : ici, il n'y a qu'un seul restaurant... pardon, une seule école.
- **Auth** (`apps/api/src/auth/`) : JWT d'accès court (15 min par défaut, `JWT_ACCESS_TTL`) signé et vérifié côté serveur à chaque requête (`JwtAuthGuard` recharge l'utilisateur + permissions depuis la base à chaque appel, jamais depuis le payload du jeton — un compte désactivé ou un rôle modifié a un effet immédiat). Rafraîchissement via cookie `httpOnly` (`refresh_token`, scope `path=/auth`), rotation à chaque `POST /auth/refresh` (l'ancien jeton est révoqué). Mots de passe hachés Argon2id. Verrouillage après 5 échecs consécutifs (15 min, `MAX_TENTATIVES_CONNEXION`/`DUREE_VERROUILLAGE_MINUTES` dans `auth.service.ts` — décision technique de sécurité, pas une règle métier de l'école, donc non listée dans `DECISIONS_PENDING.md`). Récupération administrée (`PATCH /users/:id/reset-password`, `USER_MANAGE`) : jamais d'auto-inscription ni de lien de réinitialisation par e-mail.
- **Permissions** (`apps/api/src/roles/`) : catalogue de 6 permissions Lot 1 (`SETTINGS_MANAGE`, `USER_MANAGE`, `ROLE_MANAGE`, `ACADEMIC_YEAR_MANAGE`, `ACADEMIC_STRUCTURE_MANAGE`, `AUDIT_LOG_READ`) — **volontairement incomplet** : les codes cités en exemple par le cadrage §3 (`PAYMENT_CREATE`, `PAYMENT_CANCEL_APPROVE`, `CASH_CLOSE`, `EXPENSE_CREATE`) seront ajoutés à `prisma/seed-data.ts` au fur et à mesure que leurs modules (Lots 3-5) seront construits, jamais avant qu'une route ne les vérifie réellement. 5 rôles seedés conformes au tableau du cadrage §3 (`ADMINISTRATEUR`, `DIRECTION`, `SECRETAIRE_CAISSIER`, `COMPTABLE`, `AUDITEUR`) — `SECRETAIRE_CAISSIER`/`COMPTABLE` n'ont encore aucune permission assignée (leur domaine n'existe pas avant les Lots 2-5). `GET /roles`/`GET /permissions` sont lisibles par tout utilisateur authentifié (nécessaire pour peupler un sélecteur de rôle) ; seules les mutations exigent `ROLE_MANAGE`.
- **Structure académique** (`apps/api/src/{sections,cycles,levels,classes}/`) : hiérarchie `Section → Cycle → Level → Class` conforme au modèle recommandé du cadrage §2. Lecture ouverte à tout utilisateur authentifié, écriture derrière `ACADEMIC_STRUCTURE_MANAGE`. `Class.serie`/`Class.groupe` : colonnes prévues par anticipation de la décision D02 (séries de lycée, sous-groupes), non exploitées tant qu'elle reste ouverte.
- **Années scolaires** (`apps/api/src/academic-years/`) : statuts `BROUILLON`/`ACTIVE`/`CLOTUREE`. Une seule année `ACTIVE` à la fois — **activer une nouvelle année échoue explicitement s'il en existe déjà une active** (jamais de bascule automatique silencieuse ; la Direction doit d'abord clôturer l'ancienne). Une année `CLOTUREE` ne peut plus être modifiée.
- **Audit** (`apps/api/src/audit/`) : `AuditService.log()` est **append-only par conception** (aucune méthode de mise à jour/suppression exposée). Sérialise automatiquement les valeurs Prisma (dates comprises) en JSON — jamais besoin de sérialiser soi-même côté appelant. Ne journalise **jamais** un hash de mot de passe, ni avant ni après une réinitialisation. `GET /audit-logs` derrière `AUDIT_LOG_READ`, filtrable par utilisateur/entité/période.
- **Établissement** (`apps/api/src/school/`) : `GET /school` public à tout utilisateur connecté, `PATCH /school` derrière `SETTINGS_MANAGE`. `devise`/`fuseauHoraire` fixés par les exigences non fonctionnelles du cadrage (XAF, Africa/Brazzaville) — pas exposés en modification tant qu'aucun besoin réel de les changer n'apparaît.
- **Tests** : 39 tests e2e (Jest + Supertest, `apps/api/test/*.e2e-spec.ts`) contre une base Postgres dédiée (`shakespeare_academy_test`, jamais la base de dev) — auth (login/lockout/refresh/logout/changement de mot de passe), permissions fines (CA14 : refus serveur direct sans la permission requise), années scolaires (règle d'unicité de l'année active), structure académique (hiérarchie, doublons, filtres), utilisateurs (création/désactivation/réinitialisation), rôles/permissions (catalogue, assignation), audit (journalisation, jamais de hash), établissement. **Suite e2e exécutée avec `--runInBand`** (obligatoire : plusieurs fichiers de test partagent la même base dédiée, une exécution parallèle provoque des collisions de clés uniques/FK entre suites). Fixtures partagées : `prisma/seed-data.ts` (référentiel rôles/permissions/admin, réutilisé par le vrai script de seed **et** par les tests, pour ne jamais faire diverger les deux) + `test/utils/` (`test-app.ts`, `clean-database.ts`, `fixtures.ts`).
- **Seed** (`apps/api/prisma/seed.ts`, `npm run db:seed`) : idempotent, crée l'établissement (nom par défaut "Shakespeare Academy", à corriger via `PATCH /school` avec le vrai nom/adresse une fois connus), les 5 rôles, les 6 permissions Lot 1, et un compte `ADMINISTRATEUR` (`admin@shakespeareacademy.cg` / `ChangeMe123!` par défaut, **à changer immédiatement en production** — `doitChangerMotDePasse: true` dès la création, `ADMIN_SEED_EMAIL`/`ADMIN_SEED_PASSWORD` pour surcharger).
- **Lot 2 (élèves, responsables, inscriptions) fait et vérifié** : mêmes garanties que le Lot 1 (build/lint propres, e2e verts, vérification manuelle réelle via `curl`).
- `NumberSequence` (`apps/api/src/common/number-sequence.service.ts`) : séquence atomique générique (`schoolId`, `type`, `scope`), un seul `upsert` (INSERT ... ON CONFLICT côté Postgres) donc atomique sous accès concurrent sans verrou explicite. Utilisée pour le matricule (`type: 'MATRICULE'`, `scope` vide — permanent, D32) et le numéro d'inscription (`type: 'ENROLLMENT'`, `scope: academicYearId` — par année). Réutilisable telle quelle pour les numéros de reçu/pièce du Lot 4 (RG10).
- **Matricule** : généré automatiquement si non fourni, format `000001` (6 chiffres, sans année — D32 corrigé en cours de Lot 2 : un premier brouillon de `DECISIONS_PENDING.md` proposait un format incluant l'année, qui aurait contredit la permanence du matricule déjà actée dans la même section). **Numéro d'inscription** : `{libellé année}-{5 chiffres}` (ex. `2026-2027-00001`), par année — jamais confondu avec le matricule, qui lui ne change jamais.
- **Élèves/responsables** (`apps/api/src/students/`) : `POST /students` crée l'élève et son premier responsable dans une même transaction Prisma (cadrage §12), conforme au parcours §4.1. Un responsable est retrouvé-ou-créé par téléphone au sein de l'établissement (`Guardian.@@unique([schoolId, telephone])`, même patron R9 qu'Elyon) — un même parent n'est jamais dupliqué pour un deuxième enfant. `POST /students/:id/guardians`/`PATCH /students/guardians/:id`/`DELETE /students/:id/guardians/:guardianId` gèrent les responsables au-delà de la création initiale (fratrie, tuteur additionnel) — le lien `StudentGuardian` peut être détaché sans que RG18 s'applique (ce n'est ni un élève, ni une inscription, ni une facture, ni une transaction).
- **D33 (détection de doublon) implémentée** : `POST /students` compare nom+prénom+date de naissance normalisés (accents/casse ignorés) aux élèves déjà connus. Une correspondance exacte renvoie 409 avec le doublon potentiel dans le corps de la réponse, sauf si `forcerCreation: true` est explicitement passé — alerte non bloquante, jamais un blocage définitif ni une fusion automatique (l'écran de fusion reste différé, comme documenté dans D33).
- **`GET /students/search`** (cadrage §4.1/§12) : matricule, nom/prénom (normalisés), téléphone d'un responsable, ou date de naissance exacte (format `AAAA-MM-JJ`).
- **Inscriptions/réinscriptions** (`apps/api/src/enrollments/`) : `POST /enrollments` couvre les deux cas du cadrage §4.1/§4.2 avec un seul endpoint — `type` (`INSCRIPTION`/`REINSCRIPTION`) est **toujours dérivé côté serveur** (REINSCRIPTION si l'élève a au moins une inscription antérieure, quel que soit son statut), jamais choisi par le client. RG01 (une seule inscription **active** par élève et par année) appliqué en application, pas en contrainte DB : une inscription annulée ne bloque pas une nouvelle inscription pour la même année (correction possible sans contrainte artificielle). Rejette une classe qui n'appartient pas à l'année scolaire indiquée (400) et toute inscription dans une année `CLOTUREE` (409). `POST /enrollments/:id/cancel` exige un motif, journalisé (RG15) — pas d'approbation Direction à ce stade (aucune facture n'y est encore rattachée, contrairement à une annulation de paiement au Lot 4).
- **Alerte impayés antérieurs (§4.2) volontairement absente** : elle suppose des factures/paiements qui n'existent pas avant le Lot 3/4. L'implémenter maintenant aurait exigé d'inventer une donnée financière — reportée telle quelle, sans placeholder trompeur (ex. toujours "aucun impayé").
- **Permissions Lot 2** : `STUDENT_READ` (lecture élèves/responsables/inscriptions — Administrateur, Direction, Secrétaire-caissier, Auditeur) et `ENROLLMENT_MANAGE` (création/modification élève, responsable, inscription, annulation — Administrateur, Secrétaire-caissier uniquement), une seule permission de gestion plutôt qu'une par sous-domaine (le cadrage §3 regroupe ces activités pour le même rôle). Comptable n'a accès à aucune des deux (son périmètre, sorties/rapports, arrive au Lot 5).
- **Tests** : 57 tests e2e au total (+18 pour le Lot 2 : création élève+responsable en transaction, matricules séquentiels, réutilisation d'un responsable partagé, D33 avec/sans `forcerCreation`, recherche, dossier élève, rattachement/détachement de responsable, permissions STUDENT_READ/ENROLLMENT_MANAGE, inscription/réinscription, RG01, classe hors année, année clôturée, annulation avec motif + audit, filtres).
- **Frontend des Lots 1 à 5 fait et vérifié manuellement dans le navigateur** (`apps/web`) : App Router, Tailwind v4. Aucune page pour le Lot 6 (import initial, pilote, sauvegarde) — n'existe pas encore côté backend.
- **Refonte du design (16 septembre 2026)** — l'interface initiale (classes Tailwind par défaut, gris/bleu générique) a été jugée insuffisamment soignée ; refonte complète vers un système de design propre plutôt qu'un simple ravalement de couleurs. `app/globals.css` définit une palette indigo/or via le bloc `@theme` de Tailwind v4 (génère automatiquement les classes `bg-primary`, `text-ink-muted`, etc. — jamais de couleur Tailwind par défaut du type `slate-500` dans le code applicatif désormais), `color-scheme: light` explicite sur `:root` (empêche le navigateur d'appliquer ses styles sombres natifs aux champs/boutons selon le thème système). Police `Fraunces` (`next/font/google`, `app/layout.tsx`, variable `--font-display`) pour les titres, en plus des `Geist Sans`/`Geist Mono` déjà en place. `components/ui.tsx` réécrit en bibliothèque de composants partagés (`Card`, `Button` à 5 variantes, `Input`/`Select`/`Field`, `Badge`, `StatCard`, `EmptyState`, `Stepper`) — remplace le style inline dupliqué page par page. `components/app-shell.tsx` (remplace `app-header.tsx`, supprimé) : sidebar fixe à gauche en desktop, tiroir coulissant avec hamburger en mobile (`lucide-react`, nouvelle dépendance, pour les icônes). Migration mécanique des classes `slate-*` vers les tokens du thème sur toutes les pages existantes (script Node ponctuel, non conservé). Toutes les pages Lot 1/2/3 vérifiées dans le navigateur en desktop (1280px) et mobile (375px), y compris avec `prefers-color-scheme: dark` émulé (aucune page n'a de variante sombre dédiée — `color-scheme: light` garantit qu'aucune ne casse pour autant).
- **Bug frontend trouvé et corrigé pendant cette refonte** : `lib/api.ts` traitait tout 401 (y compris depuis `/auth/login`/`/auth/refresh` eux-mêmes) comme "session expirée, tenter un rafraîchissement" — un simple mauvais mot de passe déclenchait un rafraîchissement (qui échoue, aucune session valide) puis affichait "Session expirée, veuillez vous reconnecter." au lieu du vrai message serveur "Identifiant ou mot de passe incorrect.". Corrigé via une liste `AUTH_ENTRY_POINTS` (`/auth/login`, `/auth/refresh`) exclue de cette logique — ces deux routes affichent toujours leur message réel.
- **Polish visuel des pages restantes fait (16 septembre 2026)** — le premier passage de la refonte n'avait donné un traitement bespoke qu'à Login/Dashboard/AppShell ; les autres pages n'avaient reçu que la migration mécanique des classes `slate-*`. Complété sur Élèves (liste), dossier élève, Structure académique, Utilisateurs & rôles, Journal d'audit, Établissement et Années scolaires : avatars à initiales (élèves, utilisateurs), icônes `lucide-react` par section, badges colorés sémantiquement (ex. `actionColor()` sur le journal d'audit — vert pour *_CREATE/APPROVE/REQUEST, bleu pour *_UPDATE, orange pour *_CANCEL/REJECT, rouge pour *_DELETE), `EmptyState` illustré partout où une liste peut être vide, étapes numérotées en badges circulaires sur Structure académique (Section→Cycle→Niveau→Classe). **Vue en cartes sur mobile** (`sm:hidden`/`hidden sm:block`) en plus du tableau desktop existant, pour Élèves et Utilisateurs — les tableaux HTML restent illisibles sur un écran de téléphone même avec un simple `overflow-x-auto`, une vraie disposition alternative était nécessaire pour l'exigence "compatible mobile". Aucun changement de comportement, purement présentationnel — vérifié dans le navigateur (élève de test créé/consulté/nettoyé).
- **Auth frontend** (`apps/web/lib/api.ts`, `apps/web/contexts/auth-context.tsx`) : le jeton d'accès ne vit **qu'en mémoire** (jamais `localStorage` ni cookie non-httpOnly), cohérent avec le choix côté API — au chargement de la page, un rafraîchissement silencieux via `POST /auth/refresh` (cookie httpOnly) restaure la session sans redemander le mot de passe. `api.ts` retente automatiquement une requête ayant reçu 401 après un rafraîchissement réussi ; sinon, redirige vers `/login`. `doitChangerMotDePasse` (renvoyé par `/auth/login`, pas par `/auth/me`) redirige vers `/changer-mot-de-passe` avant tout accès au tableau de bord.
- **Permissions côté client** : `useAuth().hasPermission(code)` masque des boutons/liens pour l'ergonomie uniquement (ex. nav "Utilisateurs & rôles" visible seulement avec `USER_MANAGE`) — **jamais un contrôle de sécurité**, le serveur revérifie systématiquement (§14, déjà appliqué côté API). Une page consultée sans la permission de mutation reste lisible avec ses champs désactivés plutôt que masquée entièrement (ex. `/parametres/etablissement` sans `SETTINGS_MANAGE`).
- **Écrans Lot 1** : connexion, changement de mot de passe forcé, tableau de bord (année active + effectif + raccourcis), établissement, années scolaires (création/activation/clôture), structure académique (Section→Cycle→Niveau→Classe en 4 colonnes avec sélection en cascade), utilisateurs & rôles (deux onglets : création/désactivation/réinitialisation de mot de passe ; permissions par rôle sous forme de pastilles cliquables), journal d'audit (liste filtrable par objet).
- **Écrans Lot 2** : liste élèves avec recherche (debounce 300ms sur `GET /students/search`), dossier élève (identité, responsables avec ajout/détachement, parcours annuel avec annulation d'inscription), et un **assistant d'inscription unique** (`/eleves/inscription`) couvrant à la fois la première inscription et la réinscription — reflète exactement la conception backend (`POST /enrollments` dérive `type` automatiquement) : onglet "Élève déjà connu" (recherche) vs "Nouvel élève" (formulaire avec responsable), puis sélection année/section/cycle/niveau/classe en cascade, puis confirmation. La détection de doublon (D33) y est gérée : un 409 avec `doublonPotentiel` affiche une alerte avec un bouton explicite « Ce n'est pas un doublon, créer quand même » (jamais un contournement silencieux).
- **Bug réel trouvé et corrigé en vérifiant dans le navigateur** : sur `/parametres/structure`, créer une classe renvoyait bien 201 côté serveur mais n'apparaissait pas dans la liste — `loadAll()` (rechargement après création) ne rafraîchissait jamais `classes`, qui dépend d'un `useEffect` séparé keyé sur `[selectedLevelId, selectedYearId]` (des valeurs inchangées par un simple ajout). Corrigé en extrayant `loadClasses()` et en l'appelant explicitement après chaque création, en plus de `loadAll()`.
- **Contrainte d'environnement de vérification (pas un bug du projet)** : dans cette session, le port 3000 par défaut du serveur de développement Next.js renvoyait une réponse HTML corrompue/mise en cache pour toutes les requêtes de chunks JS (`Uncaught SyntaxError: Unexpected token '<'`) au sein du navigateur intégré de l'outil — reproductible avec Turbopack et webpack, mais absent en `curl` direct depuis la machine, et absent après avoir changé le port du serveur web (3010). Cause isolée à l'infrastructure de la session d'outillage (probablement un cache de proxy propre à ce port dans cet environnement), pas au code : aucun changement de configuration n'a été fait dans le projet pour cette raison (le port 3000 reste la valeur par défaut normale de `next dev`).
- Voir `README.md` (racine) pour les instructions d'installation/lancement/tests.

## Lot 3 — tarifs, factures, remises, solvabilité (16 septembre 2026)

**Fait et vérifié** : schéma migré, 18 tests e2e (75 au total, tous verts), build/lint propres, vérification manuelle réelle dans le navigateur (données créées via l'API, financial-status/discount/approve testés en direct, données de test nettoyées après coup).

- **Modèles** (`apps/api/prisma/schema.prisma`) : `FeeType` (code, nom, `obligatoire`, `avecTranches`), `FeeSchedule` (par `academicYearId`+`levelId`+`feeTypeId`, unique) avec soit `montant` (frais à montant unique) soit des `InstallmentSchedule` enfants (frais en tranches) — **jamais les deux** : `FeeSchedulesService` refuse la création si `montant`/`installments` ne correspondent pas exactement à `FeeType.avecTranches`. `Invoice` (une par `Enrollment`, `@unique`, statut `EMISE`/`ANNULEE`) et `InvoiceLine` (RG03 : `montant`/`libelle` sont un **snapshot** figé à la génération, jamais recalculés si la grille tarifaire change ensuite). `Discount` (par `InvoiceLine`, type `MONTANT_FIXE`/`POURCENTAGE`, statut `EN_ATTENTE`/`APPROUVEE`/`REJETEE`, auteur + approbateur). `School.seuilImpayeCritiqueFcfa` (`Int?`, D40) : seuil configurable au-delà duquel `EN_RETARD` devient `IMPAYE_CRITIQUE` — `null` par défaut, jamais inventé.
- **Génération automatique de facture à l'inscription** (`EnrollmentsService.create()`/`cancel()`, `InvoicesService`) : la création d'une `Enrollment` et la génération de sa facture sont dans **la même transaction Prisma** (CA02 : jamais une inscription sans facture, même vide) — pour chaque `FeeSchedule` dont `FeeType.obligatoire = true` correspondant au (niveau, année) de la classe, une ligne par tranche (si `avecTranches`) ou une ligne unique (sinon). **Un frais facultatif (`obligatoire = false`) n'est jamais facturé automatiquement** — testé explicitement. Annuler une inscription annule sa facture dans la même transaction (`InvoicesService.cancelForEnrollment`).
- **D39 résolue (17 septembre 2026)** : `FeeType.appliesTo` (`TOUS`/`INSCRIPTION`/`REINSCRIPTION`, `TOUS` par défaut) — cas réel confirmé par l'école : "Frais d'inscription" (45 000 XAF) réservé aux nouveaux élèves, "Frais de réinscription" (60 000 XAF) réservé aux élèves déjà connus, jamais les deux à la fois. `InvoicesService.generateForEnrollment()` ne retient que les `FeeSchedule` dont le `FeeType` correspond à `TOUS` ou au type exact de l'inscription. Testé explicitement (`fees-and-invoices.e2e-spec.ts`), 76 tests e2e au total.
- **Bug réel trouvé et corrigé (17 septembre 2026)** : sur `/parametres/structure`, après la création d'une section/d'un cycle/d'un niveau, rien ne sélectionnait automatiquement l'élément créé — la colonne suivante restait bloquée sur "Sélectionnez une section/un cycle/un niveau" jusqu'à ce que l'utilisateur reclique dessus, sans aucun indice que c'était nécessaire (signalé par l'utilisateur : "on ne peut pas ajouter un cycle, un niveau et une classe" — confirmé en observant zéro cycle/niveau/classe créés en production malgré deux sections déjà présentes). Corrigé par une sélection automatique de l'élément fraîchement créé à chaque étape. Deuxième blocage au même endroit : le formulaire de création de classe disparaissait silencieusement sans aucune année scolaire créée, sans explication — remplacé par un message explicite avec lien direct vers "Années scolaires".
- **Remises** (`apps/api/src/discounts/`) : `POST /discounts` (permission `ENROLLMENT_MANAGE`, comme la création d'inscription) crée toujours une remise `EN_ATTENTE` — **aucun seuil d'auto-approbation** (D17, provisoire, seuil = 0). `POST /discounts/:id/approve|reject` exigent `DISCOUNT_APPROVE`, **attribuée à Direction uniquement** (D19 — Administrateur a `FEE_MANAGE` mais pas `DISCOUNT_APPROVE`, testé explicitement en 403). `computeApprovedDiscountAmount()` (`discounts/discount-amount.util.ts`) : seules les remises `APPROUVEE` réduisent le solde d'une ligne, toujours plafonné au montant de la ligne (jamais négatif), réutilisé à la fois par `DiscountsService` et `FinancialStatusService`.
- **Statut de solvabilité** (`apps/api/src/financial-status/`, `GET /students/:id/financial-status`, cahier §6) : calculé, **jamais saisi**. `montantPaye` (mis à jour au Lot 4, 17 septembre 2026) somme désormais les vrais `Payment` `VALIDE` de chaque ligne — un paiement `ANNULE` (RG09) n'est jamais compté. Statuts implémentés : `SOLVABLE` (rien facturé, ou tout couvert par une remise sans être `EXONERE`), `A_ECHOIR` (solde futur, aucune échéance passée), `EN_RETARD` (au moins une échéance passée non couverte), `IMPAYE_CRITIQUE` (`EN_RETARD` **et** montant exigible au-dessus de `School.seuilImpayeCritiqueFcfa`, D40), `EXONERE` (remises approuvées couvrant 100 % du montant facturé). Une facture `ANNULEE` n'entre jamais dans le calcul. `prochaineEcheance` et `lignesEnRetard` exposés pour l'explicabilité (jamais une simple couleur sans détail, cahier §6).
- **Permissions Lot 3** : `FEE_MANAGE` (Administrateur uniquement — configuration tarifaire) et `DISCOUNT_APPROVE` (Direction uniquement — D19). Lecture des types de frais/grilles tarifaires/factures ouverte à tout utilisateur authentifié (comme la structure académique), écriture derrière les permissions ci-dessus.
- **Frontend** : `/tarifs` (nav "Tarifs & facturation", `FEE_MANAGE` pour les formulaires de création — lecture seule sinon) — panneau "Types de frais" (création + liste avec badges Obligatoire/Tranches) et panneau "Grilles tarifaires" (filtrable par année/niveau, formulaire dynamique : champ montant unique ou lignes de tranches selon le type de frais choisi). `components/financial-status-card.tsx` (intégré au dossier élève, `/eleves/[id]`) : badge de statut coloré, quatre indicateurs (facturé/remises/payé/restant), prochaine échéance, lignes de la facture de l'inscription active avec bouton "Demander une remise" (`ENROLLMENT_MANAGE`) et boutons "Approuver"/"Rejeter" sur une remise `EN_ATTENTE` (`DISCOUNT_APPROVE`) — vérifié en direct dans le navigateur (demande créée, approuvée, montants recalculés immédiatement sans rechargement de page).
- **Tests** : `test/fees-and-invoices.e2e-spec.ts`, 18 cas — types de frais (création, doublon de code, 403 sans `FEE_MANAGE`), grilles tarifaires (montant unique, tranches, incohérence montant/tranches rejetée), génération de facture (montant unique, tranches, frais facultatif jamais facturé, annulation d'inscription annule la facture), remises (création/approbation/rejet, double approbation refusée en 409, 403 pour Administrateur), solvabilité (SOLVABLE sans facture, EN_RETARD sur frais immédiatement exigible, A_ECHOIR sur échéance future, EXONERE à 100% de remise, facture annulée exclue du calcul). `test/utils/clean-database.ts` étendu (discount/invoiceLine/invoice/installmentSchedule/feeSchedule/feeType, enfants avant parents).

## Lot 4 — paiements et reçus (17 septembre 2026)

**Fait et vérifié** : schéma migré, 12 nouveaux tests e2e (88 au total, tous verts), build/lint propres, vérifié manuellement de bout en bout dans le navigateur (paiement partiel, solde, reçu imprimé, réimpression, données de test nettoyées après coup). Demande explicite de l'utilisateur : "payer les frais d'inscription et les frais de scolarité, générer un reçu pour chaque opération, pouvoir réimprimer un reçu plus tard" — la caisse (ouverture/clôture de session, D20-D23) n'a **pas** été construite, hors du périmètre de cette demande.

- **D41 (nouvelle décision, simplification délibérée)** : un `Payment` cible **toujours une seule** `InvoiceLine`, jamais réparti sur plusieurs — le montant est systématiquement plafonné au solde restant de cette ligne (`montant - remises approuvées - paiements VALIDE déjà encaissés`). Ce choix rend un trop-perçu (RG08) structurellement impossible sans compte d'avance (D11, toujours non activé) et évite tout le débat D12/D13 sur la ventilation multi-lignes — un parent qui paie l'inscription ET une tranche de pension fait simplement deux paiements distincts, chacun avec son propre reçu (exactement ce que l'utilisateur a demandé : "pour chaque opération, générer un reçu").
- **Modèle** (`apps/api/prisma/schema.prisma`) : `Payment` (`invoiceLineId`, `montant`, `modePaiement` `ESPECES`/`MOBILE_MONEY`, `referenceExterne` optionnelle — requise si `MOBILE_MONEY`, cadrage §11 déjà tranché : jamais une intégration API automatisée, un simple enregistrement manuel avec référence), `numeroRecu` (unique par école), `statut` `VALIDE`/`ANNULE`, `motifAnnulation`/`annuleParUserId` — **RG09/RG18 : un paiement `VALIDE` n'est jamais modifié ni supprimé**, une correction passe par `ANNULE` (motif obligatoire), le solde de la ligne se recalcule alors automatiquement en excluant ce paiement (aucune écriture inverse séparée à créer, le recalcul suffit).
- **`PaymentsService`** (`apps/api/src/payments/`) : `POST /payments` (`PAYMENT_CREATE`) refuse tout montant `<= 0`, tout montant dépassant le solde restant (400), et tout paiement sur une ligne déjà soldée (409) ou une facture `ANNULEE` (409). Numéro de reçu généré via `NumberSequenceService` (`type: 'RECEIPT'`, série continue par école, format `REC-000001` — D28). `POST /payments/:id/cancel` exige `PAYMENT_CANCEL_APPROVE`, **Direction uniquement** (même principe que `DISCOUNT_APPROVE`/D19 — Administrateur peut encaisser mais pas annuler, testé explicitement en 403). `POST /payments/:id/reprint` journalise `PAYMENT_REPRINT` (D30, RG15) et renvoie les mêmes données de reçu — l'émission initiale (`PAYMENT_CREATE`) n'est jamais comptée comme une réimpression.
- **Permissions Lot 4** : `PAYMENT_CREATE` (Administrateur + Secrétaire-caissier — encaissement ET réimpression, même geste opérationnel cahier §3) et `PAYMENT_CANCEL_APPROVE` (Direction uniquement). Codes repris tels quels du cahier §3 (déjà cités en exemple dans ce fichier avant même que le module existe).
- **`FinancialStatusService` mis à jour** : `montantPaye` n'est plus figé à 0 (voir plus haut) — chaque ligne inclut désormais ses paiements `VALIDE`, le solde par ligne (et donc `montantRestant`/le statut global) reflète les vrais encaissements.
- **Frontend** : `components/financial-status-card.tsx` étendu — chaque ligne de facture affiche son solde restant réel et un bouton "Payer" (`PAYMENT_CREATE`) ouvrant un formulaire inline (montant pré-rempli au solde restant, mode de paiement, référence externe si Mobile Money) ; section "Historique des paiements" listant chaque reçu avec boutons "Réimprimer" (`PAYMENT_CREATE`) et "Annuler" (`PAYMENT_CANCEL_APPROVE`, si `VALIDE`). Nouvelle page `/recus/[id]` (hors du groupe `(dashboard)`, sans le chrome `AppShell`) : reçu imprimable (école, numéro, élève, motif, montant, mode, caissier ; bandeau "REÇU ANNULÉ" si le paiement l'est), bouton "Imprimer" (`window.print()`, classes `print:` Tailwind pour masquer les boutons à l'impression) et lien "Retour au dossier".
  **Bug réel trouvé et corrigé en vérifiant dans le navigateur** : le premier jet ouvrait le reçu via `window.open(url, "_blank")` — un nouvel onglet n'a **jamais** le jeton d'accès (qui ne vit qu'en mémoire JS de l'onglet d'origine, choix de sécurité délibéré, voir "Auth frontend" plus haut), donc le nouvel onglet affichait systématiquement l'écran de connexion, et se retrouvait sur le tableau de bord après connexion — jamais sur le reçu attendu. Corrigé en remplaçant par une navigation client `router.push()` dans le **même onglet** (le jeton reste disponible), avec un lien "Retour au dossier" pour revenir ensuite — testé de bout en bout (paiement → reçu affiché → retour → second paiement soldant la ligne → réimpression du premier reçu, tout fonctionnel sans perte de session).
- **Tests** : `test/payments.e2e-spec.ts`, 12 cas — encaissement complet avec numéro de reçu séquentiel, paiement partiel (D10) puis complément soldant la ligne, refus au-delà du solde (400), Mobile Money sans référence refusé (400), 403 sans `PAYMENT_CREATE`, annulation par Direction avec libération du solde, 403 pour Administrateur sur l'annulation, refus de double annulation (409), réimpression journalisée (D30), refus d'encaisser sur une facture annulée (409), `financial-status` reflétant un vrai paiement (passage à `SOLVABLE`), listes par élève/par ligne.
- **Reçu enrichi (17 septembre 2026, demande explicite)** : logo de l'établissement (`School.logoUrl`, upload `POST /school/logo` — `multer`/`diskStorage`, JPEG/PNG/WebP 5 Mo max, servi statiquement via `app.useStaticAssets` dans `main.ts`, `NestExpressApplication` — fichiers dans `apps/api/uploads/`, volume Docker nommé `academy_uploads` pour survivre à une recréation de conteneur, jamais dans l'image), montant en toutes lettres (`apps/web/lib/number-to-words-fr.ts`, règles de pluriel françaises correctes sur "cent"/"vingt" — ne prennent un "s" que si rien de plus petit ne suit dans le nombre entier, ex. "trois cent mille" jamais "trois cents mille"), emplacements "Signature du caissier"/"Cachet de la Direction" (zones vides, aucune signature électronique). `process.cwd()` utilisé pour les chemins d'upload plutôt que `__dirname` — ce dernier diffère entre dev (`ts-node` depuis `src/`) et production (`dist/src/`), piège identifié avant de casser quoi que ce soit.
- **Correction de saisie sur une inscription (17 septembre 2026, demande explicite : "si on a fait une erreur sur le nom de l'élève ou la classe, on doit pouvoir modifier")** : `PATCH /students/:id` existait déjà côté API (Lot 2) mais n'avait pas de formulaire — ajouté dans `/eleves/[id]` (bouton "Modifier" sur la carte Identité). Nouveau `PATCH /enrollments/:id/change-class` (D43) : corrige la classe d'une inscription `ACTIVE`, refuse explicitement un changement vers un **niveau différent** (400 — les `InvoiceLine` déjà émises sont un snapshot lié au niveau, RG03) ; un vrai changement de niveau reste annulation + nouvelle inscription. `Enrollment.class` expose désormais `levelId` (`ENROLLMENT_INCLUDE`) pour que le frontend sache proposer les classes-sœurs du même niveau.
- **"Autres frais" — module de facturation ponctuelle (17 septembre 2026, demande explicite : tenues, livres, cantine, autres)** : `POST /invoices/:id/lines` (`ENROLLMENT_MANAGE`) ajoute une `InvoiceLine` manuelle à une facture déjà émise, pour un `FeeType` sans tranches (`avecTranches=true` refusé en 400 — les tranches passent toujours par `/fee-schedules`). Aucun nouveau modèle : les types de frais facultatifs (tenue, cantine…) se créent normalement via `/tarifs` (`FEE_MANAGE`, `obligatoire=false`), cette route ne fait qu'y rattacher un montant ponctuel à UN élève précis. Une fois la ligne créée, le paiement/reçu réutilise exactement le flux Lot 4 existant. Bouton "Autre frais" dans `financial-status-card.tsx`.

## Lot 5 — sorties financières, clôture de journée, élèves insolvables (17 septembre 2026)

**Fait et vérifié** : schéma migré, 11 nouveaux tests e2e (106 au total, tous verts), build/lint propres, vérifié manuellement de bout en bout dans le navigateur (sortie enregistrée → approuvée par Direction → apparaît dans la clôture du jour ; élève avec échéance non couverte listé comme insolvable). **La caisse au sens strict (session d'ouverture/clôture avec fonds initial, D20-D23) n'a pas été construite** — voir D44 : la "clôture de journée" est un rapport recalculé à la demande, pas une session figée.

- **Sorties financières** (`apps/api/src/expenses/`) : `Expense` (catégorie, montant, description, `dateDepense`, statut `EN_ATTENTE`/`APPROUVEE`/`REJETEE`). Catégories confirmées par l'école (D24) : Versement banque, Paiement salaire, Paiement facture, Achat matériel, Autre — `EXPENSE_CATEGORIES` (`expense-categories.ts`), une simple liste validée côté DTO plutôt qu'un enum Postgres, pour rester extensible sans migration. `POST /expenses` (`EXPENSE_CREATE` — Administrateur + Comptable, cadrage §3 : "Comptable : sorties, rapports...", jusqu'ici sans aucune permission réelle) crée toujours `EN_ATTENTE` ; `POST /expenses/:id/approve|reject` exigent `EXPENSE_APPROVE` (Direction uniquement, D25 — même principe que `DISCOUNT_APPROVE`/`PAYMENT_CANCEL_APPROVE`, seuil de validation = 0, provisoire). Écran `/depenses`.
- **Clôture de journée** (`apps/api/src/reports/`, `GET /reports/cash-closing?date=`, `CASH_CLOSE`) : recalculée à chaque appel (jamais une valeur stockée) — entrées = `Payment` `VALIDE` du jour, sorties = `Expense` `APPROUVEE` du jour (une dépense encore `EN_ATTENTE` n'est jamais comptée), solde du jour, et solde cumulé (toutes entrées - toutes sorties depuis le début, jusqu'à la fin du jour sélectionné). Écran `/cloture` avec sélecteur de date (par défaut aujourd'hui), quatre indicateurs + détail des mouvements du jour.
- **Élèves insolvables** (`GET /reports/insolvent-students`, `STUDENT_READ`) : parcourt tous les élèves `ACTIF`, réutilise `FinancialStatusService.getForStudent()` (même logique que le dossier individuel, jamais un statut recalculé différemment), filtre `EN_RETARD`/`IMPAYE_CRITIQUE`, trié par montant exigible décroissant. Complexité N+1 assumée (effectif d'une école pilote). Écran `/insolvables` avec contact du responsable prioritaire (lien `tel:`).
- **Permissions Lot 5** : `EXPENSE_CREATE` (Administrateur, Comptable), `EXPENSE_APPROVE` (Direction), `CASH_CLOSE` (Administrateur, Direction, Secrétaire-caissier, Comptable — accès en lecture aux rapports financiers). Comptable a enfin de vraies permissions (`STUDENT_READ`, `EXPENSE_CREATE`, `CASH_CLOSE`) après être resté vide depuis le Lot 1.
- **Tests** : `test/expenses.e2e-spec.ts` (6 cas) et `test/reports.e2e-spec.ts` (5 cas) — création/approbation/rejet d'une sortie, catégorie hors liste refusée (400), 403 pour Administrateur sur l'approbation, refus de double approbation (409), filtre par statut ; clôture additionnant entrées/sorties/solde correctement, dépense `EN_ATTENTE` exclue, 403 sans `CASH_CLOSE` ; élève avec frais exigible non couvert listé comme insolvable, élève entièrement payé absent de la liste.

## Déploiement production — académy.lobima.online (12 septembre 2026)

**Déployé et vérifié en conditions réelles** : `https://academy.lobima.online` (web) et `https://api-academy.lobima.online` (API), sur le **même VPS qu'Elyon** (autre projet du même opérateur, `elyon-restaurant-mvp`), à la demande explicite de l'utilisateur — pas un choix d'architecture pris seul.

- **Cohabitation avec Elyon sur un VPS partagé** : Shakespeare Academy vit dans son propre répertoire (`/docker/shakespeare-academy` sur le VPS) avec son propre `docker-compose.yml` (nom de projet `shakespeare-academy`, réseau `shakespeare-academy_default`, base Postgres dédiée `academy-db`) — **jamais géré par le `docker-compose.yml` d'Elyon**. Seuls `academy-api`/`academy-web` rejoignent *en plus* le réseau Docker d'Elyon (`elyon-restaurant-mvp_default`, déclaré `external: true`) pour être joignables par son Caddy déjà en place sur les ports 80/443 — aucun deuxième Caddy possible sur les mêmes ports.
- **Piège réel rencontré en déployant, pas anticipé** : `academy-db` s'appelait initialement `db` — mais Elyon a **aussi** un service nommé `db`. Un conteneur connecté à deux réseaux Docker où le même alias existe sur chacun résout ce nom de façon ambiguë : `academy-api` s'est retrouvé à résoudre "db" vers la base **d'Elyon** (échec d'authentification, mauvais réseau) plutôt que la sienne. Corrigé en renommant le service `db` → `academy-db` (jamais un nom générique pour un service qui vit sur un réseau partagé avec un autre projet).
- **Deux autres bugs réels trouvés et corrigés pendant ce déploiement** :
  1. Le point d'entrée du Dockerfile (`node dist/main.js`, copié du patron Elyon) était faux pour ce projet : `tsconfig.json` ne fixe pas `rootDir`, et la compilation couvre à la fois `src/` et `prisma/` (à cause de `seed.ts`) — tsc calcule alors une racine commune et préserve `src/` sous `dist/`. Le vrai point d'entrée est `dist/src/main.js`. Puisque `dist/prisma/seed.js` est lui aussi déjà compilé (mêmes dépendances `@prisma/client`/`argon2`, déjà en `dependencies` de production), la production peut lancer le seed via `node dist/prisma/seed.js` sans jamais avoir besoin de `ts-node` (resté en devDependency, absent de l'image de production).
  2. `docker-compose.yml` positionne les variables optionnelles à une chaîne vide (`${ADMIN_SEED_EMAIL:-}`) quand elles sont absentes de `.env.production` — jamais réellement *absentes* du `process.env` du conteneur. `seed-data.ts` utilisait `options.adminEmail ?? 'valeur par défaut'` : `??` ne se déclenche que sur `null`/`undefined`, jamais sur `''`. Un premier compte admin a donc été créé avec un email **et un mot de passe vides**, invisible tant que personne ne testait un vrai login. Corrigé dans `prisma/seed.ts` (`process.env.ADMIN_SEED_EMAIL || undefined`, jamais la valeur brute) — potentiellement le même piège pour toute variable optionnelle future lue via `??` plutôt que `||`.
- **Structure des fichiers Compose** : `docker-compose.yml` est sûr par défaut (aucun port publié sur `academy-db`, pas de montage lié aux tests e2e) — utilisable tel quel aussi bien en dev qu'en production. `docker-compose.override.yml.example` (copié en `docker-compose.override.yml`, gitignored) republie le port Postgres (`5544`) et monte `init-test-db.sql` **uniquement** pour le développement local — jamais présent sur le VPS. Même patron que `docker-compose.override.yml.example` côté Elyon.
- **Secrets** : `.env.production` (jamais commité, sur le VPS uniquement) contient `POSTGRES_PASSWORD`/`JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` générés aléatoirement (`openssl rand -hex 32`), jamais des valeurs par défaut. Voir `.env.production.example` à la racine pour la liste complète.
- **Caddy** : deux blocs ajoutés à la suite du `Caddyfile` **d'Elyon** (`/docker/elyon-restaurant-mvp/Caddyfile` sur le VPS — fichier appartenant à l'autre projet, sauvegardé avant modification) pour `academy.lobima.online`/`api-academy.lobima.online`, appliqués par un **rechargement gracieux** (`caddy reload`, jamais un restart du conteneur) pour ne jamais interrompre le trafic Elyon en cours. Certificats Let's Encrypt de production obtenus et vérifiés (émetteur réel, pas auto-signé) dès que le DNS (deux enregistrements A vers `77.37.87.119`, ajoutés par l'utilisateur) a propagé.
- **Compte administrateur de production** : créé via `docker exec ... node dist/prisma/seed.js` puis son mot de passe changé immédiatement après la première connexion réelle (le mot de passe par défaut `ChangeMe123!` documenté dans ce fichier ne doit **jamais** rester actif sur un déploiement public) — nouveau mot de passe connu seulement de l'utilisateur.
- **Vérifié de bout en bout en conditions réelles** : DNS, HTTPS (certificat réel), CORS (`WEB_ORIGIN`), login réel, changement de mot de passe forcé, tableau de bord chargé — sans jamais interrompre Elyon (vérifié après coup : `app.lobima.online`/`api.lobima.online` toujours 200, tous les conteneurs Elyon toujours `healthy`/`Up`, aucun redémarrage de leurs conteneurs).
- **Structure académique et tarifs réels configurés en production (17 septembre 2026)** : à la demande de l'utilisateur, structure complète créée pour l'année active 2026/2027 — section Francophone (Maternelle : TPS/PS/MS/GS, Primaire : CP/CE1/CE2/CM1/CM2, Collège : 6e/5e/4e/3e, Lycée : 2nde C) et section Anglophone (Maternelle : Pre-nursery/Nursery 1/Nursery 2, Primaire : Class 1-6) — 23 niveaux, une classe par niveau (même nom, à scinder en plusieurs classes par niveau plus tard si l'effectif le justifie). Tarifs réels : "Frais d'inscription" (45 000 XAF, `appliesTo: INSCRIPTION`), "Frais de réinscription" (60 000 XAF, `appliesTo: REINSCRIPTION`), "Pension" en 3 tranches égales (`appliesTo: TOUS`) — 110 000 XAF/tranche en Maternelle, 100 000 en Primaire/Collège, 120 000 en Lycée (330 000/300 000/300 000/360 000 XAF par an selon le cycle). 69 grilles tarifaires créées (23 niveaux × 3 types de frais) via un script ponctuel (non conservé dans le dépôt, propre à cette seule école) appelant directement l'API de production. **Dates limites des 3 tranches de pension fixées par défaut à titre provisoire** (1er octobre / 15 janvier / 1er avril, 5 jours de grâce chacune) faute d'un calendrier de paiement exact communiqué par l'école — modifiables à tout moment via `PATCH /fee-schedules/:id` ou l'écran `/tarifs`, à ajuster dès que l'école confirme ses vraies échéances. Vérifié avec un vrai élève de test (inscription en TPS → facture exacte 45 000 + 3×110 000 = 375 000 XAF, aucune ligne de réinscription — confirme le bon fonctionnement de D39 sur données réelles) puis nettoyé.
- **Vérification publique des reçus par QR code (D29, 17 septembre 2026, demande explicite : "le reçu doit être difficilement falsifiable")** : `Payment.verificationToken` (jeton opaque distinct du numéro de reçu séquentiel — jamais ce dernier, devinable par simple incrémentation) — `GET /payments/verify/:token` public (`@Public()`), renvoie uniquement numéro/montant/statut/date/motif/nom de l'élève/établissement, jamais de téléphone ni autre donnée sensible. `PermissionsGuard` a dû être corrigé pour respecter `@Public()` (jusqu'ici seul `JwtAuthGuard` en tenait compte — une route publique au sein d'un contrôleur par ailleurs protégé par `@RequirePermission` restait bloquée en 403, `request.user` n'étant jamais peuplé). QR code (`qrcode`, généré côté client) affiché sur `/recus/[id]`, pointant vers une nouvelle page publique `/verifier-recu/[token]` (aucun compte requis, consultable en scannant depuis un téléphone). Vérifié en production avec un vrai reçu (`REC-000002`) : `GET /payments/verify/:token` répond sans en-tête d'authentification, page de vérification affichant "Reçu authentique" avec les bonnes informations, reçu imprimable affichant le QR code correctement positionné.
- **Import initial de 25 élèves déjà inscrits, depuis le fichier Excel de suivi de l'école (17 septembre 2026)** — voir `DECISIONS_PENDING.md` §15 (D45-D48) pour le détail des décisions prises avec l'utilisateur avant d'importer (traitement des tarifs de fratrie via remises, absence de compte Direction pour les approuver, absence de contacts responsables, mode de paiement par défaut). Un changement de code réel en a découlé : **`CreateStudentDto.responsable` devient optionnel** (D31 révisé) — le schéma (`StudentGuardian`, table de jonction) l'a toujours permis, seul le DTO l'imposait ; un responsable reste ajoutable à tout moment via le bouton "Ajouter" déjà existant sur la fiche élève. Import réalisé via un script ponctuel (non conservé dans le dépôt) appelant la vraie API de production (jamais d'écriture SQL directe pour élève/facture/paiement) — seule l'approbation des 51 remises l'a été, en base, à la demande explicite de l'utilisateur et strictement scopée à cet import (le contrôle applicatif "Direction approuve une remise" reste intact pour l'avenir). Vérifié manuellement dans le navigateur sur un cas avec remise et un cas sans — montants exacts des deux côtés (voir DECISIONS_PENDING.md pour le détail).
- **Tableau de bord en couleurs, liste par classe, exports PDF et Excel de toutes les listes (21 septembre 2026, demande explicite)** : `StatCard` (`components/ui.tsx`) devient une carte teintée avec liseré gauche, icône pleine et valeur colorées selon le sens (`success` vert = positif comme encaissé ou solde, `danger` rouge = négatif comme restant dû, impayé ou sorties, `warning` orange = à surveiller comme remises ou éléments en attente, `info` bleu = neutre, `primary`/`accent` = identité), avec une jauge optionnelle (`progress`) ; le taux de recouvrement change de couleur par seuil (75 et plus vert, 40 à 74 orange, en dessous rouge), le solde de caisse passe au rouge s'il est négatif. Le tableau de bord ajoute une barre segmentée de la facturation (encaissé vert, remises orange, restant dû rouge), des barres par section et par classe (bleu Anglophone, violet Francophone), les pourcentages garçons/filles. Les listes en cartes reçoivent la même logique (sorties : orange en attente, vert approuvée, rouge rejetée ; clôture : entrées vertes avec +, sorties rouges avec - ; insolvables : impayé critique en rouge). **Nouvelle page `/eleves-par-classe`** (`GET /reports/students-by-class`, JSON, mêmes lignes que l'export CSV existant qui en est désormais dérivé) : un bloc par classe (section, cycle, effectif, garçons/filles), filtre par classe et recherche, élèves cliquables. **Exports PDF et Excel (.xlsx) sur toutes les listes** : `lib/export.ts` (100 % navigateur, `jspdf` + `jspdf-autotable` + `write-excel-file`, chargés à la demande) et `components/export-buttons.tsx` (`ExportButtons`, nom de l'établissement lu une fois via `GET /school`) exportent exactement les données affichées (recherche et filtres compris) ; pages couvertes : tableau de bord (indicateurs + effectif par classe), élèves, élèves par classe (un onglet Excel et une section PDF par classe, plus l'export de chaque classe seule), insolvables, sorties financières, clôture de journée (récapitulatif, entrées, sorties), tarifs, utilisateurs, journal d'audit. Les montants restent numériques dans Excel (format `#,##0`, sommables) avec une ligne de total quand elle a un sens ; le PDF remplace l'espace fine insécable de `toLocaleString("fr-FR")`, absent des polices standard de jsPDF. Vérifié dans le navigateur : fichiers réellement produits (xlsx = archive valide avec un onglet par classe, PDF relu visuellement, accents corrects) ; **le lien du menu "Élèves" restait actif sur "Élèves par classe"** (`startsWith("/eleves")`), corrigé en comparant avec `href + "/"`. Les anciens boutons CSV des pages Élèves et Insolvables sont remplacés (les routes `/reports/export/*` restent disponibles).
- **Correctif suite au redéploiement (17 septembre 2026, pas une régression du code)** : redéployer ce lot a révélé que les commandes `docker compose build`/`up` avaient été lancées sans `--env-file .env.production` — sur ce projet (contrairement à un fichier `.env` par défaut que Docker Compose charge automatiquement), les vraies valeurs de production ne sont jamais reprises sans ce flag explicite, `academy-api` retombant silencieusement sur les valeurs par défaut du `docker-compose.yml` (`POSTGRES_PASSWORD: shakespeare`) au lieu du vrai mot de passe généré aléatoirement — le site est resté indisponible (401/500 en cascade) jusqu'à ce que le mot de passe réel de la base soit resynchronisé et que les conteneurs soient relancés avec `--env-file .env.production`. Toute commande `docker compose` future sur ce déploiement doit inclure ce flag.

- **Application installable (PWA) et mode hors ligne avec synchronisation (21 septembre 2026, demande explicite : "Internet étant un souci en Afrique", périmètre choisi : tout, paiements compris)** — voir `DECISIONS_PENDING.md` §16 (D49) pour la décision et ses limites. **Installation** : `app/manifest.ts`, icônes générées (`public/icons/`, « S » doré sur fond indigo, **générique** : le vrai logo n'existe pas en fichier), bouton « Installer l'application » (`components/install-app-button.tsx`, `lib/pwa-install.ts` : vraie invite sur Chrome/Edge/Android, explication « Partager → Sur l'écran d'accueil » sur iOS) dans le menu latéral et la page de connexion. **Service worker** `public/sw.js` (enregistré uniquement en production, `components/pwa-register.tsx`) : fichiers statiques d'abord depuis le cache, pages et données de navigation réseau d'abord avec repli sur la dernière copie (réseau lent = 3,5 s), `public/offline.html` en dernier recours ; jamais les appels à l'API (autre adresse). **Lectures hors ligne** : `lib/api.ts` copie chaque GET réussi dans IndexedDB (`lib/offline-cache.ts`, propre à un utilisateur, effacée à la déconnexion) et la relit si le réseau échoue (délais 15 s lecture / 30 s écriture) ; la recherche d'élèves retombe sur la liste copiée ; « Préparer le mode hors ligne » (`lib/offline-warmup.ts`, auto toutes les 6 h) copie élèves, factures, paiements et demande au service worker de précharger les pages. **Connectivité** (`lib/connectivity.ts`) : `navigator.onLine` n'est pas fiable, l'état bascule hors ligne à la première requête en échec et revient en ligne sur une vraie réponse ou un `/health` toutes les 15 s. **Session** : au démarrage sans réseau, `/auth/me` copié restaure l'utilisateur (aucun mot de passe redemandé). **Saisies hors ligne** (`lib/outbox.ts`, `lib/offline-actions.ts`) : file d'attente rejouée dans l'ordre, chaînage `$ref:<id>` (une inscription attend la création de l'élève), envoi par le seul compte auteur, survit à la déconnexion (confirmation demandée) ; encaissement → **reçu provisoire** `PROV-{appareil}-{jjmmaa}-{n}` (`app/recus/provisoire/[id]`), le reçu officiel REC-xxxxxx n'existe qu'après l'envoi (le reçu officiel affiche « Remplace le reçu provisoire … ») ; solde de l'élève recalculé localement avec les encaissements en attente. Page **« Synchronisation »** (`/hors-ligne`) : état, Réessayer, « Créer malgré le doublon », Abandonner. **Serveur** : `IdempotencyInterceptor` global (table `idempotency_records`, clé `Idempotency-Key` par utilisateur ; 409 si la clé sert sur une autre route, 400 si mal formée), `Payment.numeroProvisoire` (unique par école) et `saisieHorsLigneAt`, `dateSaisie` bornée à 45 jours ; migration `20260921120000_offline_sync`. Exclusivement en ligne : approbations, annulations, réimpressions auditées, autre frais, changement de classe, clôture. 10 tests e2e ajoutés (`offline-sync.e2e-spec.ts`), 125 au total. **Vérifié dans le navigateur (build de production, réseau coupé par surcharge de `fetch` puis arrêt réel de l'API)** : encaissement hors ligne → reçu provisoire, solde recalculé, synchronisation automatique → REC-000007 ; nouvel élève + inscription hors ligne → envoyés dans l'ordre ; rechargement complet API arrêtée → session, dossier et solde relus depuis la copie. **Non vérifié ici** : l'enregistrement du service worker n'a pas pu être testé (le navigateur intégré de cette session refuse de l'enregistrer ; Chrome n'était pas connecté) — à contrôler sur un vrai téléphone après déploiement (installation, ouverture sans Internet).

- **Lot 7 : espace de saisie « Vie scolaire » (référentiel pédagogique et personnel) (21 septembre 2026)** : la Direction a validé D50 à D78 sans modification (`DECISIONS_PENDING.md` §17 passée en TRANCHÉ) et a demandé de préparer l'espace où elle saisira elle-même ce qu'elle devait fournir. **Rien n'est prérempli** (aucun créneau, matière, enseignant ni date). **Backend** `apps/api/src/pedagogy/` : jours de classe (`School.joursClasse Int[]`, lundi à vendredi par défaut, D52), créneaux horaires `TimeSlot` (grille commune ou propre à une section, D53 ; format `HH:mm`, chevauchement refusé dans une même grille, jamais entre grille commune et grille de section), salles `Room`, matières `Subject` + `SubjectLevel` (niveaux auxquels une matière est enseignée, volume hebdomadaire facultatif ; retirer un niveau déjà affecté est refusé), enseignants `Teacher` + `TeacherSubject` (fiche sans compte, sans paie ni contrat, D56 ; homonyme exact refusé), affectations `TeachingAssignment` (un seul enseignant par classe et matière, RV01 ; la matière doit être enseignée au niveau de la classe, enseignant actif obligatoire), trimestres `Term` et jours sans classe `CalendarEvent` (D55 ; dates dans l'année scolaire, pas de chevauchement de trimestres, année clôturée non modifiable), classe pilote `Class.pilote` et enseignant volontaire `Teacher.volontairePilote` (un seul de chaque, exclusivité garantie par le service), récapitulatif `GET /pedagogy/summary` (ce qui est saisi et ce qui manque, dont `affectations.requises`/`manquantes`). Nouvelle permission **`PEDAGOGY_MANAGE`** (Administrateur et Direction) : toute écriture et toute lecture des enseignants (données personnelles) l'exigent ; la lecture des créneaux, salles, matières, trimestres et calendrier est ouverte à tout utilisateur connecté. Chaque action est journalisée (RG15). Migration `20260921130000_lot7_referentiel_pedagogique` (nommée après `offline_sync` : `prisma migrate dev` avait proposé un horodatage antérieur, renommé pour garder l'ordre d'application). `test/utils/clean-database.ts` mis à jour (enfants avant parents). **Frontend** : page `/vie-scolaire` (lien de menu réservé à `PEDAGOGY_MANAGE`), sept onglets (`components/vie-scolaire/`) : suivi de la saisie (huit rubriques avec statut et raccourcis, choix de la classe pilote et de l'enseignant volontaire, rappel des deux documents à remettre hors application), horaires, matières, enseignants, affectations (grille par classe), calendrier, salles ; boutons +/− partout. Saisie en ligne uniquement (une erreur hors ligne est affichée clairement). **Ne fait pas encore** : l'emploi du temps lui-même (séances, conflits) est le Lot 8 ; un créneau ou une salle supprimé aujourd'hui pourra devenir non supprimable quand des séances les référenceront (à traiter au Lot 8 : désactiver plutôt que supprimer). 35 tests e2e ajoutés (`pedagogy.e2e-spec.ts`), 160 au total ; vérifié dans le navigateur (saisie de créneaux avec refus de chevauchement, matière avec niveau, enseignant, salle, affectation, trimestre, jour férié, suivi passant de 0/8 à 5/8, aucun débordement à 390 px sur les sept onglets) ; données de test supprimées de la base locale après vérification.
- **Lot 8 : emploi du temps (21 septembre 2026)** : séances hebdomadaires versionnées, conflits refusés, changements ponctuels. **Backend** `apps/api/src/timetable/` : `Timetable` (une version par année : `BROUILLON` modifiable, `PUBLIE` immuable avec date d'effet, `ARCHIVE` quand une version plus récente la remplace, RV03), `TimetableEntry` (classe, matière, enseignant, salle, jour, créneau ; les heures sont copiées du créneau à la saisie), `TimetableException` (annulation, remplacement d'enseignant, changement de salle à une date précise : n'écrit jamais dans la version, l'historique publié reste intact). **Saisie** : l'enseignant se déduit de l'affectation de la classe (jamais saisi à la main) ; le créneau doit être un cours (pas une pause) de la grille de la section de la classe ; le jour doit être un jour de classe ; salle active et enseignant actif obligatoires. **RV02** : classe, enseignant et salle ne peuvent pas être à deux endroits au même moment (deux séances qui s'enchaînent sont permises) ; le refus nomme la séance en cause ; la publication revérifie tout (paire par paire, affectations changées, enseignants ou salles devenus inactifs) et est bloquée avec la liste des problèmes. Un seul brouillon par année ; une nouvelle version repart par défaut d'une copie de la version en vigueur (`vide: true` pour repartir de zéro) ; date d'effet dans l'année et strictement postérieure à celle de la version en vigueur ; année clôturée non modifiable. `POST /timetables/:id/resync` remet à jour les enseignants d'un brouillon après un changement d'affectation (un conflit créé est signalé, rien n'est écrasé). **Semaine réelle** : `GET /timetable/day|week` applique à la version en vigueur ce jour-là les changements ponctuels et les jours sans classe (calendrier, jours hors `joursClasse`), filtrables par classe, enseignant ou salle ; un changement ponctuel exige une version publiée en vigueur à cette date, un jour de classe, un remplaçant ou une salle libres à ce moment (une séance annulée libère ses ressources), un motif, et un seul changement par séance et par date. **Permissions** : nouvelle **`TIMETABLE_READ`** (Administrateur, Direction, Secrétaire-caissier, Auditeur) pour la consultation ; toute écriture (brouillon, publication, changements ponctuels) reste sous `PEDAGOGY_MANAGE`, et les brouillons sont invisibles sans elle (404). Les changements ponctuels sont sous `PEDAGOGY_MANAGE` en attendant le rôle Surveillant des Lots 9 et 10. **Protections du Lot 7** : un créneau utilisé ne change plus d'heures ni de type et ne se supprime pas, une salle utilisée (séance ou changement ponctuel) ne se supprime pas (la désactiver), une affectation qui sert dans un emploi du temps ne se supprime pas. Audit RG15 sur chaque action. Migration `20260921140000_lot8_emploi_du_temps` (nommée après `offline_sync`, même raison qu'au Lot 7). **Frontend** : page `/emploi-du-temps` (lien de menu sous `TIMETABLE_READ`, `components/emploi-du-temps/`) : onglet Versions (sélecteur de version, grille jours × créneaux par classe, enseignant ou salle, ajout par + / changement de salle / retrait en brouillon, boutons Vérifier, Mettre à jour les enseignants, Publier avec date d'effet, Supprimer le brouillon) et onglet Semaine réelle (navigation par semaine, changement ponctuel par séance, Rétablir) ; export PDF et Excel de ce qui est affiché. La vue par enseignant tire ses noms des séances (la liste des enseignants reste réservée à `PEDAGOGY_MANAGE`). **Ne fait pas encore** : impression mise en page d'une grille (l'export est une liste triée jour et heure), notification aux parents d'un changement (Lot 12), rôle Surveillant (Lots 9 et 10). 21 tests e2e ajoutés (`timetable.e2e-spec.ts`), 181 au total ; vérifié dans le navigateur (ajout de séances, refus d'un conflit d'enseignant avec le message nommant la séance, publication, annulation ponctuelle en semaine réelle, export PDF produit, vue par enseignant, aucun débordement à 390 px après correction de la barre de navigation de semaine) ; données de test supprimées de la base locale. **Déployé en production le 21 septembre 2026** : migration appliquée au démarrage de l'API (tables d'emploi du temps vides), `TIMETABLE_READ` accordée aux quatre rôles par un script ponctuel (sans réinitialiser les autres permissions), routes vérifiées (401 sans jeton), conteneurs Elyon non touchés.
- **Lot 9 : assiduité des élèves (21 septembre 2026)** : appel séance par séance, absences et retards, justificatifs, corrections encadrées, hors ligne. **Backend** `apps/api/src/attendance/` : `AttendanceCall` (un appel par séance de l'emploi du temps et par date, `@@unique([entryId, date])`, avec instantané de la classe, de la matière, de l'enseignant qui tenait réellement la séance, remplaçant compris, et des heures), `AttendanceRecord` (un enregistrement par élève inscrit, unique par appel et élève), `AttendanceCorrection` (historique jamais modifié : ancien et nouveau statut, motif, auteur), `AbsenceJustification` (un par enregistrement), `AbsenceReason` (motifs saisis par la Direction, aucun prérempli). **Règles** : par défaut tout le monde est présent, on ne liste que les exceptions (D60) ; `POST /attendance/calls` remplace l'état entier de l'appel, donc renvoyer la même saisie (synchronisation, réponse perdue, deux appareils) ne crée jamais de doublon ; le **statut est calculé par le serveur** d'après les minutes de retard et le seuil paramétré (RV05 : jusqu'au seuil retard, au-delà absent, un retard supérieur à la durée de la séance est refusé) ; on ne fait pas l'appel d'une séance annulée, d'un jour sans classe, d'une séance absente de l'emploi du temps en vigueur ce jour-là ni d'un jour futur. **Verrouillage (RV04, D59)** : l'auteur modifie son appel jusqu'à la fin de la journée dans le fuseau de l'établissement ; ensuite, ou pour tout autre utilisateur, seule la vie scolaire ou la Direction (`ATTENDANCE_CORRECT`) corrige, avec **motif obligatoire**, chaque changement de statut étant écrit dans `AttendanceCorrection` et l'audit (`ATTENDANCE_CALL_CORRECT`) ; un appel oublié se saisit après coup par la même voie. **Hors ligne** : l'appel porte `saisiLe` (heure de l'appareil, bornée à 45 jours comme les encaissements) ; s'il tombe le jour de la séance, l'appel arrivé après minuit reste accepté sans motif comme appel de l'auteur (à faire confiance à l'horloge de l'appareil, même limite qu'au Lot 6). **Justificatifs (D58)** : saisis et décidés sous `ATTENDANCE_CORRECT` (au nom du responsable, le portail parent vient au Lot 11) ; motif de la liste ou commentaire obligatoire ; déposé après le délai (jours de classe, hors jours sans classe du calendrier) il est enregistré et signalé « hors délai », pas refusé d'office ; une seule décision (accepter ou refuser). **Paramètres provisoires modifiables** (`GET/PATCH /pedagogy/settings`, onglet « Assiduité » de Vie scolaire) : `School.retardMaxMinutes` (15, D57) et `School.delaiJustificatifJours` (3, D58), jamais codés ailleurs. Lectures : `GET /attendance/day`, `/sheet`, `/absences` (filtres classe, période, justificatif), `/students/:id/history` (compteurs : séances appelées, absences, retards, excusées, non justifiées). **Permissions** (migration `20260921150000_lot9_assiduite`, nommée après `lot8` pour l'ordre d'application) : `ATTENDANCE_TAKE`, `ATTENDANCE_CORRECT`, `ATTENDANCE_READ` (données de mineurs, RV12) ; Administrateur et Direction les ont toutes, l'Auditeur lit seulement, la Secrétaire-caissière n'a rien. **Nouveau rôle `SURVEILLANT`** (vie scolaire) : lecture des élèves, de l'emploi du temps, et les trois permissions. Le rôle ENSEIGNANT n'existe pas encore : les enseignants n'ont pas de compte (Lot 7), donc l'appel est fait par le surveillant ; quand ils en auront, un rôle avec `ATTENDANCE_TAKE` sans `ATTENDANCE_CORRECT` se comporte déjà comme prévu (testé avec un rôle « appel seul »). Audit RG15 : création, mise à jour, correction, justificatif, décision, motifs. **Frontend** : page `/appel` (lien de menu sous `ATTENDANCE_READ`), onglet Appel (jour, classe, séances avec leur état, « En attente d'envoi » si l'appel est encore sur l'appareil) et feuille d'appel (tout présent, boutons Présent / Retard / Absent, minutes de retard avec avertissement au-delà du seuil, motif demandé pour une correction, message clair quand la modification est interdite) ; onglet Absences et justificatifs (liste filtrée, développable, justificatif, accepter ou refuser, export PDF et Excel) ; carte « Assiduité » dans la fiche de l'élève ; onglet « Assiduité » de Vie scolaire (règles et motifs). L'appel passe par la file d'attente hors ligne existante (`kind: "attendance"`, page Synchronisation : « Appel ») et « Préparer le mode hors ligne » copie les séances du jour et leurs feuilles d'appel. **Ne fait pas encore** : alertes et notifications (Lot 12 ; D61 : aucun seuil d'alerte inventé), portail parent et déclaration par le parent (Lot 11), compte enseignant, pointage des enseignants (Lot 10), appel par demi-journée (option de D60 non construite), justificatif avec pièce jointe. Le filtre de classe de l'écran Appel ne propose que les classes de l'année active. 21 tests e2e ajoutés (`attendance.e2e-spec.ts`, dates relatives à aujourd'hui, tous les jours en jours de classe), 202 au total ; vérifié dans le navigateur à 390 px (feuille d'appel, statuts, calcul du seuil, enregistrement, **appel créé sans Internet puis synchronisé sans doublon et marqué « hors ligne »**, modification hors ligne d'un appel existant, justificatif et acceptation, onglet Vie scolaire, carte de l'élève, aucun débordement) ; données de test supprimées de la base locale. **Déployé en production le 21 septembre 2026** : migration appliquée au démarrage de l'API (tables d'assiduité vides, paramètres à 15 minutes et 3 jours), permissions et rôle `SURVEILLANT` créés par un script ponctuel sans réinitialiser les autres permissions (vérifié en production que ce rôle n'existait pas), routes vérifiées (401 sans jeton), conteneurs Elyon non touchés. Un compte SURVEILLANT reste à créer par l'Administrateur (Utilisateurs & rôles).
- **Lot 10 : pointage des enseignants par QR code (21 septembre 2026)** : à la demande de la Direction, **D62 est modifiée** (`DECISIONS_PENDING.md`) : l'enseignant scanne lui-même, au lieu d'être pointé par le surveillant. **Deux modes par enseignant** (`Teacher.modePointage`, réglé dans Vie scolaire, Enseignants) : `SEANCE` (collège, lycée) scanne le **QR de la salle** au début et à la fin de chaque cours ; `JOURNEE` (maternelle, primaire) scanne le **QR de l'entrée de l'école** à l'arrivée et au départ. **Compte relié à une fiche** : `Teacher.userId` (unique : un compte, une fiche ; onglet Enseignants, liste `GET /teachers/linkable-users`) ; nouveau rôle **`ENSEIGNANT`** (`TIMETABLE_READ` et `TEACHER_CHECKIN_SELF` seulement : aucune donnée d'élève, l'appel par l'enseignant reste à décider). **QR codes** : modèle `PointageCode` (un par salle, un pour l'entrée), jeton aléatoire jamais renvoyé par une route ouverte (`GET /rooms` ne le contient pas : le lire à distance annulerait la preuve de présence), gérés sous `PEDAGOGY_MANAGE` (générer les manquants, **régénérer** : l'ancien QR cesse aussitôt de fonctionner, supprimer une salle supprime son QR) ; le QR encode `{origine}/pointage?c=jeton`, l'appareil photo du téléphone l'ouvre, le code imprimé dessous sert de secours. Limite honnête : un QR imprimé peut être photographié ; la protection est la validation par un tiers, les écarts signalés et la régénération (un QR tournant sur écran serait une évolution). **Règles** (`apps/api/src/teacher-checkins/`) : le serveur décide de la nature du scan, l'enseignant ne choisit pas. Par séance : parmi ses séances du jour (remplaçant compris, séances annulées exclues), un **début** est possible de la fenêtre d'ouverture jusqu'à la fin de la séance, une **fin** d'après le début jusqu'à la fenêtre après la fin ; on retient l'événement dont l'heure prévue est la plus proche, **à égalité la fin d'abord** (à la jonction de deux cours, un scan clôt le premier et le suivant ouvre le second) ; retard = début pointé moins l'heure prévue, **signalé au-delà de la tolérance** (D63) ; un QR d'une autre salle que celle prévue est accepté mais marqué « autre salle » ; un scan rejoué à l'identique ne change rien (idempotent) ; un double scan trop rapproché est refusé (« vous avez déjà pointé à HH:mm »). Par journée : premier scan = arrivée (retard comparé à la première séance du jour, **aucune heure d'attente inventée sans séance**), les suivants = départ (le dernier compte). QR d'un mauvais type, inconnu ou remplacé, compte non relié, fiche inactive, jour sans séance : refus explicites. **Validation (RV06)** : le pointage naît `EN_ATTENTE` ; un tiers (`TEACHER_CHECKIN_VALIDATE`) le valide ou le rejette (motif obligatoire), une seule décision ; **le serveur refuse qu'un validateur traite ou corrige son propre pointage** (testé avec un rôle qui cumule pointage et validation) ; la vie scolaire saisit ou corrige un pointage (enseignant sans téléphone, scan oublié) avec un **motif obligatoire**, validé d'office puisque posé par un tiers, tracé dans l'audit (`TEACHER_CHECKIN_MANUAL`, valeur avant et après). **Hors ligne** : le scan part avec l'heure de l'appareil (`scanneLe`, bornée à 45 jours comme les encaissements), la file d'attente existante (`kind: "checkin"`, page Synchronisation : « Pointage ») le rejoue dans l'ordre, le serveur résout la séance d'après cette heure ; l'écart entre heure du scan et heure de réception est affiché (« hors ligne »). **Heures effectuées (RV07, D64)** : `GET /teacher-checkins/summary?month=` calcule d'après les **seuls pointages validés** : par séance, séances tenues (début et fin validés) et leur **durée prévue**, séances en attente, rejetées, incomplètes, non pointées ; par journée, jours présents et heures entre arrivée et départ ; retards signalés ; le remplaçant est crédité de la séance qu'il tient, l'absent voit « séances confiées à un remplaçant » ; export PDF et Excel. **Permissions** (migration `20260921160000_lot10_pointage_enseignants`, nommée après `lot9`, générée par `prisma migrate diff` car `migrate dev` demandait une confirmation interactive) : `TEACHER_CHECKIN_SELF`, `TEACHER_CHECKIN_READ`, `TEACHER_CHECKIN_VALIDATE` ; Administrateur, Direction et Surveillant lisent et valident, l'Auditeur lit. **Paramètres provisoires modifiables** (`School`, `GET/PATCH /pedagogy/settings`, onglet « QR codes et règles ») : `pointageFenetreMinutes` 15, `pointageToleranceMinutes` 10, `pointageEcartMinMinutes` 5. **Frontend** : page `/pointage` (« Mon pointage », `TEACHER_CHECKIN_SELF`) avec scan caméra (`jsqr`, `components/qr-scanner.tsx`, caméra coupée après usage, saisie du code en secours), résultat clair, état du jour, scans en attente d'envoi ; un QR ouvert avec l'appareil photo arrive sur `/pointage?c=...` et **pointe aussitôt, y compris après connexion** (`/login?next=...` : l'adresse demandée est conservée, adresse interne seulement) ; page `/pointage-enseignants` (`TEACHER_CHECKIN_READ`) : pointages du jour (valider, rejeter, saisir, corriger), heures du mois (tableau et export), QR codes et règles (QR à l'écran, PDF de tous les QR, régénérer). Le préchargement hors ligne est devenu prudent pour un compte sans `STUDENT_READ` (il levait une erreur 403 non gérée). **Ne fait pas encore** : géolocalisation, QR tournant, appel des élèves par l'enseignant lui-même, paie. 23 tests e2e ajoutés (`teacher-checkins.e2e-spec.ts`, scans datés d'hier avec `scanneLe`), 225 au total ; vérifié dans le navigateur à 390 px (lien de QR ouvert sans être connecté, connexion puis début de séance pointé automatiquement, double scan refusé, fin de séance, arrivée d'un enseignant en mode journée **scannée sans Internet puis synchronisée**, validation par l'administrateur, heures du mois « 0 h 40 », QR à l'écran et PDF généré, réglage du mode et du compte dans Vie scolaire, aucun débordement) ; **le décodage de la caméra a été vérifié par un aller-retour QR généré puis décodé avec `jsqr`, mais la vraie caméra d'un téléphone n'a pas pu être testée ici** ; données de test supprimées de la base locale. **Déployé en production le 21 septembre 2026** : migration appliquée au démarrage de l'API (tables de pointage vides, paramètres à 15, 10 et 5 minutes), permissions et rôle `ENSEIGNANT` créés par un script ponctuel sans réinitialiser les autres permissions (vérifié en production que ce rôle n'existait pas), routes vérifiées (401 sans jeton), conteneurs Elyon non touchés. **Pour démarrer** : créer les comptes des enseignants (rôle Enseignant) dans « Utilisateurs & rôles », les relier à leur fiche et choisir leur mode dans Vie scolaire, Enseignants, puis générer et imprimer les QR dans « Pointage enseignants », onglet « QR codes et règles ».
- **Lot 11 : comptes parents et portail en lecture (21 septembre 2026)** : un responsable consulte, en lecture seule, l'emploi du temps, les absences et retards, la situation financière et les reçus de ses enfants. **Décision d'architecture principale : un compte parent n'est PAS un `User`.** Plusieurs routes du personnel (`GET /rooms`, `/school`, `/academic-years`, `/absence-reasons`...) n'exigent qu'un utilisateur connecté, sans permission : un parent bâti sur `User` y aurait eu accès, et une simple erreur de permission sur une route future aurait exposé des élèves d'autres familles. Le parent a donc sa **propre table** (`ParentAccount`, relié à un `Guardian` déjà enregistré), son **propre jeton** (clé dérivée de `JWT_ACCESS_SECRET` mais différente, marqueur `typ: parent`), sa **propre garde** (`ParentAuthGuard`, les routes `/portal/*` sont publiques pour la garde du personnel) et son **propre cookie** de renouvellement (`parent_refresh_token`, chemin `/portal`, httpOnly). Un jeton de parent n'ouvre aucune route du personnel et inversement ; c'est **testé sur douze routes du personnel** dont celles sans permission. **Activation (D66, D76)** : le secrétariat génère un code (`POST /parent-accounts/guardians/:id/activation-code`, 8 caractères sans lettres ambiguës, « K7MQ-2XPA »), **montré une seule fois** ; seule son empreinte SHA-256 est conservée (ni le code ni le mot de passe n'apparaissent jamais dans l'audit) ; un seul code valable à la fois ; durée de validité paramétrée (`School.parentCodeValiditeJours`, 7 par défaut) ; à usage unique ; **brûlé après 5 essais faux** ; les messages d'erreur ne distinguent jamais « numéro inconnu » de « code faux ». `POST /portal/activate` (numéro, code, mot de passe de 8 caractères au moins, consentement coché, version de la politique) crée le compte et **enregistre le consentement** (`ParentConsent`, une ligne par acceptation avec la version) ; la version `CONSENT_VERSION` de l'API et celle de la page web sont à garder identiques, une version périmée est refusée. Le numéro saisi se compare aux chiffres seuls (« +242 06 000 0001 » égale « 242060000001 »). Un nouveau code sur un compte existant **réinitialise le mot de passe et ferme les sessions ouvertes**. Connexion par numéro et mot de passe, verrouillage 15 minutes après 5 échecs, désactivation immédiate (`PARENT_ACCOUNT_MANAGE`). **Isolation (RV08, D67)** : chaque route `/portal/children/:id/...` vérifie que l'élève est rattaché à CE responsable avec l'accès non retiré (`assertChild`) ; sinon **404**, jamais 403 (on ne confirme pas l'existence de l'élève). `StudentGuardian.accesPortail` permet à la **Direction seule** (`PARENT_ACCESS_REVOKE`) de retirer ou rétablir l'accès d'un responsable pour UN enfant, avec motif obligatoire et audit, sans toucher aux autres responsables de cet enfant ni aux autres enfants du même responsable. **Ce que le parent reçoit** est réduit à l'essentiel : jamais le motif interne d'un changement d'emploi du temps, ni les corrections d'appel, ni le nom du validateur, ni les identifiants internes, ni le jeton de vérification des reçus (testé sur le contenu des réponses). Les libellés financiers sont adaptés (« Paiement en retard » y compris pour un impayé critique). **Permissions** (migration `20260921170000_lot11_comptes_parents`, générée par `prisma migrate diff`) : `PARENT_ACCOUNT_MANAGE` (Administrateur, Direction, Secrétaire-caissier), `PARENT_ACCESS_REVOKE` (Direction). Les parents n'ont ni rôle ni permission. **Frontend** : espace `/parents` (connexion, activation, enfants, fiche de l'enfant en trois onglets : emploi du temps par semaine, absences, finances et reçus, changement de mot de passe, politique de confidentialité), en français seulement (D68) ; page du personnel `/portail-parents` (« Comptes parents » : recherche, code d'activation affiché une fois avec message à copier, désactiver, réactiver, retrait d'accès par enfant) ; lien « Espace parents » sur la connexion du personnel. Le client du portail (`lib/portal-api.ts`) est distinct du client du personnel et ne garde aucune copie hors ligne. **Ne fait pas encore** : déclaration d'un justificatif par le parent (D58 prévoit qu'il le déclare : ce lot est en lecture seule, il remet un mot au secrétariat), notifications (Lot 12), messagerie (Lot 13), impression du reçu, récupération de mot de passe par SMS ou e-mail, anglais. **La politique de confidentialité affichée est un projet à faire valider par l'école ou un conseil juridique avant toute ouverture aux parents.** 19 tests e2e ajoutés (`parent-portal.e2e-spec.ts`), 244 au total ; vérifié dans le navigateur à 390 px (code généré côté secrétariat, activation avec numéro saisi sous une autre forme et case de consentement obligatoire, fiche de l'enfant : emploi du temps, absence excusée avec motif, situation financière et reçu, déconnexion, mauvais mot de passe puis connexion) ; données de test supprimées de la base locale.
  **Déployé en production le 21 septembre 2026** : migration `20260921170000_lot11_comptes_parents` appliquée au démarrage de l'API (tables parents vides, code valable 7 jours), `PARENT_ACCOUNT_MANAGE` et `PARENT_ACCESS_REVOKE` créées par un script ponctuel sans réinitialiser les autres permissions, routes vérifiées (`/portal/consent-info` 200 avec la version `2026-09-v1`, `/portal/me` et `/parent-accounts` 401 sans jeton, pages `/parents/connexion` et `/parents/confidentialite` 200), conteneurs Elyon non touchés. **Pour démarrer** : le secrétariat vérifie les numéros de téléphone des responsables sur les fiches élèves, puis génère un code par responsable dans « Comptes parents » et le lui remet en main propre. **Avant d'ouvrir aux parents : faire valider le texte de la politique de confidentialité par l'école.** Aucune donnée réelle n'a été utilisée pour les tests.
- **Lot 12 : notifications aux parents (21 septembre 2026)** : un responsable est prévenu quand une absence ou un retard est saisi pour son enfant, quand un cours de sa classe est annulé ou remplacé, et quand l'emploi du temps de sa classe change. **Canaux (D65)** : le message dans l'application est toujours créé ; l'alerte push est gratuite (Web Push, standard VAPID, bibliothèque `web-push`, aucun fournisseur payant, SMS et WhatsApp non activés) et s'ajoute si le parent l'a activée sur son téléphone. **Rien de sensible sur le canal externe (RV10, D69)** : l'alerte dit « Une absence a été signalée pour Alice. Ouvrez l'application pour le détail. », sans matière, heure, durée, motif, note ni montant (testé sur le contenu exact des alertes) ; le détail de la séance n'est visible que dans l'application connectée, le motif d'un changement ponctuel ou d'un justificatif n'est jamais transmis. Aucun type de notification financière n'existe (D70). **Ne bloque jamais la saisie** : les points d'entrée `NotificationsService.notifyAttendance/notifySessionChange/notifyTimetablePublished` attrapent toute erreur, les messages dans l'application sont créés tout de suite et l'envoi push part en arrière-plan (`idle()` l'attend dans les tests). Testé : service push qui refuse, qui lève une exception, mécanisme de notification entièrement en panne (l'appel est enregistré, la publication aboutit), appareil disparu (abonnement supprimé, la notification reste dans l'application). Le suivi d'envoi est conservé par notification (`pushStatut` : ENVOYE, ECHEC avec l'erreur, DESACTIVE, AUCUN_APPAREIL, GROUPE). **Qui reçoit (RV08, D67)** : seuls les responsables dont le compte est ACTIF et dont l'accès au portail n'est pas retiré pour CET enfant ; le retrait d'accès fait aussi disparaître de la liste les notifications déjà reçues sur cet enfant (elles contiennent son prénom). Testé, avec la mutation « filtre d'accès supprimé » qui fait échouer le test (un premier jet de ce test ne la voyait pas, à cause du regroupement : la notification existante absorbait la suivante, il marque désormais la première comme lue). **Événements** : ABSENCE et RETARD (uniquement ce qui vient de changer d'après l'état précédent de l'appel : un appel renvoyé à l'identique après une coupure ne renotifie personne), ENSEIGNANT_ABSENT (séance ANNULEE ou REMPLACEE), EMPLOI_DU_TEMPS_MODIFIE (changement de salle et publication d'une nouvelle version). Un changement sur un jour déjà passé n'alerte personne. Une annulation peut avoir une autre cause qu'une absence : le texte dit « cours annulé ou remplacé ». **Regroupement (D71)** : un enfant absent toute la journée ne fait pas six alertes : absences, retards et cours annulés du même enfant le même jour complètent une seule notification tant qu'elle n'est pas lue (une seule alerte push, le message dit « 2 absences ») ; les changements d'emploi du temps se regroupent pendant `School.notifGroupeMinutes` (60 minutes, provisoire, modifiable en base, pas encore d'écran de réglage). Une notification lue n'absorbe plus rien : l'événement suivant en crée une nouvelle et alerte. **Préférences** : par parent et par type d'événement (`ParentNotificationPreference`, absent = activé), respectées à l'envoi (`pushStatut = DESACTIVE`, le message reste dans l'application). **Appareils** (`ParentPushSubscription`, `endpoint` unique) : un téléphone partagé suit le parent qui vient de s'abonner, jamais le précédent, et la déconnexion retire l'appareil. **Modèle** (migration `20260921180000_lot12_notifications`, générée par `prisma migrate diff`, uniquement additive) : `ParentNotification`, `ParentNotificationPreference`, `ParentPushSubscription`, `School.notifGroupeMinutes`. Suppression en cascade avec le compte parent. Aucune nouvelle permission (les parents n'en ont pas). **Routes du portail** (`ParentAuthGuard`, jamais le jeton du personnel) : `GET /portal/notifications`, `GET /portal/notifications/unread-count`, `PATCH /portal/notifications/:id/read` (404 pour la notification d'une autre famille), `POST /portal/notifications/read-all`, `GET`/`PUT /portal/notification-preferences`, `GET /portal/push/public-key`, `POST /portal/push/subscriptions` (adresse https obligatoire ; un corps sans `keys` répondait 500, corrigé en 400), `POST /portal/push/unsubscribe`. Les accroches métier sont dans `AttendanceService.saveCall`, `OccurrencesService.createException` et `TimetablesService.publish`. **Variables d'environnement** : `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, documentées dans les deux `.env.example` et **déclarées dans le bloc `environment:` du service `academy-api` de `docker-compose.yml`** (sans cela elles n'atteignent pas le conteneur). Vides = aucune alerte push, les notifications restent dans l'application, sans erreur. Changer la paire de clés invalide tous les appareils déjà enregistrés. **Frontend** : cloche avec le nombre de non lues dans l'en-tête de l'espace parents (mise à jour toutes les minutes et à chaque changement de page), page `/parents/notifications` (liste, un clic ouvre la fiche de l'enfant et marque lue, « Tout marquer comme lu », réglage par type d'événement, activation des alertes sur ce téléphone avec l'explication d'installation sur iPhone/iPad et l'état « bloqué » du navigateur), gestionnaires `push` et `notificationclick` dans `public/sw.js`, `lib/push.ts`. **Ne fait pas encore** : écran de réglage de `notifGroupeMinutes`, vue du secrétariat sur les envois en échec (le suivi est en base), purge selon la durée de conservation (D77, aucune tâche planifiée), annonces (Lot 13), SMS et WhatsApp, notification quand un changement ponctuel est supprimé. 30 tests e2e ajoutés (`notifications.e2e-spec.ts`), 6 tests unitaires (`notification-texts.spec.ts`) ; 274 tests e2e et 6 tests unitaires du module passants, `tsc` propre sur les deux apps. Les tests utilisent un faux service push (`test/utils/fake-push-sender.ts`, branché par `createTestApp`) : aucun envoi réel en CI. **Vérifié dans le navigateur** (base jetable, supprimée ensuite) : activation d'un compte, absence et changement de salle saisis côté école, cloche à 2, page des notifications avec le détail et sans le motif interne, réglage « Absence signalée » désactivé et conservé en base, clic sur une notification (marquée lue, ouvre la fiche de l'enfant), déconnexion puis redirection vers la connexion, mise en page à 390 px sans défilement horizontal. **Preuve du vrai envoi** : un faux appareil FCM avec de vraies clés de chiffrement a été abonné, le vrai expéditeur (signature VAPID) a envoyé, le vrai service de Google a répondu que l'appareil n'existe pas, l'abonnement a été supprimé et la notification est restée dans l'application. **Non vérifié** : l'affichage d'une vraie alerte sur un vrai téléphone (le navigateur intégré refuse les notifications).
  **Déployé en production le 21 septembre 2026** : migration `20260921180000_lot12_notifications` appliquée au démarrage de l'API (tables vides, regroupement à 60 minutes), **paire de clés VAPID générée sur le serveur** (jamais affichée, écrite dans `.env.production` en mode 600, 3 lignes `VAPID_*`) : **ne plus la changer** une fois des parents abonnés, sinon tous leurs appareils devront se réabonner ; aucune nouvelle permission ; routes vérifiées (`/portal/notifications`, `/portal/notification-preferences` et `/portal/push/public-key` répondent 401 sans jeton de parent, `/parents/notifications` 200, `sw.js` servi avec les gestionnaires push), aucun avertissement « VAPID absentes » dans les logs, conteneurs Elyon non touchés. Aucun compte parent n'existe encore en production : les alertes n'ont donc pas pu être essayées avec un vrai téléphone. **Pour essayer** : activer un compte parent (Comptes parents), ouvrir l'espace parents sur un téléphone, page Notifications, « Activer les alertes sur ce téléphone », puis saisir une absence côté école. Sur iPhone, ajouter d'abord l'application à l'écran d'accueil.
- **Lot 13 : messagerie sécurisée et annonces (21 septembre 2026)** : les enseignants, la vie scolaire et la Direction échangent avec les responsables des élèves dans l'application, et publient des annonces de classe. **La règle centrale (RV09, D72) est vérifiée côté serveur à chaque ouverture ET à chaque envoi**, pas seulement à la création : un enseignant n'écrit qu'aux responsables d'un élève dont la CLASSE ACTUELLE lui est affectée, un parent n'écrit qu'aux enseignants affectés à la classe de SON enfant, jamais à un autre parent ; si l'enseignant perd son affectation, il ne peut plus écrire, le parent non plus (l'historique reste lisible et le parent est invité à écrire à l'école) ; on ne peut écrire qu'à un responsable au compte actif et à l'accès non retiré pour l'élève (le retrait d'accès de la Direction, D67, coupe la messagerie sur cet enfant des deux côtés et cache la conversation au parent). La vie scolaire et la Direction (`MESSAGE_DESK`) tiennent le « guichet de l'école » : conversation partagée entre elles, écriture à tout responsable, annonce pour toute classe. Un enseignant est relié à son compte par `Teacher.userId` (Lot 10) : sans ce lien il n'est pas proposé aux parents. Un test **casse volontairement la règle** (`teacherCanReach` qui répond toujours oui) : deux tests échouent. **Modèle** (migration `20260921190000_lot13_messagerie`, générée par `prisma migrate diff`, uniquement additive) : `MessageThread` (un responsable, un interlocuteur, un élève ; type ENSEIGNANT ou ECOLE ; dernière lecture de chaque côté), `Message` (retrait modéré : `retireAt/retireParId/retireMotif`, jamais supprimé), `MessageReport`, `Announcement` ; `School.messageDelaiReponseJours` (2) et `School.messageNumeroMinChiffres` (9), **valeurs provisoires modifiables en base, pas encore d'écran de réglage** ; deux types de notification (`MESSAGE_RECU`, `ANNONCE`). **Aucun numéro échangé ni affiché (RV09)** : aucune réponse de l'API ne contient de téléphone ni d'e-mail (testé sur treize réponses, y compris la supervision) ; un message qui contient une suite d'au moins 9 chiffres (« 06 12 34 56 78 », « +242 06 000 0001 », « 06.12.34.56.78 ») est refusé, alors qu'une date « 21.09.2026 », un montant ou un numéro de salle passent. Le contrôle ne voit pas un numéro écrit en toutes lettres. Un message refusé à l'ouverture ne laisse pas de conversation vide. **Supervision (D73, RV11)** : `MESSAGE_SUPERVISE`, Direction seule (ni un enseignant, ni la vie scolaire, ni l'administrateur : testé). La liste ne montre que des repères, jamais un extrait du texte ; **chaque ouverture d'une conversation est journalisée** (`MESSAGE_SUPERVISED_READ`, avec le nombre de messages, jamais leur texte). **Signalement et retrait (D75)** : un responsable ou un membre du personnel signale un message reçu (jamais le sien, une seule fois) ; la Direction traite les signalements et retire un message avec un motif obligatoire : le message reste en base, les participants voient « Ce message a été retiré par la Direction » sans le motif, la Direction voit le texte d'origine et le motif. Les annonces se retirent de la même façon (auteur ou Direction). Le journal ne contient jamais le texte d'un message. **Notifications (Lot 12 étendu)** : un nouveau message prévient SEUL le responsable destinataire (pas l'autre responsable de l'enfant), sans le contenu, ni sur l'alerte push (« Vous avez un nouveau message concernant Alice ») ni dans le titre de l'annonce ; plusieurs messages le même jour ne font qu'une notification et une alerte ; une annonce prévient les responsables des élèves de la classe (une notification par enfant, une seule alerte, regroupée par fenêtre) ; une panne de notification ne bloque pas l'envoi du message. Le personnel n'a pas d'alerte push : il voit un compteur dans la messagerie. **Permissions** : `MESSAGE_USE` (Enseignant, Direction, vie scolaire), `MESSAGE_DESK` (Direction, vie scolaire), `MESSAGE_SUPERVISE` (Direction). **Consentement** : la version de la politique passe à `2026-09-v2` (`CONSENT_VERSION` et page web, à garder identiques) : elle mentionne maintenant les notifications, la messagerie, la supervision par la Direction et le retrait modéré ; aucun compte parent n'existait en production, donc aucun parent n'a à reconfirmer. **Le texte reste un projet à faire valider par l'école ou un conseil juridique**, et l'information du PERSONNEL sur la supervision reste à intégrer à des conditions d'usage à rédiger. **Frontend** : espace parents `/parents/messages` (conversations, nouveau message avec choix de l'enfant puis de l'enseignant ou de l'école, avis de supervision et délai de réponse indicatif), `/parents/messages/[id]` (fil, réponse, signalement, lecture seule si l'enseignant n'est plus affecté), `/parents/annonces`, icône des messages non lus dans l'en-tête ; côté personnel `/messagerie` (conversations, nouveau message classe puis élève puis responsable, onglets Supervision et Signalements pour la Direction, avec l'avis « cette lecture est enregistrée ») et `/annonces`. **Ne fait pas encore** : pièces jointes (D75 : texte seul), écran de réglage du délai et du seuil de chiffres, alerte push pour le personnel, purge selon la durée de conservation (D77, aucune tâche planifiée), recherche dans le texte des messages, tableau de bord Direction (Lot 14). 32 tests e2e ajoutés (`messaging.e2e-spec.ts`) et 15 tests unitaires (`messaging.util.spec.ts`), 306 tests e2e au total, `tsc` et lint propres. **Vérifié dans le navigateur** (base jetable supprimée ensuite) : un parent refusé quand il écrit un numéro (message d'erreur clair), message envoyé à l'enseignant, l'enseignant le voit avec « 1 non lu » et répond, la Direction voit dans la supervision des repères sans texte, ouvre la conversation avec l'avis d'enregistrement (le journal contient bien `MESSAGE_SUPERVISED_READ` sans le texte), retire un message avec un motif (le parent voit « Ce message a été retiré par la Direction »), publie une annonce que le parent voit avec sa notification, et l'icône des messages non lus. **Non vérifié** : une vraie alerte sur un téléphone (déjà signalé au Lot 12).
